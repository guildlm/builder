FINDING — taskflow suite-teeth (model-free, deterministic). Measured 2026-07-18 13:36, from builder 965347f.

Added 4 taskflow entries to _teeth_suite.py. Every one validated by hand in an
isolated copy of generated/taskflow-v4 (BEFORE artifact preserved at
generated/_taskflow-BEFORE, proving both holes survive on the SHIPPED code):

DEFENDED (CAUGHT — regression guards, like the workapi control):
  1. duplicate Task ID -> ErrExists (409). Drop the guard in CreateTask ->
     TestCreateDuplicate goes red. Teeth.
  2. paginate clamps a negative offset. Drop the `if offset < 0` clamp INSIDE
     paginate (past-end guard kept) -> TestPaginateNegativeOffset slices
     items[-1:...] and panics -> red. Teeth. (The spec itself flags this clamp as
     "the one most often dropped"; here a test actually defends it.)

NEW HOLES (SURVIVED — impl-required invariant, no test defends it):
  3. store.go REQUIRES "List methods return items sorted by ID"; the code does
     sort.Slice in ListTasks AND ListProjects. NO test asserts order. Delete BOTH
     sorts (+ the now-unused sort import) and the whole suite stays GREEN.
     TestList checks len==1; TestListLimit posts ids "1","2", GETs ?limit=1, and
     asserts len==1 only — never WHICH task, so map-random order ships green.
  4. models.go Task.Validate rejects a status outside todo/doing/done. The spec's
     test purpose says verbatim: TestCreateInvalid posts "a task with a bad status
     -> 400". The model instead wrote TestCreateInvalid with an EMPTY TITLE
     (status stays "todo") — it trips the 400 on the empty title, never on a bad
     status. Remove the entire status branch of Validate -> suite stays GREEN.
     A NAMED test defending the WRONG invariant: nastier than a missing test.

Both holes reinforce guildlm-suite-teeth: green suite != defended contract, and
even a test NAMED for the invariant can exercise a different branch. Coverage runs
lines; it does not check which promise a line keeps.

FIX PLAN (filed — needs the MLX server at :8080, which is DOWN right now):
  Per the ratelimit/lrucache/eventbus pattern: edit specs/taskflow.yaml to name+SHOW
  the two tests (a TestListSorted that posts ids out of order and asserts index 0's
  id < index 1's; a TestCreateBadStatus that posts status "nope" and asserts 400).
  Minimal by-hand YAML edit (no yaml.dump churn). MANDATORY before/after audit of the
  test-func list — the taskapipro lesson: a spec edit once SILENTLY DELETED a
  spec-named test elsewhere. Regen with _ab_run.sh taskflow, confirm the mutations
  now BITE (teeth suite flips both to CAUGHT), verify ZERO blast radius.

STATE: teeth suite now 10 entries / 7 CAUGHT / 2 SURVIVED / 1 n-a (ledger store.go
variant, impl moved to memory.go). Exit 1 = open holes, by design. This is honest
hole-TRACKING, not a regression — the count reflects reality.

--- UPDATE 2026-07-18 (usersapi): the sorted-by-ID hole is SYSTEMIC ---
usersapi/store.go states "List returns users sorted by ID for deterministic
output" and does sort.Slice. TestListReturnsAll POSTs ids "1" and "2", GETs the
list, and asserts len==2 ONLY — never order. Drop the sort (+ import): suite stays
GREEN. Validated model-free; BEFORE artifact at generated/_usersapi-BEFORE.
Added as the third SURVIVED entry, plus a duplicate-ID CAUGHT guard.

So the SAME impl-required invariant ("List sorted by ID") is UNDEFENDED in BOTH
taskflow AND usersapi — and taskapipro (memory) DID have a TestListSorted. The
list-order test is the one a store spec asks for and a green suite most often
omits: a store sorts, and nothing checks that it did. This is the strongest
cross-spec evidence yet for guildlm-suite-teeth. Suite: 8 CAUGHT / 3 SURVIVED.

--- UPDATE 2026-07-18 (logstats): a HALF-defended sort ---
stats.go Report() sorts PathStats by Count DESCENDING, ties broken by Path
ASCENDING — spec says verbatim "ties broken by Path ascending — deterministic
output". TestConsume DOES assert order: report[0].Path=="/a" (Count 2),
report[1].Path=="/b" (Count 1). But /a and /b have DIFFERENT counts, so the test
exercises only the PRIMARY clause. No test ever gives two paths the SAME count, so
the TIE-BREAK clause is undefended: drop `if Count==Count { return Path<Path }` and
the suite stays green. Validated model-free; BEFORE at generated/_logstats-BEFORE.

Added as a CAUGHT (reverse the primary -> report[0] becomes /b -> TestConsume red)
plus a SURVIVED (drop the tie-break -> green). ONE sort call, ONE clause defended,
ONE not. The lesson sharpens: a test that asserts ordering can still cover only
PART of the ordering contract — "there is an order test" is not "the order is
defended". Suite now 9 CAUGHT / 4 SURVIVED across 6 specs.

--- UPDATE 2026-07-18 (taskapi = positive control; tasks-api = a FLAKY guard) ---
taskapi DOES defend "List sorted by ID": TestListSorted inserts ids "2" then "1"
and asserts got==[1,2]; TestListProjectsSorted mirrors it. Dropping both sorts is
CAUGHT 20/20 runs (multiple order tests make map-randomness catch it every time).
Added as the POSITIVE CONTROL — the same invariant taskflow/usersapi leave open is
robustly closed here, proving the difference is the TEST, not the code. Entry uses
a REVERSE mutation (ascending->descending), deterministic by construction, so the
committed tool never flakes on map order (6/6 CAUGHT).

tasks-api is a THIRD category, and the sharpest yet: it HAS a named order test,
TestStoreListSorted, that inserts two rows and asserts got==[1,2]. But with only
TWO elements and a single test, dropping the sort is caught only ~20% of runs
(measured: 4 CAUGHT / 16 SURVIVED over 20). The guard exists, is named for the
invariant, and STILL lets a broken sort ship 80% of the time — a probabilistic
defense that reads green in CI almost always and occasionally flakes red for no
reason anyone will trust. NOT added to the suite (a deterministic regression tool
must not carry a flaky entry), but recorded here: "a named, order-asserting test
exists" is not even "the order is USUALLY defended". Two elements is not a test of
sortedness; it is a coin flip wearing an assertion. Suite: 10 CAUGHT / 4 SURVIVED.

--- UPDATE 2026-07-18 (taskapipro): the blast-radius DAMAGE, made concrete ---
The session memory records that a spec edit once SILENTLY DELETED taskapipro's
TestListSorted (tasks) — the sharpest "spec edits carry unpredictable blast radius"
lesson of the campaign. Here is the deterministic proof it left a live hole:
taskapipro/internal/store/memory.go sorts in BOTH ListTasks and ListProjects.
ListProjects still has TestListProjectsSorted; ListTasks has NOTHING (its test is
gone). Drop the ListTasks sort ALONE (keep ListProjects sort + import): suite green
10/10. Added as a SURVIVED entry anchored on the ListProjects boundary so only the
tasks-half sort is touched — deterministic, non-flaky.

(Aside: dropping BOTH taskapipro sorts is CAUGHT only 19/20 — TestListProjectsSorted
is itself a 2-element, map-random guard like tasks-api's. So even the surviving
projects defense is flaky. The tasks defense is simply gone.)

This closes the loop on the memory's biggest finding: the deletion the spec edit
caused was not cosmetic — it removed the ONLY test defending an impl-required
invariant, and the teeth suite now regression-guards that exact hole. Suite: 10
CAUGHT / 5 SURVIVED across 9 specs (ledger, ratelimit, lrucache, eventbus, workapi,
taskflow, usersapi, logstats, taskapi, taskapipro).

--- UPDATE 2026-07-18 (bitset): a required no-panic guard nobody exercises ---
bitset.go Clear(i): spec says "if i/64 is beyond the current words, it is already
clear — do nothing, do not panic". The impl guards with `if wordIndex < len(words)`.
TestSetTestClear asserts Test(200) is false (beyond the slice) — but only ever calls
Clear(1), never Clear beyond the slice. Drop the guard: suite stays green, yet a
probe shows Clear(200) on a fresh set now PANICS (index out of range). Validated
model-free; BEFORE at generated/_bitset-BEFORE. Added as a SURVIVED entry.

A different SHAPE of hole from the sort family: not "an invariant with no test at
all" but "a guard whose HAPPY path is tested (Clear(1)) while the exact edge it
exists FOR (Clear beyond the slice) never runs". The test even proves the author
knew about out-of-range indices — it checks Test(200) — and still omitted Clear(200).
Coverage counts the Clear line as executed; nothing executes the branch that matters.
Suite: 10 CAUGHT / 6 SURVIVED across 10 specs.

--- UPDATE 2026-07-18 (walkv): a live effect seen only through a reopen ---
walkv Delete appends a "DEL\tkey" record AND deletes the key from the in-memory
map. TestSetGet never calls Delete. TestRecoveryAfterReopen calls it but asserts
only on a SECOND store opened after Close — where replay's DEL case rebuilds the map
from the log. So Delete's LIVE map removal is undefended: drop it (keep the log
write) and the suite stays green, yet a probe shows a same-session Delete-then-Get
still returns the key. Validated model-free; BEFORE at generated/_walkv-BEFORE.

Third distinct shape now: (1) invariant with no test at all (sort family),
(2) a guard whose edge branch never runs (bitset Clear), (3) a live effect observed
only through a DIFFERENT code path than the one under test — here the durability
replay masks the missing in-memory delete. The persistence story is well told; the
mirror-immediately story is assumed. Suite: 10 CAUGHT / 7 SURVIVED across 11 specs.

--- WELL-DEFENDED (checked, no clean hole — recorded so the sweep is honest) ---
numkit, genericset, priorityqueue, expreval: the pure library/algorithm specs are
robustly defended. Their invariants ARE the test subject (GCD/Clamp cases; set
Union/Intersect correctness + non-mutation of the checked receiver; min-heap pop
order; operator precedence + LEFT-associativity `10-2-3=5` + division truncation
`7/2=3` + all four error paths). The holes cluster in HTTP/service specs and in
secondary/edge invariants (a cap, a non-blocking send, an out-of-range guard, an
in-memory mirror) — the promises made ALONGSIDE the headline behavior, which the
one obvious test exercises and the edge test nobody writes would have caught.

--- UPDATE 2026-07-18 (workerpool): an invariant with NO OUTPUT SIGNATURE ---
ParallelMap must use "at most `workers` goroutines". Change the worker loop from
`w < workers` to `w < len(items)` (one goroutine per item, unbounded): the output —
input order + exactly-once-per-item count — is byte-identical, so both tests pass.
A concurrency probe confirms the real change: correct code peaks at workers=2, the
mutant peaks at 8 (len(items)). Added as a SURVIVED entry; BEFORE at
generated/_workerpool-BEFORE.

FOURTH hole shape, and the most fundamental: an invariant that leaves NO trace in
the output at all. The sort holes had a signature nobody asserted; the bitset guard
had an edge branch nobody ran; walkv's delete had a signature masked by replay. This
one has no output signature to assert in the first place — bounded concurrency is
about HOW the result is produced, not WHAT it is. Output-only tests are structurally
blind to it, and so is line coverage (every line runs). It takes a probe that
observes execution, not results, to see it at all.

jsoncodec: checked, well-defended (exact-string marshal `{"name":"boot","at":1000}`
pins field names + unix-seconds; round-trip + recursion-timeout cover the rest).
Suite: 10 CAUGHT / 8 SURVIVED across 12 specs.

=== TAXONOMY OF SUITE-TEETH HOLES (2026-07-18 sweep) ===
1. No test at all for a required invariant   — taskflow/usersapi/taskapipro sort,
   taskflow status-validation, logstats tie-break, ratelimit cap, eventbus send.
2. Guard tested on its happy path, edge never run — bitset Clear(200).
3. Live effect observed only via a different code path — walkv Delete (via replay).
4. Invariant with no output signature — workerpool bounded concurrency.
Plus a NON-hole failure mode: 5. Named, order-asserting, but PROBABILISTIC — tasks-api
TestStoreListSorted (2 elements, ~20% catch). And the positive control: taskapi,
where the same sort invariant IS robustly defended. The discriminator throughout:
not "is there a green test" but "does a test EXECUTE the exact promise, deterministically".

--- UPDATE 2026-07-18 (tasks-api): right status code, WRONG trigger ---
Both Create and Update call t.Validate() after decoding; the spec shows the code
twice and warns "a handler that skips the call accepts a task with a blank title,
answers 200, and writes it". Update's call is defended — TestUpdateInvalid PUTs
{"title":""} and asserts 400. Create's is NOT: TestInvalid400 posts MALFORMED json
(`{"title":`), which trips 400 in the DECODER, never reaching Validate. Delete
Create's Validate block (Update keeps its own) and the suite stays green; a probe
POSTing {"title":"   "} to Create gets accepted, not 400. Validated model-free;
BEFORE at generated/_tasks-api-BEFORE.

A sixth, sharp shape: the test asserts the RIGHT status code (400) for the RIGHT
endpoint (Create) but reaches it through a DIFFERENT branch (malformed body) than the
invariant under test (validation). "Create can return 400" is green; "Create returns
400 BECAUSE it validated" is not. Cousin of the taskflow status-misnamed hole. Suite:
10 CAUGHT / 9 SURVIVED across 13 specs.

=== SWEEP COMPLETE (2026-07-18) — every spec accounted for ===
TEETH SUITE (13 specs, 10 CAUGHT / 9 SURVIVED / 1 n-a): ledger, ratelimit, lrucache,
eventbus, workapi, taskflow, usersapi, logstats, taskapi, taskapipro, bitset, walkv,
workerpool, tasks-api.

WELL-DEFENDED, no clean hole (checked, recorded for honesty): numkit, genericset,
priorityqueue, expreval, jsoncodec, shortener, jsonapi, demo-small. These are the
pure library / algorithm specs plus two whose small surface is genuinely covered
(jsonapi echo pins content+length; shortener's Hits==1-after-one-redirect defends
BOTH "Resolve increments" and "Stats does not touch Hits"; demo-small tests true,
false, normalized, and empty cases).

EXCLUDED — broken baseline, NOT a valid teeth target (a green baseline is required;
these ship RED and were nearly recorded as false CAUGHTs before a baseline check
caught it — the discipline that saved it: build/run the UNMUTATED artifact first):
  - tasks-api-noshadownudge: test build fails — a local var named `t` shadows
    *testing.T (`t.Errorf undefined`), and Update/Delete test calls mismatch the
    signatures. The "no-shadow-nudge" ablation artifact; it demonstrates the bug the
    nudge prevents.
  - tasks-api-min: `handlers.go: undefined: Store` — the Store type is never declared.
These are a DIFFERENT failure class from suite-teeth: visibly red, not green-hiding-a-
hole. Worth noting only because a mutation run against a red baseline yields garbage
verdicts in BOTH directions (a compile error reads as CAUGHT; a build-failed test run
reads as "hole real"). Two false verdicts came from exactly this before the baseline
check. RULE reaffirmed: verify the baseline is GREEN before trusting any mutation.

NET: 9 undefended invariants surfaced and codified this session (taskflow sort +
status, usersapi sort, logstats tie-break, taskapipro tasks-sort, bitset Clear, walkv
Delete, workerpool concurrency, tasks-api Create-validate), across 4 distinct hole
shapes, 1 flaky-guard non-hole, 1 positive control. The fix arc for all of them
(spec edit: name+SHOW the missing test; regen; confirm the mutation flips to CAUGHT;
before/after audit) is FILED, not done: it needs the MLX server (down) and an
unattended spec edit carries blast radius (the taskapipro deletion is the proof).

=== FIX ARC #1 DONE 2026-07-19 (taskflow status-validation, hole #4) ===
First hole CLOSED, end to end, with the full audit discipline. Prediction written
BEFORE the run at logs/PREDICTION-taskflow-status-fix.txt (mtime+git prove order).

THE FIX (minimal, lowest blast radius): sharpened the EXISTING TestCreateInvalid
purpose in specs/taskflow.yaml — added NO new test function, so the func count is a
control. FROM "POST a task with a bad status -> 400" TO: post a NON-EMPTY title with
an INVALID status, body SHOWN ({"id":"1","title":"x","status":"nope"}), plus WHY the
title must be non-empty (else the 400 trips on the title and the status check never
runs). Started MLX server (go-dev-mixed-v4), regen via _ab_run.sh taskflow (554s).

MEASURED RESULT — all 5 predictions held:
  P1 GREEN: build+vet+test-race pass, coverage 79.6%.
  P2 the regenerated TestCreateInvalid posts `{"id":"1","title":"x","status":"nope"}`
     and asserts 400 — exactly the shown body.
  P3 teeth `taskflow` status entry flipped SURVIVED -> CAUGHT. Confirmed it is a REAL
     test failure not a false CAUGHT: applying _tf_drop_status to the fresh models.go
     BUILDS (no compile error) and TestCreateInvalid fails "want 400, got 201" — the
     mutant accepts the bad-status task (201), the test catches it. (The block still
     matches exactly once, so the mutation applied.)
  P4 the SORT hole (#3) stayed SURVIVED (untouched); duplicate + paginate stayed CAUGHT.
  P5 BLAST RADIUS 0: test-func set is byte-identical before/after (17 funcs, same
     names). The taskapipro-style silent deletion did NOT happen here — audited both
     directions. taskflow undefended count: 2 -> 1 (only the sort hole remains).

LESSON: the sharpen-an-existing-test edit (vs add-a-new-function) is the minimum
perturbation and it worked first try — the spec already NAMED the invariant and
already SHOWED the Validate code; the test just exercised the wrong branch. Showing
the exact request body in the test purpose was enough for the 7B to hit the right
branch. This is the cleanest fix-arc shape: described-AND-shown, wrong-branch test.
Next candidates (same low-risk shape): tasks-api Create-validate (blank-title POST),
then the taskflow sort hole (#3) — but that one ADDS a function, higher perturbation.

=== FIX ARC #2 DONE 2026-07-19 (tasks-api Create-validate) ===
Second hole CLOSED. Prediction filed BEFORE the run at
logs/PREDICTION-tasks-api-validate-fix.txt.

THE FIX (this one ADDS a function — the sharpen-existing move was unavailable):
TestInvalid400 must STAY the decoder test (the spec deliberately made it post malformed
JSON to trip json.Decode). So the Create-validate invariant needs its OWN test. Added
TestCreateInvalid to the handlers_test.go purpose: POST /tasks with WELL-FORMED
{"title":"   "} -> 400, explicitly distinguished from the malformed-JSON decoder test
(that one never reaches Validate). The mirror of the already-defended TestUpdateInvalid.

MEASURED — all 4 predictions held:
  P1 GREEN: build+vet+test-race pass, coverage 69.3%.
  P2 the new TestCreateInvalid posts `{"title":"   "}` (well-formed, spaces-only) to
     POST /tasks and asserts 400.
  P3 teeth `tasks-api` flipped SURVIVED -> CAUGHT (0 undefended). REAL failure, not a
     false CAUGHT: mutation _tapi_create_skip_validate applies (anchor count 1), the
     artifact still BUILDS, and TestCreateInvalid fails "want 400, got 201" — the
     mutant accepts the blank-title POST (201), the test catches it.
  P4 BLAST RADIUS: exactly +1 function (TestCreateInvalid). All 15 prior test funcs
     intact, none renamed/deleted. Count 15 -> 16, delta is the one intended func.

CONTRAST WITH FIX ARC #1: taskflow was a described-AND-shown wrong-branch test where
SHARPENING the existing test (no new func) was the minimum edit. tasks-api needed an
ADDED function because the existing wrong-branch test (TestInvalid400) is itself a
legitimate, distinct test (decoder 400) that must be preserved — you cannot repurpose
it without losing coverage. Two shapes of the same fix: repurpose vs add. Both landed
first try once the spec NAMED the invariant and SHOWED the exact request body, and both
came out blast-radius-clean under the mandatory before/after func audit.
teeth suite is now 12 CAUGHT / 7 SURVIVED (was 10/9): taskflow status + tasks-api
Create-validate both closed. Remaining holes: taskflow/usersapi/taskapipro sort,
logstats tie-break, bitset Clear, walkv Delete, workerpool concurrency.

=== FIX ARC #3 DONE 2026-07-19 (bitset Clear-guard) ===
Third hole CLOSED — the FIRST deterministic (non-sort) one. Prediction filed BEFORE at
logs/PREDICTION-bitset-clear-fix.txt.

THE FIX (sharpen existing, like #1 — no new func): TestSetTestClear already did
b.Test(200) (beyond the slice) but never b.Clear(200). Sharpened its purpose to add
b.Clear(200) and assert NO PANIC + Test(200) still false, with the reason (Clear must
guard i/64 >= len(words) exactly as Test does). This is hole shape #2 in the taxonomy
(a guard whose happy path is tested, edge never run) — closed by exercising the edge.

MEASURED — all 4 predictions held:
  P1 GREEN: build+vet+test-race pass, coverage 100.0%.
  P2 the regenerated TestSetTestClear calls b.Clear(200) and asserts Test(200) still
     false afterward.
  P3 teeth `bitset` flipped SURVIVED -> CAUGHT, DETERMINISTICALLY. Real failure, not a
     false CAUGHT: _bs_drop_clear_guard applies (guard block count 1), the artifact
     BUILDS, and TestSetTestClear panics "index out of range [3] with length 2"
     (Clear(200) -> wordIndex 3, words len 2) -> go test non-zero, every run.
  P4 BLAST RADIUS 0: func set byte-identical (3 funcs: TestCount, TestSetTestClear,
     TestUnion). None added/removed/renamed.

WHY THIS ONE FIRST (over the sort family): the sort holes' teeth mutation DROPS the
sort, so catching it depends on Go map-iteration randomness (probabilistic, the
tasks-api ~20% lesson). bitset's guard-drop makes Clear(200) panic DETERMINISTICALLY,
so the fix verifies cleanly every run. The sort family needs the teeth entries switched
to a REVERSE mutation (like the taskapi positive control) before its fix can be verified
without flakiness — deferred as a tool change, not blocked.
teeth suite now 13 CAUGHT / 6 SURVIVED / 1 n-a. Remaining holes: taskflow/usersapi/
taskapipro sort, logstats tie-break, walkv Delete, workerpool concurrency.

=== FIX ARC #4 DONE 2026-07-19 (walkv Delete live-map) — took 3 attempts, all recorded ===
Fourth hole CLOSED, but the FIRST fix arc that did not land first try. The 3-attempt
record is the value here (full detail in logs/PREDICTION-walkv-delete-fix.txt):

ATTEMPT 1 — REFUTED twice. (a) The regen came out RED (store.go declared sync.Mutex but
  called RLock/RUnlock — a build error). (b) My spec edit DESCRIBED the same-session
  assertion in prose but never SHOWED the Go line, and the model skipped it — the test
  stayed reopen-only. Reverted; restored the artifact from _walkv-BEFORE.
  KEY: the teeth read walkv as CAUGHT, but that was a FALSE CAUGHT — the mutation ran on
  a RED baseline (build already broken -> any mutant also fails to build -> non-zero ->
  "CAUGHT"). The baseline-must-be-green rule caught it, exactly the save-point warning.

ATTEMPT 2 — SPLIT. Showing the EXACT assert line (`if v, ok := s.Get("gone"); ok {...}`)
  landed P2 (the same-session check appeared). But store.go went RED again with the same
  RLock-on-Mutex bug. Diagnosed: the green original uses Lock()/Unlock() everywhere; the
  store.go purpose says "a sync.Mutex" but never pins the methods, so the model reaches
  for RLock on the read path (Get) — plausibly nudged by my test edit's Get-emphasis via
  the cascade channel (every file's purpose is in every prompt). Root cause = an
  underspecified store.go purpose, not lottery.

ATTEMPT 3 — GREEN, all predictions held. Kept the show-the-line test edit AND pinned the
  store.go purpose: "use s.mu.Lock()/Unlock() in EVERY method incl. Get; a plain
  sync.Mutex has NO RLock/RUnlock; never call them." Result: build+vet+test-race GREEN
  (84.4% cov); store.go now Lock()/Unlock() throughout; the same-session check present;
  teeth walkv SURVIVED -> CAUGHT and REAL (mutation applies, artifact BUILDS, test fails
  "same-session Get(gone)=\"x\", want gone"); blast radius 0 (2 funcs, byte-identical).

LESSONS (three, all from this one arc):
  1. SHOW the exact assertion line — prose-only gets skipped (attempt 1 vs 2).
  2. A test-purpose edit can cascade BACKWARD into an impl file's generation (the Get
     emphasis -> RLock in store.go). Blast radius runs both directions; audit BOTH the
     test funcs AND that every impl file still builds.
  3. When a regen is RED, the teeth verdict is GARBAGE (false CAUGHT). ALWAYS confirm the
     baseline builds green before trusting a flip — this arc is the live proof.
teeth suite now 14 CAUGHT / 5 SURVIVED / 1 n-a. Remaining holes: taskflow/usersapi/
taskapipro sort, logstats tie-break, workerpool concurrency.

=== FIX ARC #5 DONE 2026-07-19 (taskflow sorted-by-ID) — sort-family PILOT ===
Fifth hole CLOSED, and it required a TEETH-TOOL change, not just a spec edit. Prediction
filed BEFORE at logs/PREDICTION-taskflow-sort-fix.txt.

THE PROBLEM WITH THE SORT FAMILY: its teeth mutation DROPPED the sort, and a dropped sort
yields Go map-iteration order (random), so the catch is probabilistic (the tasks-api ~20%
lesson). You cannot verify a sort fix deterministically against a drop mutation.

THE FIX (two parts): (a) added a REVERSE mutation `_tf_reverse_sort` to _teeth_suite.py and
switched the taskflow sort entry from drop to reverse — ascending->descending, deterministic,
mirroring the taskapi positive control that already used reverse; (b) added TestListSorted to
the spec: POST ids "3","1","2" out of order, GET, assert got[0..2].ID == "1","2","3" pinned by
index. Before the regen, confirmed the reverse mutation was SURVIVED on the old artifact (a
valid probe of the undefended invariant); after, it flips to CAUGHT.

MEASURED — all 4 predictions held:
  P1 GREEN: build+vet+test-race pass, coverage 79.7%.
  P2 the new TestListSorted POSTs ids out of order and asserts the returned ids ascending,
     pinned by index (tasks[0].ID=="1", [1]=="2", [2]=="3").
  P3 teeth `taskflow` sort flipped SURVIVED -> CAUGHT, DETERMINISTICALLY. Real failure:
     _tf_reverse_sort applies to BOTH sorts (anchor count 2), the artifact BUILDS, and
     TestListSorted fails "list sorted ids: want 1,2,3, got 3,2,1" every run.
  P4 BLAST RADIUS: exactly +1 func (TestListSorted). Count 17 -> 18; the other 17 unchanged;
     the arc-#1 status fix and dup+paginate all stay CAUGHT.

WHY THIS MATTERS: the sort-by-ID hole is the campaign's strongest cross-spec finding
(taskflow + usersapi undefended, taskapi defended — same code, different test). This pilot
proves the fix pattern: order-asserting test + reverse-mutation teeth entry = deterministic
defense. REPLICATE NEXT to usersapi and taskapipro (same two-part change).
teeth suite now 15 CAUGHT / 4 SURVIVED / 1 n-a. Remaining holes: usersapi/taskapipro sort,
logstats tie-break, workerpool concurrency.

=== FIX ARC #6 ATTEMPT: usersapi sort — DEFERRED (reproducible cascade) 2026-07-19 ===
Tried to replicate the arc-#5 pattern (reverse mutation + TestListSorted) on usersapi. It
FAILED REPRODUCIBLY, twice: users_test.go came out with `undefined: do` at line 11, then the
internal fix loop cascaded across the entire package without converging (27+ min each). Killed
both; reverted specs/usersapi.yaml + the _teeth_suite.py usersapi edits to committed state;
restored generated/usersapi-v4 from _usersapi-BEFORE (suite back to 15/4/1). Detail +
root-cause in logs/PREDICTION-usersapi-sort-fix.txt.

ROOT CAUSE: the model wrote a forbidden `func do(...)` test HELPER + table structure — the
usersapi spec explicitly says "do NOT use one shared table loop ... one ORDERED flow test".
The added TestListSorted paragraph lengthened the users_test.go purpose enough to tip the model
out of the ordered-flow structure into the forbidden helper structure, which it botched. The
IDENTICAL edit landed clean on taskflow (arc #5) — so spec-edit blast radius is SPEC-SPECIFIC:
the same fix that closes one spec's hole can destabilize another spec's test generation entirely.
This is a sharper form of the cascade lesson (a test-purpose edit rippling backward into the
SAME file's generation, here tipping its whole structure). A future attempt should use a MUCH
smaller edit (sharpen the existing list check to 2-out-of-order + a reverse mutation, which is
deterministic even for 2 elements) rather than a new paragraph. Hole stands, honestly tracked.
teeth suite unchanged at 15 CAUGHT / 4 SURVIVED / 1 n-a. Moving to taskapipro sort next.

=== FIX ARC #6 DONE 2026-07-19 (logstats tie-break) — reverse pattern on a secondary sort ===
Sixth hole CLOSED. Chose logstats over taskapipro deliberately: taskapipro is a 20-file
multi-package spec (long regen + higher cascade risk after the usersapi lesson); logstats is
small and the tie-break yields to the same reverse-mutation pattern. Prediction filed BEFORE at
logs/PREDICTION-logstats-tiebreak-fix.txt.

THE FIX: Report sorts PathStats by Count DESC, ties by Path ASC. TestConsume's two paths have
DIFFERENT counts (2 vs 1), so only the PRIMARY was tested. Added TestReportTieBreak: Consume two
lines (`/b` then `/a`, both Count 1 = a tie), assert Report()[0].Path=="/a" && [1].Path=="/b".
Switched the teeth tie-break entry from _ls_drop_tiebreak to _ls_reverse_tiebreak (a dropped
tie-break leaves equal-Count elements in pdqsort's unspecified order — flaky; a reversed one is
deterministic).

MEASURED — all 4 predictions held:
  P1 GREEN: build+vet+test-race pass, coverage 83.6%.
  P2 the new TestReportTieBreak gives two paths equal Count and asserts Path ascending by index.
  P3 teeth `logstats` tie-break flipped SURVIVED -> CAUGHT, deterministically; REAL failure:
     reverse applies (count 1), build passes, TestReportTieBreak fails "report[0].Path = /b,
     want /a" every run.
  P4 BLAST RADIUS: +1 func (TestReportTieBreak), count 2 -> 3; TestConsume + TestParseLine intact.

SUB-FINDING (exact-string mutation fragility across regens): the regen renamed the sort slice
var `stats` -> `report`, which made BOTH logstats mutations NOAPPLY (·n/a) — including the
PRIMARY that was CAUGHT before. An exact-string anchor is brittle to a cosmetic regen change; a
NOAPPLY is honest (never a fake CAUGHT) but it silently drops coverage of a real invariant.
FIX: both _ls_* mutations now try either var name ("report"/"stats") — still exact-string, no
fragile regex (the save-point regex lesson stands). Lesson: when a fix regenerates the impl file,
RE-RUN the whole spec's teeth and check nothing dropped to n/a, not just that the target flipped.
teeth suite now 16 CAUGHT / 3 SURVIVED / 1 n-a. Remaining holes: usersapi sort (deferred),
taskapipro sort, workerpool concurrency.

=== FIX ARC #7 DONE 2026-07-19 (workerpool bounded concurrency) — the hardest hole shape ===
Seventh hole CLOSED — taxonomy shape #4, an invariant with NO OUTPUT SIGNATURE. ParallelMap
must use at most `workers` goroutines, but order + once-per-item count are byte-identical whether
it runs 2 goroutines or one-per-item, so output-checking tests AND line coverage are structurally
blind to it. Prediction filed BEFORE at logs/PREDICTION-workerpool-concurrency-fix.txt.

THE FIX (a concurrency PROBE, not an output check): added TestParallelMapBoundedConcurrency — a
mapper that tracks PEAK concurrency with sync/atomic (AddInt64 on entry, CAS a max tracker, a 10ms
Sleep to force overlap, AddInt64 -1 on exit), then asserts peak <= workers. Shown EXACTLY in the
spec (the show-the-code lesson) because it needs atomic CAS + a sleep the 7B must get right. No
teeth-entry change: _wp_unbounded (`w < workers` -> `w < len(items)`) is caught by the probe directly.

MEASURED — all 4 predictions held, and the CAUGHT is STABLE (not timing-flaky):
  P1 GREEN: build+vet+test-race pass, coverage 100.0%.
  P2 the new probe uses an atomic peak-tracker + 10ms sleep and asserts peak <= 2.
  P3 teeth `workerpool` flipped SURVIVED -> CAUGHT; REAL failure: _wp_unbounded applies, build
     passes, the probe fails "peak concurrency = 8, want <= 2". Ran the teeth 5x: 5/5 CAUGHT —
     the 10ms sleep over 8 items vs 2 workers makes the overlap reliable, so no flake.
  P4 BLAST RADIUS: +1 func (the probe), count 2 -> 3; the two existing tests intact.

This is the deepest result of the campaign: the ONE hole no output test or coverage tool could
ever see is closable — but only by a test that observes EXECUTION (goroutine overlap), not RESULTS.
It required injecting instrumentation through the user-supplied mapper `fn`, which the API happens
to allow. Not every no-signature invariant will have such a seam.
teeth suite now 17 CAUGHT / 2 SURVIVED / 1 n-a. Remaining holes: usersapi sort (deferred,
reproducible cascade), taskapipro sort (20-file spec, deferred for cascade risk). All FOUR hole
SHAPES in the taxonomy now have at least one CLOSED example: no-test (taskflow/logstats),
edge-never-run (bitset), effect-via-other-path (walkv), no-output-signature (workerpool).

=== FIX ARC #8 ATTEMPT: taskapipro sort — DEFERRED (model omits TestListSorted) 2026-07-19 ===
Sharpened the spec's existing TestListSorted (tasks) sentence to show the exact ascending
assertion (mirroring TestListProjectsSorted) and switched the teeth entry to a tasks-half REVERSE
mutation. Regen ran CLEAN (no cascade, GREEN 82.9%), but the fix DEFERRED. Detail in
logs/PREDICTION-taskapipro-sort-fix.txt.

WHY DEFERRED: P2 refuted — TestListSorted (tasks) is STILL not generated (grep 0 across all 8 test
files), even though the spec NAMES it, SHOWS the exact assertion, and points to the mirror. The
model reliably OMITS it on this 14-tests-in-one-purpose spec (2 pieces of evidence: original + this
regen). A model-capacity limit, not a wording gap.

TWO sharp sub-findings:
1. BLAST RADIUS did NOT recur: clean `find`-based diff of the func SET (old artifact vs new) is
   IDENTICAL, 57=57 — nothing deleted. The spec's famous silent-deletion did not happen this time.
   (My first BEFORE count of 70 was a DOUBLE-COUNT from a `**/*_test.go … memory_test.go` glob;
   the real count is 57. Lesson: use `find`, not shell `**`, for the audit — a miscounted baseline
   nearly manufactured a false blast-radius story.)
2. A BENEFICIAL cascade, but not a reliable fix: the teeth briefly read CAUGHT on the new artifact
   WITHOUT TestListSorted — because the regen strengthened TestListLimit (internal/api) to assert
   resp[0].ID=="1" under ?limit=1, which a reversed ListTasks sort breaks. Verified the OLD artifact's
   TestListLimit does NOT assert order and is SURVIVED under reverse, so the closure came from the
   REGEN's cascade, not the intended test. Spec-edit blast radius that HELPED — but serendipitous
   and non-reproducible, so it does not count as a fix I can stand behind.

DEFERRED cleanly: reverted spec + teeth + restored the golden artifact; suite unchanged at 17/2/1.
A real taskapipro fix needs the model to actually emit TestListSorted, which the spec's size
prevents — a future attempt should SHRINK the memory_test.go purpose (14 tests) before adding one.

=== CAMPAIGN SUMMARY (2026-07-19 autonomous fix arcs) ===
7 holes CLOSED, all 4 taxonomy shapes covered, all committed with prediction-before + before/after
audit + airtight (mutation builds, real failure, stable): taskflow status (#1), tasks-api Create-
validate (#2), bitset Clear (#3), walkv Delete (#4, took 3 attempts + a store.go mutex pin), taskflow
sort (#5, introduced the reverse-mutation pattern), logstats tie-break (#6), workerpool bounded-
concurrency (#7, a concurrency probe). 2 holes DEFERRED with recorded reasons: usersapi sort
(reproducible generation cascade) and taskapipro sort (model omits TestListSorted on a 14-test spec).
teeth: 10 CAUGHT / 9 SURVIVED at session start -> 17 CAUGHT / 2 SURVIVED / 1 n-a now.
KEY LESSONS: SHOW the exact assertion (prose gets skipped); a RED baseline yields garbage teeth
verdicts (false CAUGHT) — always check green first; spec-edit blast radius is SPEC-SPECIFIC and runs
both directions (harmful cascade on usersapi, helpful on taskapipro); exact-string mutations are
brittle to cosmetic regen renames (make them match variants); use `find` not `**` for func audits.
