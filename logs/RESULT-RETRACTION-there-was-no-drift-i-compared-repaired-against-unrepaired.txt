RETRACTION — there was no within-process drift. I compared REPAIRED files against UNREPAIRED ones.
===================================================================================================
31 July 16:02. Retracts logs/RESULT-generation-drifted-in-one-process-and-the-gates-repaired-it.txt
and everything built on it.

THE MEASUREMENT THAT ENDS IT — every as-drawn snapshot, read from .pre-fix.json
    ctl-1  store.go  7c9f06c7  377B     models_test.go  006ed8a6  1562B
    ctl-2  store.go  7c9f06c7  377B     models_test.go  006ed8a6  1562B
    ctl-3  store.go  7c9f06c7  377B     models_test.go  006ed8a6  1562B
    arm-1  store.go  7f2e9f83 2263B     models_test.go  006ed8a6  1562B
    arm-2  store.go  7f2e9f83 2263B     models_test.go  006ed8a6  1562B

    ALL THREE CONTROLS GENERATED BYTE-IDENTICAL OUTPUT. BOTH ARMS DID TOO. Generation inside this
    process is perfectly deterministic, exactly as the campaign already believed.

THE ERROR
    I compared `generated/ledger-parity-ctl-1/...` — a FINISHED tree, whose files the fix loop had
    already repaired — against `generated/ledger-parity-ctl-3/...` — a tree still being generated,
    whose files had not been repaired yet.

    So every "difference" was the repair, not a difference in what the model wrote. ctl-1's shipped
    store.go is 413B using money.Money because the requalify gate FIXED it; ctl-3's mid-draw store.go
    was 377B using models.Money because it had not been fixed yet. Both models wrote the same 377B.

    The correct as-drawn source is `.pre-fix.json`, which I had used earlier today for exactly this
    reason — in the 21-of-21 check — and did not use here.

EVERYTHING THAT DIES WITH IT
    · "identical prompt, different output" — FALSE. Same prompt, same output.
    · "within-process determinism decays with use" — FALSE. It did not decay.
    · elapsed time / prompt cache eliminated as causes — VACUOUS. There was nothing to cause.
    · the propagation model, its refutation, the containment analysis, "six files apart",
      "materially different programs", the api-package test — ALL artifacts of the same comparison.
    · the arm-3 predictions scored as HELD at 15:58 — arm-3's as-drawn store.go is 7f2e9f83, byte
      IDENTICAL to arm-1 and arm-2. 1a is FALSE, and 2's "mechanism test hit" was the same artifact.
    · "ctl-3 carries a compile error the others do not" — FALSE. ALL THREE controls generate
      models.Money and cannot compile as drawn; all three are repaired by the requalify gate. That is
      normal harness behaviour, not a defect in one draw.

WHAT SURVIVES, AND IT IS THE PART THAT MATTERED
    The parity result is UNAFFECTED and is now cleaner than before:

        controls  as-drawn 7c9f06c7 ×3, identical   shipped identical ×3   SPLIT 3/3
        arms      as-drawn 7f2e9f83 ×2, identical                          CONSOLIDATED 2/2

    Withholding the parity clause still produces the stub, reproducibly, and the effect is measured
    on genuinely identical draws rather than on ones I mistakenly thought had drifted.

⚠️ HOW THIS HAPPENED, AND IT IS NOT A NEW LESSON
    Two hours ago I wrote that I had scored a prediction against a half-finished artifact, called it
    the `_corpus_state.py` failure mode, and said the guard exists because "a number taken from a
    half-written tree looks exactly like a result". I then kept measuring the same half-written tree
    for two more hours and built eight findings on it.

    Recognising the failure mode did not stop me repeating it, because I recognised it as a fact
    about ctl-3's LENGTH and not as a fact about which SOURCE to read. The fix is mechanical, not
    attentional: when comparing what a model WROTE, read .pre-fix.json. When comparing what a build
    SHIPPED, read the tree. Never one against the other.

════════════════════════════════════════════════════════════════════════════════════════════
THE TOOL THAT PREVENTS THIS EXISTS, IN THIS DIRECTORY, AND ITS DOCSTRING NAMES MY ERROR
════════════════════════════════════════════════════════════════════════════════════════════
    $ python _asdrawn_diff.py generated/ledger-parity-ctl-1 generated/ledger-parity-ctl-3
      as-drawn on BOTH sides: 19 common file(s) (18 .go + 1 non-Go) · IDENTICAL 19
      Byte-identical as drawn.

    One command. The entire two-hour investigation, answered correctly, in one line.

    And on a pair where one side is still generating:

    $ python _asdrawn_diff.py generated/ledger-parity-arm-1 generated/ledger-parity-arm-3
      REFUSING: generated/ledger-parity-arm-3 carries no .pre-fix.json, so its as-drawn contents are
      not recoverable.
        NOT falling back to the tree on disk: that is POST-REPAIR, and reading a repaired tree as a
        draw is the error behind every retraction on 29 July.

    IT REFUSES. It would have refused me, by name, at the first comparison.

⚠️ AND ITS DOCSTRING, WRITTEN BEFORE TODAY, DESCRIBES EXACTLY WHAT I DID
    "mistaking a post-repair tree for a draw is the single error behind every retraction on 29 July.
     A tool that silently degrades to the wrong measurement is worse than one that stops."

    Two days ago this error caused every retraction of that day. Someone — me, on 29 July — built a
    tool whose whole purpose is to make it impossible, gave it a self-test, and wrote the reason into
    the first paragraph. Today I did the comparison by hand with `shasum` and a python one-liner,
    twenty times, and reproduced the error the tool exists to prevent.

    THE LESSON IS NOT "read the docstring". It is that an ad-hoc `shasum a b` is always available and
    always looks sufficient, while the correct tool must be REMEMBERED. The repo already knew this:
    "an eyeballed number in a headline is the same class of mistake with no instrument at all, so the
    ad-hoc snippet is being replaced by something with a self-test." I then used an ad-hoc snippet.

    ⚠️ There is no process fix to add here. The instrument, the refusal, the self-test and the
    written warning all existed and none of them fired, because none of them is in the path of
    typing `shasum`. What would have caught it is asking "is there a tool for this" BEFORE the first
    measurement rather than after the eighth finding.
