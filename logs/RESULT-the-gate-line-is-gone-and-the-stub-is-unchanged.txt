RESULT — the gate demand is gone, the saved draw is real, and the artifact is unchanged
         (and what is unchanged turned out to be the CORRECT split — see the correction)
==================================================================================================
31 July. One ledger draw, fresh mlx_lm.server pid 7598, spec order unchanged (memory.go entry 5,
store.go entry 6 — the swapped arm where the demand bit). Pre-registration:
logs/PREREG-one-draw-confirms-the-gate-line-is-gone.txt, written before the draw.

PRIMARY — ✓ HELD, and it is binary
    generate internal/store/memory.go (6/19)
        best-of-N gen: kept candidate 1 of 2

    No "rejected gen candidate N: missing promised Store". The first candidate was accepted
    outright.

    THE SIX NEGATIVE CONTROLS, all drawn before the fix, all the same shape:
        ledger-pairB-p6      2 rejections
        ledger-pairB-p7      2
        ledger-pairB-p8      2
        ledger-order1        2
        taskapipro-pairB-t1  1
        workapi-pairB-w1     2

    Every prior draw of this shape logged it; this one does not. And the second half of the claim is
    visible in the same line: those draws spent both candidates and fell back to the first anyway,
    while this one kept candidate 1 and never drew candidate 2. The wasted sample is actually saved,
    not just theoretically saved.

THE PRE-REGISTERED NULL — ✓ HELD, which is the part that would have been easy to spin
    internal/store/memory.go   2307 bytes
    internal/store/store.go     381 bytes

    Against the archived B arms: p6 2373/377, p7 2374/379, p8 2373/377. This draw sits squarely in
    that band. The outcome is unchanged and the gate was, as measured, a bystander.

    ⚠️ CORRECTION, WRITTEN MINUTES AFTER THE PARAGRAPH ABOVE. I first wrote that "the content still
    collects into whichever file is generated FIRST — the consolidation is unchanged". That is the
    wrong word for what this draw produced. Opening the two files:

        store.go   package store + the Store interface, six methods, and nothing else
        memory.go  MemStore, NewMemStore, and all six methods implementing it

    THAT IS THE CORRECT SPLIT. There is no consolidation and no stub in this artifact. The archived
    B arm is the same (store.go interface-only, memory.go six methods), and the A arm — the original
    entry order — is the broken one: memory.go 14 bytes, store.go 2623.

    I read byte counts, matched them to a band, and reached for the word that had described that
    band all week, without opening either file. Ledger's reorder was recorded LAST NIGHT as an
    improvement, in my own words, "store.go keeps its interface" — so I contradicted a finding I had
    written ten hours earlier by trusting a proxy over the artifact.

    The claim that survives is the one that was pre-registered: the outcome is UNCHANGED by the gate
    fix. What is unchanged here happens to be correct behaviour, not a defect.

    ⚠️ THIS IS THE OUTCOME I PRE-REGISTERED AND IT IS STILL THE ONE I HAD TO BE CAREFUL ABOUT. Had
    memory.go come out thin on this draw I would have had a tidy story ready — "removing the demand
    let the model split the package" — off a single draw against an outcome that varies 35/67/80% by
    spec. Writing the expected null down first is the only reason that story is not in this file.

WHAT IS NOW SETTLED ABOUT THE GATE
    It demanded `Store` from the implementer in every spec, it was never satisfiable by the file
    being asked, it cost one sample per occurrence, and removing it changes NOTHING about the
    artifact. A real defect, correctly diagnosed, correctly fixed, and worth almost nothing — which
    is a fair description of most of what a harness does wrong.

AND THE CORRECTION SHARPENS THE OPEN QUESTION RATHER THAN BLURRING IT
    ledger, in its current entry order, SPLITS THE PACKAGE CORRECTLY — reproducibly, across the
    three archived B arms and this one. The stub is what the OTHER order produces (A arm: memory.go
    14 bytes) and what the other specs still produce. So the failure is not "the model cannot split
    a package"; it is narrower and more tractable than the phrase I have been using all week.

STILL OPEN — and today has now spent four edits without touching it
    How to make the model SPLIT a package's content correctly. Fix 2 made the repair applicable,
    fix 1 made the failure visible, the survival run showed the repair works whenever it can run,
    the gate fix removed a bystander demand. But the correction above narrows what "it" is: the
    model DOES split correctly under ledger's current order, four draws for four. The question is
    why the same model, on the same package, consolidates under the other order and on the other
    specs — which is a question about the CONDITIONS, not about a blanket inability.

════════════════════════════════════════════════════════════════════════════════════════════
THE DRAW'S FINAL OUTCOME, and what it did and did not settle for FIX 1
════════════════════════════════════════════════════════════════════════════════════════════
    [guildlm-build] error surface repeats — stopping extension rounds
    [guildlm-build] exhausted 6 fix rounds (5 budgeted), still failing

It ended NOT GREEN, failing in internal/api (transactions_test.go, the same error surface four
rounds running). So it took the exit FIX 1 was built for, on a real build, for the first time.

    no warning line — and that is CORRECT. The tree has NO empty planned .go file (checked
    directly, not inferred from the log's silence). The store package split properly, so
    warn_empty_planned_files(green=False) ran and had nothing to say.

    ⚠️ SO THIS DRAW IS UNINFORMATIVE FOR FIX 1'S POSITIVE CLAIM, and the pre-registration asked for
    a not-green draw precisely to see the warning APPEAR. It did not, because this build did not
    produce the defect. What it does establish is narrower and still worth having: the new exit path
    executes on a real failing build without crashing and without a spurious warning. The decisive
    evidence for the positive case remains the test that drives all of build() and fails when the
    call is removed.

    Recording it as uninformative rather than as confirmation. A silent correct run and a silent
    broken run look identical from the log, which is the whole reason the warning exists.

AND A THIRD DATA POINT FOR THE CORRECTED READING: ledger in its current entry order split the store
package correctly again (memory.go implementation, store.go interface only), on a build that FAILED
for entirely unrelated reasons in another package. Convergence and plan conformance are independent,
which is exactly what FIX 1 was arguing by existing.
