RESULT — the one-phrase fix works, and it generalises past the clause it is attached to
======================================================================================
Decisive file graded 19:35 on 30 July, from disk mid-draw (models.go is file 4; the fix loop had
not started, so this is as-drawn). Pre-registration: logs/PREREG-ledger-tagfix.txt.

    ✓ PREDICTED: tags present, 95%. HIT.
    ✗ PARTIAL branch (tags on Account only): did NOT occur — and its absence is the finding.

THE EDIT AND THE OUTCOME
    line 120: "`Account{ID string, Name string}`."
           -> "`Account{ID string, Name string}`, with JSON tags."   (+16 chars)

    models.go: 7 json tags — Account 2 · Posting 2 · Transaction 3
    control: 7 · every untagged ledger draw: 0

THE PART THAT IS NEW INFORMATION
I attached "with JSON tags" to ACCOUNT'S CLAUSE ONLY. Posting and Transaction are described in
separate sentences that still say nothing about tags. All three came back tagged.

    So the instruction is NOT local to the struct it modifies — it generalised across every struct
    in the file. That is something the corpus could NOT have told me: taskapipro and workapi both
    attach the phrase to EVERY struct, so they cannot distinguish "the model tags what it is told
    to tag" from "one mention makes it tag everything". Ledger's single-clause fix separates them.

    ⚠️ n=1 and it is the expected direction, so this is a demonstration, not a rate. What it rules
    out is the specific worry that motivated the PARTIAL branch — a half-fix that tags Account and
    silently leaves Posting.AccountID untagged, which is the exact field whose underscore defeats
    Go's case-insensitive match and breaks every transaction fixture. That failure mode was live
    when I wrote the pre-registration and is now observed not to happen.

WHAT THIS DRAW IS AND IS NOT EVIDENCE FOR — as pre-committed at 19:15
    NOT strong on its own: ledger tags itself ~50% of the time unprompted, so ONE tagged draw is
    about 2:1. The real evidence that the instruction works is the corpus: 24 of 24 across
    taskapipro and workapi. This draw makes it 25 of 25 and adds the generalisation fact.

    The informative branch was FAILURE — the first instruction-failure in 25 — and it did not
    happen. That was the only reason the draw was worth GPU, and it came back clean.

RECOMMENDATION — promote, deliberately, and not as a side effect
specs/ledger.yaml should get the phrase. It closes a real wire-format break: ledger's own fixtures
POST {"account_id":"cash","amount":500}, and untagged, Posting.AccountID never receives it.

    ⚠️ PROMOTING IT INVALIDATES generated/ledger-ctl-proc3 AS A CONTROL for any future ledger arm,
    because every spec edit rerolls the draw — measured seven times over today. Arms 1-5 are already
    graded against it and are unaffected. A sixth arm would need a fresh control drawn on whatever
    process is live then. Stated so the cost is chosen rather than discovered.

    NOT DOING IT IN THIS TURN. The arm is still drawing; promoting the spec while a draw against
    the old one is in flight is how a control gets silently mismatched.

STILL PENDING: the full as-drawn grade of the other 18 files. The +16-char addition is the edit
class that perturbed 7 of 7 in this series, so collateral is expected and is not what the arm
measures.

════════════════════════════════════════════════════════════════════════════════════════════
CORRECTION, 19:45 — THE CONTROL ALREADY TAGGED, AND models.go CAME BACK BYTE-IDENTICAL
════════════════════════════════════════════════════════════════════════════════════════════
Six files in, all six byte-identical to the control — INCLUDING models.go. The control already
carried 7 tags, and the arm's models.go is the same file, byte for byte.

    SO THE ADDED INSTRUCTION CHANGED NOTHING ON THIS DRAW. It did not cause the tags; the draw
    would have produced them anyway. The tags I reported at 19:35 are the CONTROL'S tags.

    WHAT THAT DOES TO THE CLAIMS ABOVE:
      "tags present, prediction HIT"        stands as a fact about the output, but it is NOT
                                            evidence the instruction did it — an identical file
                                            means the instruction was inert here.
      "generalises past its clause"         WITHDRAWN. Account, Posting and Transaction are all
                                            tagged because the CONTROL tagged them. Nothing about
                                            one clause propagating to three structs is shown. I
                                            read a property of the control as a property of my edit.
      "PARTIAL branch did not fire"         vacuous for the same reason.
      "25 of 25 compliance"                 revert to 24 of 24. This draw is not an independent
                                            observation of instructed compliance.

    ⚠️ THE PRE-REGISTRATION DID NOT ANTICIPATE THIS AND SHOULD HAVE. I wrote branches for tags
    present / absent / partial and never asked what the CONTROL does — even though I had measured
    at 18:25 that this exact control is one of the TAGGED ledger contents. The arm was built to
    detect a change against a baseline that could not show one. Choosing a control that already has
    the property you are trying to install is not a subtle error; it is the same shape as testing a
    fix on a case that was never broken.

    WHAT WOULD ACTUALLY TEST IT: the instruction added to a spec+condition that draws UNTAGGED.
    Nothing on disk pairs an untagged control with an instructed variant. That is a real experiment
    and this was not it.

⚠️ SEPARATELY AND STILL INTERESTING: six of six files byte-identical from a +16-character ADDITION.
Additions perturbed 7 of 7 earlier in this series. If the whole tree comes back identical this is
the first non-perturbing addition and deserves its own row — noted now, graded when the draw ends.

════════════════════════════════════════════════════════════════════════════════════════════
FINAL GRADE, 20:15 — a NULL EDIT, and the SMALLEST perturbation any spec edit has produced here
════════════════════════════════════════════════════════════════════════════════════════════
    19 files · CODE 1 · IDENTICAL 17 · PROSE 1
    ⚠ target internal/models/models.go IDENTICAL -> NULL EDIT, as the 19:45 correction predicted
    COLLATERAL 1: cmd/server/main.go · FUNCTION-LEVEL COLLATERAL 1: main

WHAT THE ARM ACTUALLY MEASURED
Nothing about the fix. The control already tagged, the target came back byte-identical, and the
instruction was inert on this draw. Everything claimed at 19:35 is withdrawn above and stays
withdrawn; the recommendation to promote the phrase rests on the corpus (24 of 24), not on this.

THE ONE THING IT DID BUY — a magnitude row the series was missing
    edit                                      chars   CODE files   func collateral
    taskflow dose1   +1 line                    +~40       3             8
    taskflow dose3   +3 lines                  +~120       8            10
    taskflow dose    +12 lines                 +~480       6            16
    ratelimit +48 lines                       +~1900       4             7
    arm 4  import-name transposition              0        5             2*
    arm 5  entry-block transposition              0        4             5
    arm 6  "with JSON tags"                     +16        1             1

    * arm 4's function count understates badly — seven methods relocated and a file stubbed, which
      register as only_a/only_b rather than "changed". Its FILE count is the honest one.

    ADDITIONS NOW PERTURB 8 OF 8 — the column survives. But arm 6 is the SMALLEST perturbation of
    any spec edit in this series, and it is an addition, while two zero-character transpositions
    sit at 4 and 5 files.

    ⚠️ SO EDIT SIZE PREDICTS NOTHING ABOUT PERTURBATION SIZE, in either direction. +16 characters
    moved one file; 0 characters moved five. This is the third independent statement of the same
    shape today — after "inertness of the text does not predict the size" (arms 4 and 5) and "the
    smaller prompt difference perturbed more" (prose vs name arms). Three routes to one conclusion:
    the magnitude of a spec edit carries no information about the magnitude of its effect.

WHAT WOULD ACTUALLY TEST THE FIX, restated so it is not lost
The instruction added to a spec+condition that draws UNTAGGED. Nothing on disk pairs an untagged
control with an instructed variant, and building one means drawing a fresh untagged control first —
which is itself a coin flip at ledger's ~50%. That is a real multi-draw experiment. This was not it.
