PREREG — replicate the non-monotone ladder on a SECOND eligible process
=======================================================================
10 August, committed BEFORE pid 46375 is killed and before any arm on the next process.

WHY THIS AND NOT ANOTHER ARM ON 46375
    Its queue is exhausted: 26 draws, the ladder complete, both violating arms redrawn. Every
    claim standing tonight is n = ONE process, and the strangest one — a +5-character edit
    reaching further than a +14-character edit on the same line — deserves the same treatment
    the campaign gives every other surprising result.

PROTOCOL (unchanged gates, both of them)
    1. Probe fresh processes with the baseline spec until INFORMATIVE. Max 8 probes.
    2. SCREEN each informative one with ledger-ownplacebo.yaml. A process that flips is
       DISCARDED and recorded; only informative AND screen-null may host arms.
       ⚠️ Today's rates: 4 informative of 8 probed, 1 eligible of 4 screened. Finding one is
       expected to cost roughly a dozen draws, and if the budget runs out that is the result.
    3. On an eligible process, in this order, RESTART=0 throughout:
           base · L4 (+5) · L6 (+14) · L5 (+13) · base-close
       L4 and L6 first because they are the violation; L5 confirms the top of the range is
       still reachable on this process at all.

ENDPOINT — three-valued, per arm: ABSENT / ABBREVIATED / LONG, plus the sha256.

REGISTERED
    P(an eligible process is found within 8 baseline probes)   = 0.65
    Given one is found:
    P(base is ABSENT and byte-identical 3a78b0d8)              = 0.80  (5 of 5 so far)
    P(L4 is ABBREVIATED)                                       = 0.45
    P(L6 is ABSENT)                                            = 0.55
    P(L5 is LONG)                                              = 0.70
    P(THE VIOLATION REPRODUCES — L4 strictly above L6 on the ordered scale) = 0.45

    VIOLATION REPRODUCES -> non-monotonicity is a property of the model's response to this line,
        not of one process, and "draw the neighbouring wordings" graduates from advice to a
        measured rule.
    VIOLATION DOES NOT   -> the ladder is a property of pid 46375. Every conclusion drawn from it
        today must be re-labelled as such IN THE LOGS, not just here — including the practical
        lesson, which would then rest on one process and must say so.
    ⚠️ A DIFFERENT ordering on the new process (say L6 above L4) is NOT a replication and must
        not be reported as "the effect is real but noisy". It would mean the wording→outcome map
        is process-specific, which is a third, worse outcome for every rule in this campaign.

VOID: repair activity in any arm · a restart between arms · base-close differing from base.
