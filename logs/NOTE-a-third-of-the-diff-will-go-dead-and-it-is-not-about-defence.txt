NOTE — read this BEFORE the v4-vs-v5 diff: up to a third of the rows go DEAD for a reason
that has nothing to do with defence
==========================================================================================

Written 10:45 on 29 July, before hole_hunt has swept anything, so it cannot be a
rationalisation of a number I have already seen.

THE MECHANISM
_hole_hunt cannot grade a tree that does not pass its own tests. Every probe on a red
artifact returns BASELINE-RED, and the tool says so plainly:

    0 of 1 probes could answer · 1 BASELINE-RED (artifact already failing: expreval-v5)
    NOTHING WAS MEASURED — every probe hit a red baseline or failed to apply.
    This is not 'no holes found'.

That is correct behaviour and it is exactly what makes the diff misreadable.

THE ARITHMETIC, from the tracked baseline

    taskapi-v4       40 rows      taskapi-v5   shipped NOT-GREEN
    expreval-v4       8 rows      expreval-v5  shipped NOT-GREEN
                     -------
                     48 of 314 baseline rows = 15%, already certain

    taskapipro-v4    62 rows      taskapipro-v5 is at fix round 5 of 6 and has not compiled
                     -------      once; NOT-GREEN is the likely outcome
                    110 of 314 = 35% if it ships red

So somewhere between a seventh and a third of the comparable set will show a LIVE verdict in
v4 and a DEAD one in v5. Not one of those rows is a defence that weakened. They are trees
that stopped compiling.

WHAT THE DIFF WILL DO WITH THEM, AND WHY IT IS NOT ENOUGH
_redraw_diff already separates this. It counts DEAD->LIVE and LIVE->DEAD on their own line
and labels them "artifacts changing state, not defences changing", and a NOAPPLY ->
BASELINE-RED flip is excluded from the live count entirely. The tool is not going to lie.

The risk is the HEADLINE. "N of M comparable rows flipped" invites reading M as the corpus,
and a third of M being dead means the campaign's closing number rests on a much smaller
base than it appears to. The count that matters is:

    rows LIVE in BOTH sweeps — the only ones where CAUGHT -> SURVIVED can even be observed.

Report that first, before any flip count, and state the three red artifacts by name.

AND THE DIRECTION MATTERS MORE THAN THE COUNT
taskapi's 40 rows going dead is not a neutral loss. taskapi-v5 is red BECAUSE the empty-list
closure fired against a real nil-slice defect — the defence working exactly as designed. So
those 40 rows are unmeasurable in v5 as a DIRECT CONSEQUENCE of a closure succeeding. A
naive reading — "40 rows we can no longer grade" — inverts a success into an absence.

    The one artifact where a closure caught a genuine bug is the one that contributes
    nothing to the capstone comparison, because catching the bug is what made it red.

That is worth stating as a limitation of mutation sweeps generally: they can only measure
suites that pass, so a suite that is currently DOING ITS JOB is invisible to them.

⚠️ DO NOT "FIX" THIS BY REPAIRING THE RED TREES. Hand-patching taskapi-v5 to green would
make its rows gradeable and would destroy the thing they would be graded on — the defect is
what the closure caught. If a comparable number for taskapi is wanted it needs a fresh draw,
not a repair.


COHERENCE CHECK (11:12) — THE v5 CORPUS MATCHES THE CURRENT SPECS, WITH ONE KNOWN EXCEPTION
============================================================================================
The capstone asks whether the REPAIRED SPEC SET produces a better corpus. That question is
only answerable if the corpus was actually drawn from the specs as they now stand. If specs
had been edited mid-sweep the v5 trees would be a mix of before-and-after and the comparison
would be about nothing in particular.

Checked by comparing every spec's mtime against its tree's:

    22 v5 trees with a spec · 1 has a spec NEWER than the draw

    taskflow      spec edited 07-29 00:42     tree drawn 07-29 00:39     SPEC-NEWER

That is the rename, by three minutes, and it is the case already known and already queued
for a retest. Every other tree was drawn from the spec as it stands today.

So the corpus is spec-coherent, and the one incoherent artifact is the one whose incoherence
IS the pending experiment. _named_present flags it by itself, which is why that hint was
added.

⚠️ MTIME IS FORGEABLE AND IS A HINT, NOT A PROOF — the same caveat _named_present carries.
It is evidence that no spec was quietly edited mid-sweep, not a guarantee. The stronger check
is `git log --since` over specs/, and for the three specs that matter most the answer is
already known from their commits: taskapipro's paging repair and shortener's prohibition both
predate the sweep, and taskflow's rename is the exception above.

⚠️ AND IT SAYS NOTHING ABOUT ledger, workapi OR THE taskapipro REDRAW, which are not drawn
yet. Re-run this check after the corpus completes; a spec edited between now and then would
be invisible to the run above.


UPDATE (12:07) — IT IS 46%, NOT 35%, AND THE REASON IS A SCOPING CHOICE
=======================================================================
This note pre-registered an upper bound of 110 of 314 rows (35%) going dead, on the
assumption that the red artifacts would be taskapi, expreval and taskapipro. workapi then
also shipped NOT-GREEN:

    taskapi-v4        40
    expreval-v4        8
    taskapipro-v4     62
    workapi-v4        36
    ------------------------
    TOTAL            146 of 314 = 46%

The pre-registered bound was too low because I bounded the SPECS I expected to fail, not the
mechanism. workapi failing was not in the scenario, and it should have been — its prediction
of NOT-GREEN was registered at 95% an hour before this note's update, by me.

AND WORKAPI'S 36 ROWS ARE DEAD FOR A REASON THAT IS NOT ABOUT WORKAPI
_teeth_suite runs the baseline as:

    _GO_TEST = ["go", "test", "-count=1", "-timeout", "60s", "./..."]

The WHOLE MODULE. workapi-v5's internal/api compiles and passes — it reported `ok` in every
round that reached the test phase — while internal/service has a scope error. `./...` fails,
so every probe in the artifact returns BASELINE-RED, including probes on the package that is
perfectly green.

    A single broken package poisons every row in the artifact. 36 gradeable rows are
    reported as ungradeable because a sibling package does not compile.

THE FIX IS OBVIOUS AND MUST NOT BE MADE NOW
Scoping the baseline to the package under test would recover those rows — it is exactly what
_grade_workapi_delete404.sh does with `go test ./internal/api/`, which is why that grade is
still obtainable while these rows are not.

But the tracked 314-row v4 baseline was measured with the whole-module rule. Changing the
rule before the v4-vs-v5 diff would compare rows taken under two different definitions of
"gradeable", and the diff would show a large recovery that is entirely instrument. That is
the shape already recorded twice in this repo — the glob-vs-rglob fix that added 144 rows,
and the standing instruction not to widen shape 6b between v4 and v5.

    Widen the instrument between a pair of sweeps and the next diff measures the widening.

QUEUED, in the same batch as shape 6b and the nil-slice shape: scope the baseline per
package, then sweep BOTH generations with the new rule.

WHAT TO REPORT FROM THE DIFF, unchanged in principle and sharper in number: rows LIVE IN
BOTH sweeps first, with the four red artifacts named and 146 stated as the dead count. A
"comparable" figure that silently includes 146 BASELINE-RED rows would describe a corpus
half of which was not measured.


HOW MUCH THE QUEUED FIX WOULD RECOVER (12:08) — measured per package, bounded
==============================================================================
The 146 dead rows are not equally dead. Broken down by the package each row sits in:

    workapi-v4     36 rows   internal/api 19 · models 7 · config 5 · service 4 · store 1
    taskapi-v4     40 rows   internal/api 29 · models 9 · store 2
    taskapipro-v4  62 rows   internal/api 40 · models 9 · config 7 · service 4 · store 2
    expreval-v4     8 rows   (root) 8

workapi is the recoverable case, and the arithmetic is stark. Its ONLY reported failure is

    vet: internal/service/service_test.go:165:9: undefined: fake

which is 4 rows. internal/api reported `ok` explicitly in four rounds — 19 rows. So AT LEAST
19 and plausibly 32 of workapi's 36 rows are gradeable right now and are being reported
ungradeable.

    At least 19 rows lost to a scoping choice; 4 lost to the actual defect.

⚠️ THE 32 IS AN UPPER BOUND, NOT A MEASUREMENT. Only internal/api was reported `ok` in the
captured log lines; models, config and store were not listed either way, and I have not run
them — doing so would put `go test` against a live generation, which is the rule recorded at
11:02. The confirmed figure is 19. The 32 assumes the untested packages pass, which is likely
given the single reported failure, and is labelled as an assumption.

THE OTHER THREE ARE NOT RECOVERABLE THIS WAY
  taskapi     its failure IS in internal/api — the nil-slice defect the empty-list closure
              caught, in projects_test.go. Scoping does not help; the package under test is
              the one that is red, and it is red for the right reason.
  taskapipro  internal/api never compiled at all across seven rounds.
  expreval    one package, no scope to narrow to.

So per-package scoping recovers workapi and nothing else, and the honest headline for the
queued fix is "19 to 32 rows of 146", not "half the dead rows". That is still the difference
between workapi contributing nothing to the capstone and contributing most of its rows — and
workapi is the artifact carrying the Delete-404 closure, which is the campaign's one clean
durability result.


WHAT LEDGER BRINGS TO THE DIFF (12:14) — profiled while it draws
=================================================================
ledger was omitted from the sweep list and is being drawn now to recover its 31 rows. What
those rows are, from the tracked baseline:

    22 CAUGHT · 8 SURVIVED · 1 SURVIVED*

The eight survivors are the rows worth reading, because they are the ones a repaired spec
could move:

    internal/api/response.go    StatusConflict -> StatusBadRequest
    internal/api/response.go    StatusUnprocessableEntity -> StatusBadRequest
    internal/api/response.go    StatusInternalServerError -> StatusBadGateway
    internal/api/response.go    drop Content-Type
    internal/store/memory.go    reverse sort by ID
    internal/models/models.go   %w -> %v (ErrInvalid)
    internal/models/models.go   json tag "memo" renamed
    internal/money/money.go     boundary < -> <=

Four of eight sit in response.go — a single file where status codes and the Content-Type
header go unasserted. That is the same shape as the campaign's other survivors: a helper
whose OUTPUT every test relies on and whose CONTRACT no test names.

WHY THIS MATTERS FOR THE HEADLINE. ledger contributes 31 comparable rows to a diff whose
live-in-both denominator is otherwise badly reduced — 146 of 314 rows dead across four red
artifacts. If ledger-v5 comes back green, it is a meaningful fraction of what remains
answerable, and it is 8 survivors' worth of room to move in either direction.

⚠️ AND IT COULD ALSO SHIP RED. Nothing about ledger has been repaired this week — no closure
was written for these eight sites, because ledger was not in the sweep and nobody was looking
at it. So the honest expectation is that its eight survivors STAY survivors: there is no
intervention whose effect they could show. A flip here would need explaining, not
celebrating.

    ledger is the one artifact in this diff with a genuine control character: same spec,
    same model, a week apart, and NO spec edits in between. Whatever its rows do is drift.

That makes it the cleanest read of "what does a redraw do on its own", separate from every
closure claim — which is worth more than the 31 rows it adds to the denominator.


CONTROL CLAIM VERIFIED, AND ITS INTERVAL CORRECTED (12:16)
===========================================================
I asserted "ledger had NO spec edits this week" in the section above. Checked it rather than
leaving it on memory:

    git log -- specs/ledger.yaml     last commit af1932d, 17 July 14:06
    mtime                            17 July 16:32

Nothing since 17 July. The closure campaign ran 26-29 July, so ledger genuinely received no
intervention. The control claim HOLDS.

THE INTERVAL WAS WRONG, THOUGH. I wrote "a week apart". The actual dates:

    specs/ledger.yaml   last edited   17 July
    generated/ledger-v4 drawn         26 July 15:19
    logs/hole-hunt-rows.tsv swept     29 July 00:07   (the re-sweep, repaired walk)
    generated/ledger-v5 drawing       29 July 12:03

So the two DRAWS are 26 July and 29 July — three days apart, from a spec nine days older
than either. "A week apart" was a guess in the same family as this morning's nine invented
timestamps, and it is corrected here rather than left because the interval is part of what
makes the control a control.

WHAT THE VERIFIED VERSION SAYS, precisely:
    same spec (unchanged since 17 July), same model, same flags, two draws three days apart,
    and NO closure written for any of ledger's eight survivors in between.

That is a tighter control than I claimed — three days and zero edits beats a week and zero
edits, because less can have drifted. And the baseline it is measured against was swept on
29 July with the repaired walk, so both sides use the same instrument.

    Expectation restated: ledger's 8 survivors stay survivors and its 22 CAUGHT stay CAUGHT.
    Any movement is redraw drift and is the number to quote whenever someone asks how much
    of a closure result could have happened by itself.
