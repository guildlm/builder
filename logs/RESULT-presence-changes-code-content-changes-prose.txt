RESULT — twelve lines' PRESENCE changes the generated code; their CONTENT changes only prose
==============================================================================================

18:09 on 29 July. Three pairwise comparisons, all on server 4439, all with identical flags,
each restricted to the files NEITHER draw repaired — so every byte compared is as-drawn.

    comparison            differs by                untouched files      nature of the diff
    v5 draw1 vs draw2     nothing                   0 of 8 differ        —
    inert vs v5           the 12 lines' CONTENT     2 of 8 differ        comment + error string
    chain5 vs v5          the 12 lines' PRESENCE    4 of 7 differ        REAL CODE

⚠️ THOSE THREE DENOMINATORS ARE NOT THE SAME FILE SET, which would let the gradient be partly
an artefact of what happened to be compared. Redone (18:12) on the files untouched in ALL FOUR
logs — one denominator, seven files, every byte as-drawn in every arm:

    v5 draw1 vs draw2     nothing                   0 of 7 differ
    inert vs v5           the 12 lines' CONTENT     1 of 7 differ   models.go, the comment
    chain5 vs v5          the 12 lines' PRESENCE    4 of 7 differ   config.go, service.go,
                                                                    models_test.go,
                                                                    middleware_chain_test.go

    The gradient is 0 / 1 / 4 on an identical file set. Stricter than the version above and
    the conclusion is unchanged. Note also that two of the four are TEST files, so presence
    reaches tests as well as implementation.

A gradient, and the middle row is the control that makes the other two mean something:
sampling variance in this condition is ZERO, so both of the other differences are real.

WHAT "REAL CODE" MEANS, concretely
    internal/config/config.go — chain5 has an entire helper that v5 does not:

        func getenv(key, fallback string) string {
            if v := os.Getenv(key); v != "" {
                return v
            }
            return fallback
        }

      and a different validation message: "max page size must not be less than default page
      size" versus "must be greater than or equal to default page size, got %d".

    internal/service/service.go — comments differ AND so does the body: `filtered :=
      make([]models.Task, 0)` is not the same line on both sides.

    Neither file is mentioned anywhere in the twelve lines, which live in the projects TEST
    entry. Adding them deleted a helper function from a config file.

WHAT "PROSE" MEANS, by contrast — the entire inert-vs-v5 difference, both hunks:
    - // Project is a domain entity representing a project with an ID and name.
    + // Project is a domain entity representing a project with a name and ID.
    - t.Fatalf("Validate returned an error for valid config: %v", err)
    + t.Fatalf("Validate: %v", err)

    One comment's word order and one error message. No identifier, expression, statement or
    import differs in any of the eight files.

    SO: PUT TWELVE MORE LINES IN AN ENTRY AND THE MODEL WRITES DIFFERENT CODE IN FILES THAT
    ENTRY DOES NOT MENTION. CHANGE WHAT THOSE TWELVE LINES SAY AND IT WRITES THE SAME CODE
    WITH SLIGHTLY DIFFERENT WORDING.

AND IT EXPLAINS THE DEFECT LINE NUMBERS, which is what makes this more than an aggregate
    chain5  different code  ->  its defects sit at projects.go 77:55 and 56:2
    v5      the code        ->  71:55 and 50:2
    inert   the SAME code as v5 (bar a comment)  ->  71:55 and 50:2

The six-line offset between the arms was never mysterious: the arms contain different code.
The inert draw matches v5 to the column because it IS v5's code.

⚠️ THIS IS THE CLEANEST COMPARISON IN THE INVESTIGATION AND IT WAS ON DISK ALL DAY. chain5 and
the v5 draws ran on the same server process, with the same flags, on the same day, differing
in exactly one thing: whether twelve lines were present. Everything measured this afternoon —
the inert arm, the trajectory prediction, the round-1 import table — was built to get at a
question this pair answers directly. I did not compare them because chain5 was filed as "the
control arm that came back masked", and a draw that cannot answer ONE question was treated as
if it could answer none.

⚠️ WHAT IT STILL DOES NOT SETTLE. The server. Every draw here ran on 4439, so "twelve lines
change the code" and "this server writes this code" both fit. The pristine pre-edit draw was
queued to settle it — see the separate note on why that draw is now expected to be VOID.

⚠️ AND THE REPAIRED FILES REMAIN EXCLUDED. projects.go and projects_test.go, where the defects
actually live, were repaired in every draw and their as-drawn contents are not recoverable.
Everything above is inferred from the files the loop happened not to touch.
