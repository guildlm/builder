🚨 RETRACTED IN FULL — see logs/RESULT-RETRACTION-there-was-no-drift-i-compared-repaired-
against-unrepaired.txt. Every 'difference' recorded below is the FIX LOOP'S REPAIR, not a
difference in what the model wrote: a finished tree was compared against a mid-generation
one. All three controls' as-drawn store.go is 7c9f06c7 (377B) and both arms' is 7f2e9f83
(2263B). Generation in this process is deterministic. Kept unedited below as the record of
how the mistake was made.
====================================================================================================

PREDICTION — arm-3 drifts from arm-1/arm-2, starts drifting at the same point, and still CONSOLIDATES
======================================================================================================
31 July 15:34, written with ctl-3 at 11 of 19 and arm-3 not yet started. Three predictions, and the
second is the one that tests a mechanism rather than a pattern.

THE SETUP
    ctl-1 (11:50) == ctl-2 (13:37), byte-identical, 19 files.
    ctl-3 (15:19) DIFFERS from both, first difference at file 5 of 19 (internal/models/models_test.go).
    arm-1 (12:20) == arm-2 (13:57), byte-identical, 18 .go files.
    arm-3 will start around 15:45, LATER in the same process than any draw so far.

PREDICTIONS
    1. arm-3 differs from arm-1 and arm-2.                                             75%
       The process has already been shown to drift, and arm-3 is drawn after the drift was observed.
       The 25% is that whatever changed is not monotonic — nothing here says the process cannot
       return to an earlier state, and the cache size at ctl-3 (5.70 GB) equals the size at ctl-2,
       which reproduced.

    2. IF it differs, the FIRST differing file is at or near index 5, as ctl-3's was.               55%
       This is the mechanism test. If drift onset is a property of the PROCESS STATE at draw time, it
       should hit the same point in the generation sequence regardless of condition. If onset moves
       around, the drift is not a clean function of when the draw started, and "the process changed"
       becomes a weaker description than it currently sounds.
       55% because I have exactly one observation of an onset and no reason beyond symmetry.

    3. arm-3 still grades CONSOLIDATED.                                                 85%
       The controls drifted in bytes and held their verdict 3 of 3. The manipulation is a
       543-character prompt difference on one file, and its effect survived a byte change it did not
       cause. The 15% is the reject condition from the parity pre-registration: if arm-3 SPLITS, the
       effect is process-state-dependent and today's 2-of-2 is not a stable result.

⚠️ WHAT WOULD MAKE ME WRONG IN THE MOST USEFUL WAY
    Prediction 3 failing. Predictions 1 and 2 are about an artefact of the server; prediction 3 is
    about whether the thing this whole experiment measured is real. A drifted arm that SPLITS would
    mean the parity effect and the drift are entangled, and the six draws of today's A/B would need
    re-reading rather than extending.

════════════════════════════════════════════════════════════════════════════════════════════
AMENDED 15:52, arm-3 at 3 of 19 — SPECIFYING AS-DRAWN vs SHIPPED, which the original did not
════════════════════════════════════════════════════════════════════════════════════════════
    The ctl-3 episode just showed why this matters: I predicted "byte-identical", measured a
    half-generated tree, declared refutation, and the SHIPPED artifact turned out identical after the
    deterministic gates repaired the drift. The prediction was right and my scoring was wrong,
    because "byte-identical" never said WHEN.

    So, stated before arm-3 finishes:

    1a. arm-3 AS-DRAWN (.pre-fix.json) differs from arm-1/arm-2 as-drawn.               70%
        Down from 75: still likely, but ctl-3 is one observation of drift and arm-1/arm-2 already
        reproduced across a 96-minute gap.

    1b. arm-3 SHIPPED is byte-identical to arm-1/arm-2 shipped.                          55%
        NEW, and the one the original conflated. ctl-3 shipped identical because the requalify gate
        erased its drift. But the arms do NOT converge — both exhausted 8 fix rounds and shipped red
        — so nothing normalises them, and whatever drift appears may survive to the artifact.
        55% because I now have exactly one measurement of the gates erasing drift, on a build that
        went green, and the arms do not.

    2.  IF as-drawn differs, the first differing file is at or near index 5.             55%  unchanged

    3.  arm-3 grades CONSOLIDATED, as-drawn AND shipped.                                 85%  unchanged
        The verdict was stable across ctl-3's drift and repair, in both states.

⚠️ THE HONEST NOTE: amendments made mid-flight are exactly how a prediction gets fitted to its
outcome, so what is added here is a DISTINCTION the original lacked, with numbers set before any
arm-3 file has been compared to anything. Nothing about arm-3 has been measured yet — the log shows
3 of 19 generated and no comparison has been run.

════════════════════════════════════════════════════════════════════════════════════════════
RESOLVED AS-DRAWN, 15:58, arm-3 at 7 of 19 files
════════════════════════════════════════════════════════════════════════════════════════════
    models.go        IDENTICAL          money.go       IDENTICAL
    models_test.go   ✗ DIFFERS          money_test.go  IDENTICAL
    store.go         ✗ DIFFERS  e926b654 -> 7f2e9f83   2295B -> 2263B
    memory.go        IDENTICAL (the 14-byte stub, same as arm-1)

    ✓ 1a HELD (70%)   arm-3 as-drawn differs from arm-1/arm-2 as-drawn.

    ✓ 2 HELD (55%)    THE MECHANISM TEST. The first differing file is internal/models/models_test.go
                      — index 5 — EXACTLY where ctl-3's drift started, in the other condition.

                      Two draws, two conditions, same onset point. That is what "drift onset is a
                      property of the PROCESS STATE at draw time, not of the condition" predicts, and
                      it was the reading I said would be weakened if onset moved around. It did not
                      move.

    ✓ 3 HELD as-drawn (85%)  arm-3's store.go declares MemStore (2263B) and memory.go is the 14-byte
                      stub. CONSOLIDATED, like arm-1 and arm-2. The parity effect survives the drift
                      in the arm condition too.

    — 1b UNRESOLVED   shipped comparison needs the fix loop to finish. This is the one that tests
                      whether the gates erase drift only on builds that CONVERGE.

AND THE DRIFT IS THE SAME DRIFT, ACROSS CONDITIONS
    arm-3's store.go references models.Money — the identical wrong qualification ctl-3 produced.
    Same onset file, same downstream symptom, in a draw whose prompt for store.go differs from
    ctl-3's by the 543-character parity clause.

    So whatever changed inside pid 7598 produces a SPECIFIC, REPEATABLE deviation rather than random
    variation: models_test.go first, then models.Money in the store layer. Two conditions, one
    signature.
