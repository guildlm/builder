RESULT — the Chain closure across five specs (N=5, one edit)
=============================================================
2026-07-27. Predictions: logs/PREDICTION-taskflow-chain.txt, logs/PREDICTION-boundary-closures.txt.
Runs: _taskflow_chain_run.sh, then _run_queue.sh -> _chain_run.sh <spec>. Base model at :8080,
no fleet, identical flags to the runs that built the -v4 trees.

THE QUESTION
  Five of twenty-five specs describe the SAME middleware composer in the same words, all five
  grew the same undefended loop, and all five got the SAME edit: one own-named test asserting
  the joined call order. Every previous closure claim in this repo is N=1 per spec. This is
  the first time one edit has been applied to five independent specs at once, so it can
  separate "naming works" from "that spec had a good day".

SCOREBOARD  (updated as each run lands)

  spec        wall    tree    test written   assertion form        mutation   dropped
  ----------  ------  ------  -------------  --------------------  ---------  -------
  taskflow    21676s  RED     yes            element-wise `want`   CAUGHT*    none (29->30)
  usersapi      546s  GREEN   yes            strings.Join          CAUGHT     none (11->12)
  taskapi      1532s  GREEN   yes            strings.Join          CAUGHT     none (40->41)
  taskapipro   2760s  GREEN   yes            strings.Join          CAUGHT     none (57->59)
  workapi      2530s  GREEN   yes            strings.Join          CAUGHT**   none (40->42)

  * taskflow graded in isolation (`go test -run TestChain...`), because its tree is RED for
    two unrelated reasons and a red baseline voids every CAUGHT/SURVIVED verdict. usersapi
    graded properly by _hole_closed, which also confirmed no registered mutation regressed.

THE ANSWER AT N=5
  THE CLOSURE ITSELF IS NOT THE VARIABLE. Both specs wrote the test, both assert the order,
  both catch the mutation. The prediction I was least sure of — that the model would assert
  the ORDER rather than the status, given that prose explaining why a WEAKER assertion is
  insufficient has the worse record here — has now held twice.

  THE RENDERING VARIES, THE CONTENT DOES NOT. usersapi wrote `strings.Join(order, ",") !=
  "first,second,handler"`, exactly as dictated. taskflow wrote an element-wise compare against
  a `want` slice — stronger than dictated, and it defeated the run script's grep for the
  literal string. Two renderings, one contract, both catch the flip. Worth remembering the
  next time a spec dictates a FORM: the model honours the assertion's MEANING more reliably
  than its syntax, and a grader keyed to syntax will lie.

  THE COST IS NOT IN THE EDIT. 21,676s against 546s for the same one-file, one-test addition —
  a factor of forty. Whatever taskflow spent six hours on, it was not this test: it wrote it,
  correctly, and dropped nothing. Two specs, two currencies, and neither paid in dropped tests.

  NEITHER SPEC DROPPED A TEST. The corpus record says taskflow "pays for a harder spec in
  DROPPED TESTS where shortener pays in TIME". Both of these added a file entry and lost
  nothing; taskflow paid in time, which is the other spec's currency. That claim is now
  narrowed twice over and should not be quoted as it stands.

WHERE THE SIX HOURS WENT — counted, not guessed
  The open question was whether taskflow's 21,676s belongs to taskflow or to the edit.
  Counting model calls in the three logs answers it, and the answer is neither size nor the
  edit:

      spec       files  rounds  resolved by          model fix-calls  wall     tree
      usersapi     9      0     nothing to fix              0           546s   GREEN
      taskapi     18      1     DETERMINISTIC GATES         0          1532s   GREEN
      taskapipro  21      2     DETERMINISTIC GATES         0          2760s   GREEN
      workapi     21      3     DETERMINISTIC GATES         2          2530s   GREEN
      taskflow    16      8     model, and never            84         21676s  RED

  taskapi has the MOST files of the three and cost zero fix-calls: "converged to green after
  fix round 1 (deterministic)" — three gate repairs, no model round-trip. taskflow spent 84
  model fix-calls across eight rounds against a budget of five and ended red.

  So the cost is the FIX LOOP, and the fix loop is cheap exactly when the deterministic gates
  can resolve it. That is the recorded ordering lesson — deterministic gate first, model for
  the residue — with a number on it for the first time: the difference between a spec whose
  failures fall inside the gate chain and one whose failures do not is 0 versus 84 calls, and
  40x wall clock.

  It also settles the attribution: the edit adds one file to every spec, and two specs that
  received it paid nothing at all.

** workapi's Chain probe first reported NOAPPLY. The declaration MOVED: this draw left
   internal/api/middleware.go as a ONE-LINE file containing only `package api` and put
   Middleware, Chain, Logging and Recover in router.go. Re-graded against router.go: CAUGHT.
   The drift warning added earlier the same day is what named the right file.

FILE LAYOUT IS ADVISORY IN A WAY CONTENT IS NOT
  That NOAPPLY is the session's quietest finding and possibly its most useful. Go does not
  care which file in a package holds a declaration, so a draw that ignores the spec's file
  assignment still compiles, still passes, and still satisfies every promise — while leaving
  a dead one-line file where the spec put the middleware.

  Everything in the instrument layer assumes otherwise: --file pins, the per-site mutation
  registry, _mirror_calls_audit's twin pairing, and every "which file holds the hole" answer
  in logs/hole-hunt-rows.tsv. Four of five draws kept the layout and the fifth did not, with
  no signal other than a probe that suddenly did not apply.

  The compiler enforces the contract; nothing enforces the layout. So a NOAPPLY is not
  evidence about a hole, and any tool that reports one must say WHERE the site went — which
  is the whole reason the drift warning exists and why it was worth fixing an hour before
  this run needed it.

FINAL: 5 of 5 CLOSED
  Every spec wrote the test. Every spec asserts the ORDER. Every mutation flips SURVIVED ->
  CAUGHT. No spec dropped a test — 29->30, 11->12, 40->41, 57->59, 40->42 — and no registered
  mutation regressed anywhere. The two config closures carried in the same edits (workapi
  QueueSize, taskapipro DefaultPageSize) both CAUGHT as well, so the two specs that received
  TWO edits are exactly the two that fared best per edit.

  The one prediction that failed is the one worth keeping: I gave 4-of-4 even odds and it was
  5-of-5, having doubted that a spec explaining why a WEAKER assertion is insufficient would
  produce the stronger one. It did, in all five, in three different syntaxes.
