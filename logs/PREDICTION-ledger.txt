PREDICTION — ledger spec fix (show the code for the one test the model dropped)
Written 2026-07-17 14:08, BEFORE the run. Prediction first, as always.

THE HOLE (proved earlier today, deterministic, model-free — see PREDICTION-completeness.txt):
  ledger's spec names SEVEN store tests and the model wrote SIX. The one missing,
  TestCreateTransactionMovesBalances, is the only one whose absence matters: wrap
  the apply loop in `if p.Amount > 0` so every credit is silently dropped — money
  vanishing from a double-entry ledger — and build, vet and the FULL suite stay
  GREEN. Write the spec's named test verbatim and it fails instantly
  (`rev: got 0.00, want -500`). The spec named exactly the right test.

WHY THIS ONE AND NOT THE OTHER SIX — the pattern is the project's own law, and I
checked it rather than assuming it:
  Of the seven, TestCreateTransactionMovesBalances is the ONLY one that must BUILD
  a mechanical construct (models.Transaction{Postings: []models.Posting{{...,
  money.Money(-500)}}}). The other six pass an Account or nothing. The spec
  DESCRIBED that construct in prose and SHOWED nothing.
  Independent corroboration in the same artifact, which I went looking for AFTER
  forming the story rather than treating the story as proof: the spec's own
  sentence "Imports errors, testing, and the models and money packages" is also
  violated — the generated file imports errors, testing, models. NO money. The
  model dropped the test that needed the construct, and then correctly omitted the
  now-unused import. Two independent signals, one cause.
  This is the same shape as the ledger deadlock, shortener, logstats, and today's
  taskapipro guard: DESCRIBED breaks, SHOWN works.

THE FIX: replaced the prose with the exact code I already verified — it compiles,
it passes on the correct store, it fails on the mutant. Plus the reason the rev
half is not decoration, and a note that the money import is the compiler's way of
telling you the test is missing.

I PREDICT (falsifiable, one rep, same process, artifact kept):
  1. The model WRITES TestCreateTransactionMovesBalances. ledger goes 1 missing
     -> 0 missing on _named_test_audit.py. Test funcs 32 -> 33.
     Confidence: high. This is the sixth instance of the thesis, and today's
     taskapipro case put the guard in BYTE-IDENTICALLY 2/2 after seven straight
     observations without it.
  2. GREEN, coverage ~flat. The new test executes lines the other tests already
     reach — the same reason the workapi gain was "invisible to coverage". IF
     COVERAGE JUMPS I DO NOT CELEBRATE, I GET SUSPICIOUS. (That instinct is what
     found today's hole: an unexplained 49->48 on a run I could have filed as a win.)
  3. BLAST RADIUS: NOTHING ELSE IS LOST. 32 -> 33, not 32 -> 32-with-a-swap.

  (3) IS THE REAL TEST AND I AM STATING WHY I MIGHT BE WRONG. This edit adds
  ~1400 chars to one file's purpose, and _file_list (builder.py:1156) puts EVERY
  purpose in EVERY prompt — so it is in all 20 files' prompts, including files
  generated earlier. That is EXACTLY the mechanism that deleted TestListSorted
  from taskapipro this morning, 2/2, from an edit in another file's section. I am
  doing the same class of thing again, on purpose, with the instrument to see it.
  My reason for predicting no loss is the hypothesis the NONAMES probe is testing
  right now: taskapipro's edit NAMED other tests (TestList/TestListLimit/
  TestListNegativeOffset) and I think the store test author de-duplicated against
  them. This edit names NO other test — it says "the other six" without naming one.
  So the two experiments are coupled: if NONAMES comes back with TestListSorted
  restored, this design choice is why, and (3) should hold.

  ############################################################################
  ### REVISED 14:10 — BEFORE THE RUN. THE GROUNDS FOR (3) COLLAPSED.       ###
  ############################################################################
  The NONAMES probe landed (rep1, pre-gate) and REFUTED the de-dup hypothesis:
  names stripped, TestListSorted STILL ABSENT, and the store test's function list
  is IDENTICAL to the arm that named them. It was never the names — added TEXT
  displaces. See PREDICTION-taskapipro.txt.

  So the reason I gave for (3) is gone. I am revising the prediction NOW, before
  the run, because a prediction whose grounds collapsed is not a prediction — and
  revising it after seeing the result is precisely how I turned a correct call
  into a wrong one yesterday.

  (3) IS WITHDRAWN. REPLACED WITH:
    3'. I DO NOT KNOW whether ledger loses a test, and I now think it is MORE
        LIKELY THAN NOT that something moves. The only measured statement I have
        is: adding text to one file's purpose deterministically deleted a
        spec-named test from an EARLIER file, and no safe form of spec edit is
        known. My edit is the same class of act, and bigger (~1400 chars).
        I predict: the new test IS written (1 and 2 stand — those rest on the
        thesis, which has six instances and today's byte-identical 2/2, not on
        the dead hypothesis). Test funcs land at 33 IF nothing is displaced, 32
        if one is. I am NOT predicting which.
    A prediction of "I don't know" is worth less than a sharp one, and I would
    rather file the honest weak one than a sharp one I no longer have grounds for.

  THIS IS NOW THE POINT OF THE LEDGER RUN. It stopped being a spec fix and became
  the first DESIGNED test of the successor question: is the displacement specific
  to text ABOUT the displaced test's topic (my ledger text is about building a
  Transaction; taskapipro's was about List, and a LIST test died), or does any
  large addition perturb the earlier files? Ledger's store test is where the new
  test goes, so the audit will show both halves at once: does the named test
  arrive, and does anything else leave.
  WHATEVER HAPPENS, THE AUDIT RUNS BEFORE AND AFTER. That is now mandatory for
  every spec fix, not a courtesy — and it is why the tool exists.

WHAT WOULD REFUTE ME, named in advance so I cannot slide past it:
  - Any test other than the new one disappears (32 -> 32, or a rename-and-drop)
    => the de-dup hypothesis is DEAD: mere ADDED TEXT displaces, names or not, and
    every spec edit is a global perturbation with no safe form. That would make
    "fix the spec by showing code" a technique with a standing cost, and the honest
    consequence is that EVERY spec fix from here on must be audited before/after,
    not just this one.
  - The model writes the test but drops the rev assertion => showing code is not
    sufficient when the code is long; the model excerpts. A worse finding than a
    plain miss, because the test would then be PRESENT and the hole still OPEN —
    and the audit, which matches on names, WOULD NOT SEE IT. If that happens the
    audit needs to grow teeth (assert the assertion), and I will say so.

BEFORE-STATE, recorded now (artifact preserved at generated/_ledger-BEFORE-specfix
because a rerun overwrites ledger-v4 — a tool of mine already destroyed one
arm's evidence today and I am not doing it twice):
  32 test funcs total: api/middleware 1 · api/router 5 · api/transactions 4 ·
  models 6 · money 4 · service 6 · store 6  (store should be 7)
  audit: 32 named, 1 missing (TestCreateTransactionMovesBalances),
         1 renamed-present (TestCreateAccountDuplicate ->
         TestCreateAccountDuplicateIsErrExists)

RUN (only after the NONAMES probe lands — one server process, PID 3082; a second
generation run would confound both):
  ./_ab_run.sh ledger ; then _named_test_audit.py ledger ; then READ THE FILE.
  And then the real check: re-apply the mutant to the NEW artifact. If the model's
  own test does not kill it, the test is theatre and I report that.

=============================================================================
--- 15:2x (clock READ from `date`) — THE DESIGN WAS WRONG. REBUILT. ---
=============================================================================
CAUGHT BEFORE RUNNING, not after: my BEFORE-state is generated/ledger-v4, and I
checked when it was made. 2026-07-15 19:24. TWO DAYS AGO, a DIFFERENT server
process, and a builder TWELVE commits back on builder.py alone.

That is the error that killed story 2 this morning, verbatim: "the guard
artifacts were from an OLD builder — never an example of this system", i.e.
POOLING ACROSS PROCESSES AND BUILDER VERSIONS. I rebuilt the same mistake four
hours later, in the experiment written to study it.

AND IT IS WORSE — THE AUDIT ITSELF IS POOLED. The artifacts it scored span
07-10 to 07-17:
    07-10  tasks-api-min          07-11  walkv, numkit, bitset, jsonapi(07-12),
    eventbus, lrucache, expreval, jsoncodec, demo-small, workerpool,
    genericset, priorityqueue, tasks-api-noshadownudge
    07-15  ledger                 07-17  taskapi, workapi, usersapi, taskflow,
    logstats, kvservice, ratelimit, shortener, tasks-api, taskapipro
=> "177 named tests, 1 missing" is TRUE BOOKKEEPING OVER THE ARTIFACTS WE HAVE
   and is NOT a statement about the model or the current builder. As a hygiene
   number it stands. As a behavioural claim it is pooled garbage, and my commit
   29c5e9c's narrative ("the model never wrote it") quietly implies the second.
   The audit measures ARTIFACTS, not the model. Saying anything about the model
   needs same-process runs, which is what this now is.

WHAT SURVIVES UNTOUCHED, because no model was involved: the mutation result. The
07-15 artifact + credit-drop mutant = build+vet+FULL SUITE GREEN, and the spec's
named test kills it. That artifact IS what this project shipped, and it has a
hole. Deterministic. That finding does not depend on which builder made it.

A SECOND STORY DIED WHILE I WAS CHECKING THIS, and I nearly wrote it down: I
inferred "completeness was ON when ledger-v4 was made, and the rule missed it
anyway" from 173f4c7's date (it went opt-in 07-16 17:22, AFTER the artifact). So
I checked the builder as it actually stood at bb17d7e — the completeness text is
NOT IN IT. grep count: 0. The rule did not EXIST on 07-15; it was born 07-16
12:10 at 612c4f3. My inference was one step from the page and it was false.

=============================================================================
THE REBUILT EXPERIMENT — 3 ARMS, ONE PROCESS (PID 3082), SAME COMMIT
=============================================================================
The hole is one defect with TWO rival remedies, and nobody has ever run them
against each other. This does.
  A. old spec, completeness OFF   (CONTROL — does the miss even reproduce HERE?)
  B. old spec, completeness ON    (does the RULE alone fix it?)
  C. new spec (code shown), OFF   (does the SPEC FIX alone fix it?)

PREDICTIONS, WRITTEN NOW, BEFORE ANY ARM RUNS:
  A: the miss REPRODUCES — TestCreateTransactionMovesBalances still absent.
     Confidence: MODERATE, and I want it on the record that this is the arm I
     am least sure of. The spec text is unchanged since the artifact was made,
     and prose-not-shown is the thesis. But twelve builder commits landed in
     between and any of them could have fixed it by accident. IF A WRITES THE
     TEST, the hole is already closed, my spec fix is UNNECESSARY, and its blast
     radius is PURE COST — I would revert af1932d's spec change and say so.
  B: the rule ON WRITES the test. Confidence: MODERATE. Its one confirmed
     benefit is exactly this failure — workapi's TestListSorted, 3/3, "the model
     drops tests the spec NAMES".
  C: the spec fix WRITES the test. Confidence: HIGH. Sixth instance of the
     thesis; today's guard went in byte-identically 2/2 after seven straight
     observations without it.

WHY THIS IS THE DAY'S REAL EXPERIMENT: if B and C BOTH work, the rule is the
cheaper remedy — no spec edit, therefore no blast radius, and today measured
that a spec edit deterministically deletes a spec-named test from another file.
But the rule costs shortener its green (5/5), which is why it is off. If C works
and B does not, the spec fix is the only remedy and its cost must be paid.
If NEITHER works, showing code is not sufficient for a test the model does not
want to write, and I have been over-selling the thesis all day.

=============================================================================
--- 16:4x (clock READ) — 3-ARM COMPLETE. ALL THREE PREDICTIONS HELD. ---
=============================================================================
One server process (PID 3082), one commit, ~75 min, three arms:

  ARM              testfuncs  store pkg  named test  mutant
  A old spec, OFF     31        5/7      ABSENT      MISSED — GREEN ON BROKEN CODE
  B old spec, ON      33        7/7      PRESENT     CAUGHT — teeth
  C new spec, OFF     33        7/7      PRESENT     CAUGHT — teeth

A: PREDICTED "the miss reproduces" — HELD, and this was the arm I said I was
   least sure of. It matters most: the hole is LIVE on the current builder in
   this process, not an artifact of a stale 07-15 tree. So the spec fix is NOT
   unnecessary and af1932d stands. Had A written the test I had committed, in
   advance, to reverting it.
   A is WORSE than the old artifact, incidentally: 5/7 store tests where 07-15
   had 6/7 (it lost TestBalancesIsACopy too). Cross-builder, so I am NOT calling
   that a regression — I am noting it and leaving it unattributed.
B: PREDICTED "the rule alone writes it" — HELD. And it writes TWO of the spec's
   missing tests (TestCreateTransactionMovesBalances AND TestBalancesIsACopy),
   and the mutant DIES on it.
C: PREDICTED "the spec fix alone writes it" — HELD, and the test BITES.

BOTH REMEDIES WORK. That is the first time they have been run against each other.

=============================================================================
THE PART I DID NOT PREDICT, AND IT IS THE BIGGEST THING TODAY
=============================================================================
The mutant check REFUSED to score arm C: "MUTANT DID NOT APPLY: found 0 sites".
The guard I built into it this morning fired, and it was right to. The code had
MOVED — and where it moved is the finding:

  spec says: store.go = "the Store INTERFACE only — no implementation in this
             file"; memory.go = the MemStore implementation.
  ARM A:  store.go 109 lines / 7 funcs (the WHOLE implementation)
          memory.go 1 line / 0 funcs  <- A DEAD FILE
  ARM B:  identical. Dead memory.go.
  ARM C:  store.go 16 lines / 0 funcs (interface only — CORRECT)
          memory.go 99 lines / 7 funcs (the implementation — CORRECT)

MY SPEC EDIT — which only added test CODE to the STORE TEST's purpose — moved the
implementation into the file the plan assigns it to and un-killed a dead file.
This is exactly the pathology scope_rule was written for ("EVERY multi-package
spec in the suite carried one of these dead files: workapi, taskapi, taskapipro,
tasks-api"), and scope_rule is ON in arms A and B. It did not prevent it. A test
file's purpose did.

AND NOTHING WAS LOST. C = A + 2, exactly the two missing store tests. The three
apparent "losses" were RENAMES TOWARD THE SPEC'S EXACT NAMES:
    TestCreateAccountDuplicateIsErrExists -> TestCreateAccountDuplicate
    TestGetAccountMissingIsErrNotFound    -> TestGetAccountMissing
    TestListAccountsEmptyIsNotNil         -> TestListAccountsEmpty
    TestListAccountsSortedByID            -> TestListAccountsSorted

=> THE HONEST HEADLINE, and it is sharper than this morning's: THE BLAST RADIUS
   IS REAL AND ITS SIGN IS NOT PREDICTABLE. The same class of act — add code to
   one file's purpose — DELETED a spec-named test in taskapipro (2/2, and 2/2
   again with names stripped) and ADDED two, fixed three names and repaired the
   package's file structure in ledger. I cannot tell from the edit which way it
   will go. That is not "spec edits are dangerous" and it is not "spec edits are
   good": it is that a spec edit is a GLOBAL, UNSIGNED perturbation, and the only
   honest way to ship one is to AUDIT BOTH DIRECTIONS AFTERWARDS. Which is why
   the tool exists, and why it caught this.

TWO OF MY OWN COUNTS WERE WRONG AND I CAUGHT BOTH BY RECONCILING:
  - "C = 32, so it lost one elsewhere" — false alarm. Per-package it is
    10+6+4+6+7 = 33. My project-wide `sort -u` COLLAPSED TestGetAccountMissing,
    which arm C has in TWO packages. The count was my instrument, not the data.
  - "C lost TestGetAccountMissingIsErrNotFound" — false alarm, a rename.
  Both alarms pointed at a loss that was not there; this morning's errors all
  pointed at findings that were bigger than they were. The direction flipped, the
  cause did not: I read a derived number instead of the thing itself.

=============================================================================
CONSEQUENCE FOR THE COMPLETENESS RULE — ITS LEDGER HAS CHANGED
=============================================================================
The rule is OFF by default because of "a cost that reproduces and a benefit that
does not" (173f4c7). The benefit has now reproduced TWICE MORE, independently:
    ratelimit  0 effect        (2 processes, 7 arms)
    workapi   +1 named test    (3/3, pre-gate)
    ledger    +2 named tests   (this run) — AND one of them has DEMONSTRATED
                                TEETH: without it, a ledger that vaporises money
                                ships green.
That is no longer "a benefit that does not reproduce". The cost side is untouched
by this and still stands (shortener, 5/5) — but shortener's SPEC has since been
fixed, which is the exact condition the code itself set for re-testing it. That
run is next, and its prediction is already filed in PREDICTION-completeness.txt,
written before any of this.
