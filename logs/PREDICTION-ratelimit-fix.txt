PREDICTION — ratelimit test-spec fix (extract the cap clause into a named+shown test)
Written 2026-07-17 21:3x (clock READ), BEFORE the run. Run queued for the next server slot.

THE FIX: the burst-cap invariant was a prose clause buried in TestBucket's
description ("tokens never exceed capacity after a long idle gap"), and the model
dropped it — mutation proved the suite has no teeth for it. Following the ledger
pattern, extracted it into its OWN named test, TestBucketCapAfterIdle, with the
code SHOWN (idle an hour, assert refill to capacity 2 and no further).

ALREADY VALIDATED MODEL-FREE (not predicted — measured): the exact test I put in
the spec PASSES on the correct artifact and FAILS on the clamp-removed mutant
("3rd Allow should be FALSE"). So IF the model writes it, the suite gains teeth
for the cap. The open question is whether the model writes it, and the blast
radius of the edit.

I PREDICT (one run, same process, before/after audit MANDATORY):
  1. The model WRITES TestBucketCapAfterIdle (shown as code = the thesis; 6th
     instance). ratelimit goes from 5 -> 6 test funcs.
  2. GREEN, coverage ~flat-to-up (the clamp line was already executed; now it is
     also ASSERTED, which coverage cannot distinguish, so ~flat is fine and a
     jump would be suspicious).
  3. The new test BITES: re-applying the clamp mutant to the NEW artifact turns it
     red. (This is the whole point — not that the test exists, that it has teeth.)
  4. BLAST RADIUS: the before-state is preserved at generated/_ratelimit-BEFORE
     (5 funcs: TestBucket, TestRegistryDistinctClients, TestRegistrySameClient,
     TestRouterHealth, TestRouterPing). After: those 5 PLUS TestBucketCapAfterIdle
     = 6, nothing lost, nothing renamed-and-dropped. I am NOT certain of this —
     today a spec edit deleted a test in taskapipro and repaired file structure in
     ledger, both unpredicted. So the after-audit decides, not this prediction.

WHAT WOULD REFUTE / REVERT:
  - Any of the 5 existing tests disappears or renames-and-drops => blast radius
    cost; weigh it against the teeth gained, and if net-negative, REVERT the edit.
  - The model writes the name but a weakened body (e.g. Add(time.Second) not Hour,
    or drops the "3rd is FALSE" assertion) => present-but-toothless, the ratelimit
    hole all over again. Re-apply the mutant; if it survives, the SHOWN code was
    not copied and I report showing-code-insufficient-here.
  - Not green => open the failure, do not count it.

RUN: ./_ab_run.sh ratelimit ; _named_test_audit.py ratelimit ; diff test-func
  lists vs /tmp/rl-before.txt ; _mutant_check-style clamp re-mutation on the new
  artifact. Keep or revert on the after-audit.

--- 22:0x (clock READ) — RESULT: ALL FOUR PREDICTIONS HELD. CLEAN KEEP. ---
  1. Model WROTE TestBucketCapAfterIdle. HELD (shown code = thesis, 6th instance).
  2. GREEN, 75.0 -> 77.0. HELD (clamp line now asserted, not just executed; +2 is
     consistent, not a suspicious jump).
  3. The new test BITES. HELD — re-applied the clamp mutant to the SHIPPED artifact:
       --- FAIL: TestBucketCapAfterIdle
     The suite now DEFENDS the burst cap it was toothless on. This is the whole
     point: not that the test exists, that it has teeth. Confirmed on the model's
     OWN copy, not mine.
  4. BLAST RADIUS: audited before vs after. Two names changed —
       TestRouterHealth -> TestHealthEndpoint   (byte-identical: /health -> 200)
       TestRouterPing   -> TestRateLimit        (byte-identical: /ping 200 then 429,
                                                  Retry-After == 1)
     Cosmetic renames, IDENTICAL assertions, nothing lost. Verified by reading both
     bodies, not by counting. So the spec edit's blast radius here is benign — but
     it IS non-zero (the model rewrote the whole test file and renamed two tests),
     which is exactly why the after-audit is mandatory and not a formality.

VERDICT: KEEP the edit (committed e76dc51). ratelimit went from a suite that could
not defend its burst cap to one that does, +1 real test, no coverage lost, +2
coverage. The suite-teeth thesis, applied end to end: found the hole by mutation,
validated the fix model-free, showed the code in the spec, and confirmed the model
writes it AND it bites AND nothing else broke.

PRE-EXISTING GAP, NOTED NOT CHASED (not caused by my edit — present in BEFORE too):
the test-spec DESCRIBES a two-clients flow test ("hit clientA once 200 and clientB
once 200 ... then clientA 429 and clientB 429") but neither before nor after wrote
it as its own function. Same described-not-shown pattern as the cap was. A future
ratelimit pass could SHOW that test too. Filed, not fixed — one edit, one audited
change at a time, which is the discipline that kept tonight clean.
