PREDICTION — does a DEDICATED name survive where an added assertion did not?
=============================================================================
2026-07-26, late. Not a new hole: a test of the repair the last finding proposed.

THE SETUP
  Four status assertions closed this afternoon reopened on the next draw. No test was lost;
  the assertions inside them were. Every DURABLE closure today gave its invariant its own
  named test; every unstable one added a line to a test that existed for another reason.

  So the four are re-specified as their own named tests, each asserting ONE thing and
  decoding nothing:
      TestListReturns200 · TestProjectListReturns200 · TestGetOK (now status-only, with the
      body assertion split into TestGetOKBody)

  TestList and TestProjectList go back to what they were: decode and count.

PREDICTION
  1. All four new names are written.
  2. tasks_handler L49 (200 list)     SURVIVED -> CAUGHT
  3. projects_handler L49 (200 list)  SURVIVED -> CAUGHT
  4. tasks_handler L64 (200 get one)  SURVIVED -> CAUGHT
  5. projects_handler L64 (200 get)   — NOT predicted. No project single-GET status test was
     added; TestProjectCreateGet already covers it and has held all day. Listed so a CAUGHT
     there is read as the existing test, not as this edit working.
  6. Nothing displaced: the other named tests all present, json tags still CAUGHT, the five
     registered taskflow mutations still CAUGHT.

  WHAT WOULD REFUTE THE REPAIR: any of 2-4 still SURVIVED. That would mean a dedicated name
  is no better than an inline assertion for this invariant, the ceiling is lower than the
  finding claims, and "name it" stops being the answer for anything an existing test could
  have covered.

  This is one draw. A hit does not make the closure durable — it makes it durable ONCE, which
  is exactly the mistake the retraction was about. The honest claim after this run, if it
  lands, is "the shape that survived one redraw was re-applied and held on the next".

OUTCOME (2026-07-26, generated/taskflow-own) — the repair works, and the one site I
excused reopened
=====================================================================================
    rc=0   687s   GREEN

    1. All four new names written (TestListReturns200, TestProjectListReturns200,
       TestGetOK status-only, TestGetOKBody).                          HIT
    2. tasks_handler    200 list     SURVIVED -> CAUGHT                HIT
    3. projects_handler 200 list     SURVIVED -> CAUGHT                HIT
    4. tasks_handler    200 get one  SURVIVED -> CAUGHT                HIT
    5. projects_handler 200 get one  — NOT predicted, and it SURVIVED.

  Nothing else moved: all four json tags still CAUGHT, all five registered taskflow
  mutations CAUGHT, no named test lost.

  THE FIFTH ROW IS THE RESULT. I excluded it on the grounds that "TestProjectCreateGet
  already covers it and has held all day" — which is precisely the assumption this
  finding says is unsafe. The 200 asserted INSIDE that test drifted away on this draw
  exactly as the other four had, while the test itself stayed. So one draw confirms the
  rule in both directions at once: three invariants given their OWN named test all held;
  the one left inside an existing test reopened.

  Three of three is not durability, it is one draw — the same caveat the retraction was
  about. What it does establish is that the repair SHAPE is right, and that my reason for
  exempting the fourth site was the very error under study.

  TestProjectGetReturns200 is now specified the same way. If it holds on the next draw
  alongside the other three, "give the invariant its own named test" has four sites and
  two draws behind it.
