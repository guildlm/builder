FINDING — taskapipro's tasks twins, the CAUGHT control arm, came back BROKEN in the redraw
==========================================================================================

Caught 29 July ~09:58 by reading internal/api/router_test.go the moment it landed (file 17
of 21) and BEFORE the fix loop ran. The as-drawn file is snapshotted, because the fix loop
rewrites this file and the evidence of what was first drawn does not survive it.

This is the number the redraw exists to produce. The save point's instruction was to read
the CAUGHT -> SURVIVED direction first, because it is the only one that can retract a claim
made this week. It arrived by hand-inspection before the sweep could ask.

WHAT THE SPEC ASKS (lines 458-463, unchanged since the closure)
    TestListDefaultsToTheConfiguredPageSize: newRouterWithPaging(2, 100), POST THREE
    tasks, GET /tasks with NO limit, assert as the test's ONLY assertion that EXACTLY 2
    come back.
    TestListCapsAtTheConfiguredMaxPageSize: newRouterWithPaging(20, 2), POST THREE tasks,
    GET /tasks?limit=100, assert EXACTLY 2.

and it then spends six lines explaining WHY, naming this exact failure:
    "THE SMALL NUMBERS ARE THE POINT — do not 'simplify' them to the real defaults. The
     obvious version of this test is 'the default is 20, so POST 25 and expect 20', and it
     is the version that CANNOT FAIL... The witness has to sit ON the boundary, not past
     it."

WHAT WAS DRAWN

    twin                                        router built           POSTs   asserts
    ------------------------------------------  ---------------------  ------  --------
    TestListDefaultsToTheConfiguredPageSize     newRouterWithPaging(2,100)   1   len == 2
    TestListCapsAtTheConfiguredMaxPageSize      newRouterWithPaging(20,2)    2   len == 2

Both routers are built EXACTLY right — the small numbers survived, which is what the spec's
six lines of reasoning were defending. The ITEM COUNT is what broke, and it broke in two
different directions:

  TWIN A CANNOT PASS. One task in the store, assert exactly 2. 1 != 2, so t.Errorf fires
  against a completely correct handler. This test goes RED on a correct tree.

  TWIN B CANNOT FAIL. Two tasks in the store, max page size 2, request limit=100. A correct
  handler clamps 100 to 2 and returns 2. A handler that IGNORES maxPageSize entirely
  returns all 2 items, which is also 2. No input distinguishes them — the identical
  bitset-shaped defect that made chain4's PROJECTS twin SURVIVE on three sites, now on the
  TASKS arm that was CAUGHT.

WHY THIS MATTERS MORE THAN A RED TREE
The tasks twins were the CONTROL. Their entry carries the attached reason; the projects
entry lacked one, SURVIVED on three sites, and was repaired by giving it the same treatment.
The whole comparison rests on the tasks arm reproducing. It did not.

    An attached reason, present and detailed and naming this exact failure mode, did not
    secure the witness across a redraw of the same spec.

That does not overturn FINDING-the-test-description-is-the-unit-of-transfer.txt — the reason
still governs what lands rather than whether it lands, and the ROUTER ARGUMENTS, which is
what the reason argues hardest for, came back perfect both times. What it kills is any
reading that an attached reason makes a witness DURABLE. It bought the boundary numbers and
not the item count, twice, in the same file.

CONSEQUENCE FOR THE PENDING PREDICTION, STATED BEFORE THE PROJECTS FILE LANDS
PREDICTION-the-two-specs-the-reboot-owed-me.txt puts "the projects test POSTs THREE" at 70%
and "the three sites flip to CAUGHT" at 60%. Those stand as written. But the control arm
just failed the same check, so a projects failure can no longer be read as "the attached
reason did not work" — the reason did not work HERE either, with a longer justification, on
the arm that previously passed. The honest reading of a double failure is a model-level
difficulty with item counts in paging witnesses, not a fact about spec prose. A projects
SUCCESS against a tasks FAILURE would be the surprising result now, and the interesting one.

A NEW PREDICTION, REGISTERED BEFORE THE FIX LOOP RUNS — this is minutes away and binary.
Twin A goes red, so the fix loop WILL edit it. There are two repairs available:
    (a) add two POSTs           -> correct, keeps the closure discriminating
    (b) change the assert to 1  -> green, and DESTROYS the closure, converting Twin A into
                                   exactly Twin B's cannot-fail shape
PREDICT (b), ~65%. Every fix round this campaign has recorded landed on the TEST side, and
changing one literal is a smaller edit than adding two request blocks. If (b) happens, the
tree goes GREEN with both tasks twins dead, and NOTHING IN A GREEN SUITE WOULD SAY SO —
which is this project's founding observation reproducing inside its own control arm.

⚠️ READ THE FIXED FILE, NOT THE RESULT LINE. taskapipro-v5 reporting GREEN is compatible
with both repairs and distinguishes neither.

EVIDENCE PRESERVED
The as-drawn file is copied to scratch before any fix round touched it, and the two facts
that matter are transcribed above verbatim: router arguments correct, POST counts 1 and 2,
assertions 2 and 2. Re-derivable after the fact only from this note.


ADDENDUM — THE TREE ALSO DOES NOT COMPILE, AND THAT COMPETES FOR THE FIX BUDGET
==============================================================================
Checked on a COPY of the tree, not the live one, while generation continued.

TWO COMPILE-BLOCKING DEFECTS, both found before the fix loop started:

1. IMPORT PATH, three files. go.mod declares `module guildlm.dev/taskapipro` and the spec
   sets `go_module: guildlm.dev/taskapipro` on line 3. But every file in internal/api uses
   the UNPREFIXED path:

       internal/api/projects.go   "taskapipro/internal/models"      WRONG
       internal/api/router.go     "taskapipro/internal/service"     WRONG
       internal/api/tasks.go      "taskapipro/internal/models"      WRONG
       internal/service/service.go "guildlm.dev/taskapipro/internal/models"   right

2. UNDEFINED VARIABLE, in Twin B itself. TestListCapsAtTheConfiguredMaxPageSize declares
   w1 and w2, then assigns `w = httptest.NewRecorder()` — w is never declared in that
   function and there is no package-level w. Straight compile error, inside the very test
   whose witness cannot fail.

THE TRANSFER-UNIT LAW DOES NOT EXPLAIN DEFECT 1, AND SAYING SO IS THE POINT.
The tempting story is that the entries stating the import path produce the right one. The
spec names the full path in exactly two entries — go.mod (line 17) and
internal/store/store.go (line 118) — and in neither of the files that got it right. Nothing
told internal/service/service.go the path and it wrote it correctly; nothing told the three
api files and they wrote it wrong. A law that covers four measurements does not cover this
one, and forcing it to would cost more than the law is worth. Recorded as unexplained.

WHY IT MATTERS FOR THE PREDICTION ABOVE
--max-fix-rounds is 5. Two compile errors across four files have to clear BEFORE any test
runs at all, so the rounds that would repair Twin A's witness may be spent on imports and
an undeclared variable. That is a THIRD outcome my binary prediction did not list:

    (c) the loop exhausts its rounds on compile errors and the tree ships NOT-GREEN with
        both twins exactly as drawn — Twin A still unable to pass, Twin B still unable to
        fail, and neither ever evaluated.

If (c) happens the prediction is not graded, because the experiment it was about never ran.
That is a genuine miss in how the prediction was written: it assumed the fix loop would
reach the assertion, and a prediction should name the conditions under which it is void.
The 65% stands for the case where Twin A is actually edited.


CHECKPOINT (~10:11) — OUTCOME (c) IS NOW UNLIKELY, AND THE REASON MATTERS
=========================================================================
The fix loop started and its DETERMINISTIC phase cleared both compile-blocking defects at
zero cost in model rounds:

    + "guildlm.dev/taskapipro/internal/{models,service,store}"   imports added, correct paths
    - w = httptest.NewRecorder()                                 -> w := ...   (both files)
    + req = httptest.NewRequest("POST", ...)                     in TestCreateDuplicate

So outcome (c) — the loop exhausting its five rounds on compile errors and never reaching
the assertion — is now unlikely. The compile errors were never going to cost model rounds,
because a deterministic gate owns them. I priced (c) without checking which phase handles
which defect, and the phases are not interchangeable: Phase 1 is free, the model rounds are
the scarce thing.

THE THIRD DETERMINISTIC FIX IS THE INTERESTING ONE. TestCreateDuplicate POSTed once and
then re-served the SAME consumed request — a one-shot body read twice — and the gate added
the second request. That is a deterministic repair of a SETUP COUNT: exactly the class of
defect prose failed to secure in the twins, fixed mechanically, in the same file, in the
same run.

    The mechanism this morning's result asked for already exists in this builder. It is
    applied to duplicate-POST setup and not to paging setup.

That is a much smaller build than the Phase-1 gate I sketched: not a new capability, an
existing gate widened to a second shape. It still waits for the draws to drain — see
NOTE-do-not-touch-src-until-the-draws-drain.txt — but the estimate drops sharply.

THE TWINS ARE UNCHANGED SO FAR. _twin_witness.py on the tree mid-loop: still 0 of 4, same
seeds (1, 2, 1, 2), same bounds (2, 2, 2, 2). The model-driven round on router_test.go is
running now, and it is what grades (a) vs (b).


CHECKPOINT 2 (~10:15) — THE BUDGET IS 6, NOT 5, AND A ROUND IS NOT WHAT I ASSUMED
=================================================================================
Two corrections to my own arithmetic, both read off the log rather than inferred.

    [guildlm-build] fix-round budget scaled to 6 for 21 files
    [guildlm-build] compile/test FAILED, fix round 1/6

1. THE BUDGET IS NOT --max-fix-rounds 5. The runner scales it by file count: 6 for a
   21-file spec. I quoted 5 from _ab_run_v5.sh's flag and never checked whether the flag
   was the final word. It is a floor the builder raises, not the number in effect.

2. A ROUND IS NOT A MODEL CALL. Round 1 ran the tests, applied ~20 DETERMINISTIC fixes
   across nine files, and THEN made model fix calls on the two test files — all inside
   round 1. So deterministic repairs ride along inside a round rather than consuming
   rounds of their own. My walk-back in checkpoint 1 was right for the right reason, and
   the original (c) worry was wrong for a reason I had not identified: not "compile errors
   are cheap" but "the round is the unit of iteration, and everything in it happens once".

WHAT ROUND 1 WAS ACTUALLY CHASING — the defect found by hand at 10:40:

    ! internal/api/projects.go:7:2: package taskapipro/internal/models is not in std
    ! internal/api/projects.go:8:2: package taskapipro/internal/service is not in std
    ! FAIL  guildlm.dev/taskapipro/cmd/server [setup failed]

The unprefixed import path, exactly as recorded, and a deterministic fix owns it
(_fix_module_prefix is in the gate list). Nothing in round 1 concerned the twins, because
a package that does not compile never runs a test — the assertion failures cannot surface
until the imports clear.

THE TWINS ARE STILL UNTOUCHED. _twin_witness after the router_test.go model round: still
0 of 4, seeds 1,2,1,2. The model's only edit to router_test.go was `w :=` back to `w =` on
line 168 — one line, and not in either twin.

    The experiment that grades (a) against (b) has not started yet. Round 1 was spent
    reaching the starting line.


CHECKPOINT 3 (~10:29) — THE WIDENING TOUCHED THE IMPLEMENTATION AND SPARED THE SITES
=====================================================================================
Round 2 widened past the tests: "fixing internal/api/tasks.go (widened in)",
"...middleware.go (widened in)". That is the regime where a fix round can destroy the thing
being measured, so it was checked rather than assumed.

CHANGED vs the as-drawn snapshot:  projects.go (15 lines), tasks.go (16), router.go (3)
UNCHANGED:                          middleware.go, response.go

Every change in projects.go is an import or an error sentinel:

    "taskapipro/internal/models"      ->  "guildlm.dev/taskapipro/internal/models"
    h.svc.Create(...)                 ->  _, err := h.svc.Create(...)
    errors.Is(err, service.ErrExists) ->  errors.Is(err, store.ErrExists)
    errors.Is(err, service.ErrNotFound) -> errors.Is(err, store.ErrNotFound)

THE FOUR PAGING SITES ARE UNTOUCHED — limit = h.defaultPageSize (x2), limit = h.maxPageSize,
offset = 0 (x2), all still present and still field assignments. The closure targets survived
a rewrite of their own file.

And both durability instruments still agree after it:
    _dead_config    0 of 6 fields dead   (unchanged from before the widening)
    _twin_witness   0 of 4 discriminate  (unchanged — seeds 1,2,1,2)

So the config closure has now survived a redraw AND an implementation rewrite within that
redraw, while the paging witnesses have survived neither. That strengthens
FINDING-structural-closures-survive-redraws-quantities-do-not.txt with a second, harsher
test the structural closure passed and the quantity closure was never in a position to take.

⚠️ WHY THIS HAD TO BE CHECKED AND NOT ASSUMED. A fix round that rewrites projects.go could
have replaced `limit = h.defaultPageSize` with a literal, or restructured the clamp into a
helper — either of which removes the site the closure is graded on, silently, while every
number in this file kept reporting on something that no longer exists. The check costs one
diff. Not doing it is how a measurement outlives its subject.


CHECKPOINT 4 (10:40) — THREE OF FIVE ROUNDS WENT ON DEFECTS THE LOOP CREATED ITSELF
===================================================================================
Five of six rounds spent. internal/api has not compiled once. Both defaults twins are
BYTE-IDENTICAL to the as-drawn snapshot — checked by hashing the function bodies, not by
eye. The fix loop has never touched them.

WHERE THE BUDGET WENT, round by round, with each defect's origin:

  1  taskapipro/internal/models not in std          GENERATION   unprefixed import path
  2  undefined: w  (projects_test.go:183)           GENERATION   as-drawn had `w =`
  3  too many arguments in call to h.svc.List       SELF         widened round mirrored
                                                                 tasks.go into projects.go,
                                                                 carrying a status arg the
                                                                 projects service lacks.
                                                                 As-drawn compiled here.
  4  declared and not used: status                  SELF         round 3's half-repair —
                                                                 removed the argument from
                                                                 the call, left the decl
  5  undefined: w  (projects_test.go:183) AGAIN     SELF         model fix reverted the
                                                                 gate's round-2 repair

    Two rounds on defects the DRAW shipped. Three on defects the LOOP shipped.

AND THE REAL DEFECT NEVER SURFACED. Twin A cannot pass — one item seeded, exactly 2
asserted — so it would have failed loudly the moment the package built, and the loop would
have seen it and had something to repair. It never built. Five rounds of compile churn,
three of them self-inflicted, consumed the budget before a single assertion ran.

    The fix loop repairs what it observes failing. An assertion defect sitting behind a
    build failure is invisible to it, and a loop that generates build failures faster
    than it clears them never gets far enough down the stack to see one.

THE (a)-vs-(b) PREDICTION IS HEADING TO VOID, NOT WRONG. I predicted that when Twin A was
repaired the loop would change the literal (65%) rather than add POSTs. Twin A has not been
repaired, has not been read, has not run. Outcome (c) — which I first wrote as budget
exhaustion, walked back, then restated as recurrence — is what actually happened, and by
the third mechanism rather than either of the first two: crowd-out.

⚠️ ONE ROUND LEFT, and the prediction is not yet void. If round 6 clears the build, the
twins run, both fail, and the loop gets exactly one round to react — which is the narrowest
possible version of the experiment and still counts. Read the twins after the RESULT line
either way; a NOT-GREEN verdict is compatible with the twins being touched and with their
never having been seen, and only the diff distinguishes those.


THE IMPORT-PATH DEFECT REPRODUCES (13:52) — as-drawn against as-drawn
======================================================================
The one permitted taskapipro redraw reached internal/api/tasks.go. Comparing AS-DRAWN to
AS-DRAWN — the 09:59 snapshot against the file just written:

    draw 1, as drawn    internal/api/tasks.go     "taskapipro/internal/models"       WRONG
    draw 2, as drawn    internal/api/tasks.go     "taskapipro/internal/models"       WRONG
    both draws          internal/service/service.go  "guildlm.dev/taskapipro/..."    right

Identical defect, identical file, two independent draws two and a half hours apart.

    The unprefixed import in internal/api is STABLE, not sampling noise. And the correct
    prefix in internal/service is stable too. The split is reproducible.

⚠️ I ALMOST COMPARED THE WRONG THING. My first check read draw 1's PRESERVED tree —
generated/taskapipro-v5-red-evidence — which is the POST-FIX state after seven rounds, where
the deterministic gate had already repaired the path to "guildlm.dev/taskapipro/internal/...".
That comparison said "draw 1 right, draw 2 wrong" and would have been reported as a
REGRESSION. It is the as-drawn snapshot that makes the comparison honest, and the only reason
one exists is that I took it at 09:59 specifically because the fix loop does not preserve what
it repairs.

    Comparing a fresh draw against a repaired tree measures the repair, not the draw. Same
    shape as the day's other population errors, and caught only because the snapshot existed.

WHAT IT MEANS FOR THE EXPLANATION I DO NOT HAVE
Recorded at 10:02: the transfer-unit law does NOT explain this defect. The spec names the
full module path in exactly two entries — go.mod (line 17) and internal/store/store.go
(line 118) — and in NEITHER of the files that get it right. service.go was never told and
writes it correctly; the api files were never told and write it wrong.

A one-off would not have been worth explaining. A defect that reproduces exactly across two
independent draws is, and it now has a clean experimental handle: same spec, same model, two
draws, one file right and three files wrong, every time.

⚠️ BOUNDED: one file of the three confirmed so far. projects.go and router.go are files 15
and 16 and have not been written. If either comes back CORRECT in draw 2, the defect is
partial rather than package-wide and this note needs revising.
