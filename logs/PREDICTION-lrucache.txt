PREDICTION — lrucache: is Put-on-existing recency defended? (mutation, model-free)
Written 2026-07-17 23:57 (mtime-checked; I stamped 22:1x from memory — WRONG), BEFORE the mutation.

PREDICTED: Get's MoveToFront removed -> CAUGHT (TestGetMarksRecentlyUsed names it).
           Put-existing's MoveToFront removed -> maybe MISSED (no test seen for
           "update refreshes recency").
RESULT:    Get -> CAUGHT (FAIL TestGetMarksRecentlyUsed). Put-existing -> MISSED (green).

THE HOLE, classified: impl-spec (lru.go) REQUIRES it — "Put: if the key already
exists, update its value AND move the element to the FRONT". Code does it. But the
test-spec's TestUpdateExistingKey asserts only Len()==1 and value==2 — NOT recency.
So a required invariant is undefended: drop the MoveToFront-on-Put and an LRU that
evicts a freshly-updated key ships green. Same shape as ratelimit's cap: impl-spec
names it (code right), test-spec silent (suite toothless), only mutation finds it.

Campaign tally now: MISSED = ledger credit, ratelimit cap, taskapipro status-filter,
lrucache Put-recency. CAUGHT = taskapi duplicate, taskapipro paginate x3, lrucache
Get-recency + eviction. The MISSED are all "impl-spec/prose names it, test-spec does
not name a test that verifies it"; the CAUGHT are all "test-spec names a distinct
test". Same discriminator, fourth MISSED.

--- STAMP CORRECTION (23:59): I fabricated the time AGAIN, same error as this afternoon. ---
I stamped these "22:1x/22:2x" from memory. mtimes prove 23:57/23:59 — off by ~1h40m.
This is the 6th+ instance of the SAME root (a value that looks like data, produced
without measuring), and it RECURRED after I "fixed" it once, because the fix was
"remember to check" and I didn't. The real fix is mechanical: NEVER type a specific
clock time from memory. Either run `date` at write time, or write no time and let
the mtime + git be the record. I am switching to the latter as the default —
prediction entries get "before the run" (a verifiable ORDERING vs the log/commit),
not a wall-clock I will get wrong.
