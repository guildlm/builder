PREDICTION — is the import break caused by the CONTENT of those twelve lines, or by twelve
lines being there at all?
===============================================================================================

Written 16:40 on 29 July, BEFORE the draw. The question is the one
FINDING-twelve-lines-in-one-entry-broke-three-other-files.txt left open in its own words:

    "which property of those twelve lines matters (length, position, the words themselves,
     the fact that they discuss imports-adjacent concepts at all) is untested. The
     separating draws are cheap and obvious: add twelve lines of UNRELATED text to the
     projects entry and see whether the imports break anyway."

THE ESTABLISHED TABLE, from round-1 logs across four draws
-----------------------------------------------------------
    draw       projects entry       server        round-1 verdict
    chain4     PRE-edit             28 Jul        FINE, measured
    chain5     PRE-edit             29 Jul/4439   ⚠ UNMEASURED  (corrected 17:26)
    v5 (1st)   POST-edit (+12)      29 Jul/4439   DEFECT, 4 error lines
    v5 (2nd)   POST-edit (+12)      29 Jul/4439   DEFECT, 4 error lines

⚠️ CORRECTED AFTER THIS PREDICTION WAS WRITTEN, BEFORE THE RESULT LANDED. chain5's row read 0
when this file was written. It is not a 0: Go skips packages whose dependency failed, chain5's
round 1 failed in internal/store, and the string "internal/api" never appears in that round —
so its imports were never evaluated. See the third correction in
FINDING-twelve-lines-in-one-entry-broke-three-other-files.txt.

    THAT RAISES THE STAKES OF THIS DRAW RATHER THAN CHANGING ITS BRANCHES. chain5 was the only
    PRE-edit arm on server 4439, so it was what held the server fixed. Without it, spec and
    server differ together across the surviving rows. This draw is pre-edit prose on 4439 — so
    a measured grade here supplies the arm chain5 was supposed to be, and does it for the
    ORIGINAL question at the same time.

THE CONTROL ARM
----------------
    inert      PRE-edit +12 INERT   29 Jul     ?

Same file, same entry, same insertion point, same line count (12), same six-space
indentation, same paragraph shape. Different subject: the twelve lines give reasoning for
the `Never name a local variable t` rule that is ALREADY in that entry, and add no new
requirement to anything.

OUTCOME VARIABLE, fixed in advance
-----------------------------------
The count of import-path errors in ROUND 1 of the log — the as-drawn state, before any
repair. Read from the log, never from the tree. Comparing draws by their post-fix trees is
the population error already made three times today.

BRANCHES
---------
  A  imports break (>=1 import-path error in round 1)      -> LENGTH/POSITION IS SUFFICIENT.
     The content of the real twelve lines is irrelevant to the defect. Adding any twelve
     lines to that entry destabilises three implementation files' imports, which makes the
     spec's blast radius a function of SIZE, not meaning, and makes every future spec edit
     a risk to files it does not mention.

  B  imports clean (0 import-path errors in round 1)       -> NOT LENGTH ALONE. Something
     about the real twelve lines specifically is doing it. Does NOT identify what: see the
     limitation below.

  C  0 import errors AND internal/api never appears in round 1 — a different package failed
     first, so the imports were never evaluated             -> UNMEASURED. Not a 0. Report as
     such and score nothing.

⚠️ BRANCH C IS SHARPER THAN IT WAS, AND ITS PRIOR IS NOT NEGLIGIBLE (refined 17:27, before the
result). As first written, C was vague — "does not reach a round-1 import check". The grader
makes it a decision procedure:

     no round 1 at all              the whole tree compiled -> imports FINE. That is BRANCH B,
                                    measured, NOT inconclusive. The original wording would
                                    have mis-scored a converged draw as C.
     >=1 import-path error          BRANCH A.
     0 errors, internal/api present it compiled and said nothing about imports -> BRANCH B.
     0 errors, internal/api absent  BRANCH C.

     One of the four draws on record (chain5) lands in C. So C is roughly a 1-in-4 outcome on
     this spec, not a remote possibility, and this prediction would have scored it as B.

THE PROBABILITIES ARE UNCHANGED — only the decision procedure is sharpened, and it is being
written down BEFORE the log exists rather than chosen after it. PREDICT A, 60%, with C's own
share now stated: A 60% · B 25% · C 15%.

Graded by `_grade_import_defect.py`, whose self-test's decisive case is that the two
zero-count arms must receive different verdicts.

⚠️⚠️ THE RUNNER'S OWN INLINE VERDICT IS SUPERSEDED AND WRONG IN TWO OF FOUR CASES. DO NOT
RECORD IT. _inert_prose_draw.sh was written at 16:41, before the masking problem was known,
and it will print a branch line when it finishes. Both of its zero-paths are defective in
exactly the ways the refinement above fixes:

    _inert_prose_draw.sh:144   no round-1 marker      -> prints "BRANCH C — INCONCLUSIVE"
                               ACTUALLY: the tree compiled, so imports resolved. BRANCH B,
                               measured. The strongest possible clean result, scored as no
                               result at all.
    _inert_prose_draw.sh:162   IMP_LINES == 0         -> prints "BRANCH B — imports are CLEAN"
                               ACTUALLY: clean ONLY IF internal/api was reached. If a different
                               package failed first this is BRANCH C, UNMEASURED — which is
                               precisely where chain5 landed and would have been scored B.

It is not edited because it is RUNNING, and bash re-reads a live script from a byte offset —
splicing into one mid-execution can feed a half-line into the middle of a command. The script
still does the two things that matter and cannot be redone later: it derived and verified the
variant spec, and it captured the log. The grading is the part that is cheap to redo and it
gets redone with the right instrument.

    THE PROCEDURE WHEN IT FINISHES: run
        .venv/bin/python _grade_import_defect.py logs/inert-taskapipro-07291641.log
    and record ITS verdict. Ignore the branch line the runner prints above it.

Weak either way and stated weakly on purpose. For A: the mechanism most available is that
twelve lines shift the prompt context for every file generated after them, and nothing in the
real twelve lines names an import path — neither the words `guildlm.dev/taskapipro` nor any
module prefix appears in them, so a content-specific route has no obvious surface. Against A:
the defect reproduced EXACTLY, four errors twice, and a diffuse length effect is a poor
explanation for a defect that lands on the same three files with the same count on two
independent draws.

⚠️ WHAT ONE CONTROL DRAW CANNOT SEPARATE, stated before the result rather than after
-------------------------------------------------------------------------------------
Three candidate mechanisms are on the table: LENGTH/POSITION, VOCABULARY (the real lines are
dense with handler/store/page-size words; the control's are not), and THE SPECIFIC CONTENT.
This draw separates the first from the other two and nothing more. If the result is B, the
honest reading is "not length alone", and a second control — twelve lines of API-vocabulary
prose that still adds no requirement — is what would separate vocabulary from content. That
follow-up is NOT pre-authorised here; it is a new experiment if B lands.

⚠️ AND THE CONTROL IS NOT SHAPE-IDENTICAL. The real twelve lines cite a measurement ("measured
on the previous draw ... SURVIVED on all three paging sites"). The control cites none, because
writing a fabricated measurement into a tracked spec to make a control look tidier is not a
trade this repo makes. So form is matched on position, length and indentation, and NOT on
rhetorical mode. If B lands, that unmatched dimension is a live alternative to "content".

N=1. A single draw cannot distinguish a mechanism from a coin, and the pre/post table is only
deterministic at n=2 per arm. If A lands, the strong claim needs a second inert draw before it
is worth writing down as a property of the pipeline.
