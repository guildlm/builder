NOTE — conditions and voids, 30 July, server 2212
=================================================

09:58 · STUB REGRESSION probe on the cross-process pair: VOID, not null.
Ran _stub_files.py on taskflow-chain6 (4439) and taskflow-xserver (2212), the same-spec
cross-process pair. Both "no stub files". That answers NOTHING about open question #3.

    taskflow is a FLAT 16-file spec. It has no internal/store/memory.go. The regression is
    three v5 trees — ledger, taskapipro, workapi — writing that file as 14 bytes of bare
    `package store` while the spec describes eight methods. A spec with no such file cannot
    exhibit it, so a clean result here is the probe not applying, not the defect being absent.

    This is the VOID branch the 29 July record demanded be added to every prediction set:
    "four predictions went VOID because the machine never reached the question." Recording it
    as a void rather than letting "both clean" read as evidence.

    ✅ USEFUL CONSEQUENCE: ledger IS one of the three affected specs, and the ledger control
    draw is already required for the pre-registered name-swap arm. That draw therefore does
    double duty — it is the arm's control AND the first 2212 sample of a spec that showed the
    regression on 4439. It still will not separate server from day (2212 and 30 July change
    together), but it is the sample that does not currently exist, at no extra GPU cost.

11:00 · STUB REGRESSION did NOT reproduce on process 42826 — and today reframes what that means.
The ledger control draw wrote internal/store/memory.go at 2282 bytes with NewMemStore and six
methods. On 4439 that same file came out as 14 bytes — `package store` and a newline — in three
v5 trees while the spec describes a full MemStore. _stub_files.py: no stubs in the tree.

    ⚠️ THIS IS NOT A RESOLUTION OF OPEN #3, AND THE REASON CHANGED TODAY. It was already
    confounded (server and day move together). But this morning's result adds a THIRD reading
    that did not exist when #3 was written, and it is the most parsimonious one:

        PROCESS IDENTITY ALONE CHANGES WHAT GETS WRITTEN. Three processes, same spec, three
        different programs — and the two sharing a boot were the furthest apart.

    So "the stub appeared on 4439 and not on 42826" may be nothing more than "those are different
    processes", which is now a MEASURED property of this pipeline rather than a hypothesis. #3
    asked "server or day?" and the honest answer may be "neither — pid", a category the question
    did not contain.

    What would actually test it: several draws of ledger across several processes, counting how
    often memory.go comes out as a stub. A stub rate is a measurement; one clean draw is not.
    Recording that so this clean file is not later cited as "the regression is fixed".

    ⚠️ Unrelated to the stub question and NOT chased: the written file has Balances() returning
    map[string]models.Money while the spec's field is money.Money, and the service layer expects a
    Balance(id) method. That is a candidate defect in this draw, noted only so it is not mistaken
    for a clean bill of health. ledger has drawn NOT-GREEN before and green is not this arm's
    question — the name swap is graded as-drawn from .pre-fix.json either way.

11:14 · CORRECTION — the throughput collapse is CONTENTION, not thermal throttling.
At 11:12 I recorded the 7x prefill slowdown as "consistent with sustained-load throttling after
two hours of continuous GPU work". That was a guess and it is wrong. Measured at 11:14:

    pmset -g therm : "No thermal warning level has been recorded"
                     "No performance warning level has been recorded"
    load average   : 14.85
    godot                                     89.1% CPU
    com.apple.Virtualization.VirtualMachine   78.5% CPU, 23% MEM   (colima's VM)
    WindowServer                              39.9%
    claude x3                                 ~30% each
    memory free    : 77%, 0 pageouts

The machine is not hot, it is BUSY. Two large unrelated processes — a Godot editor and colima's
Linux VM — are taking ~170% CPU between them. colima is also what put the ssh listener on :8080
that broke the server guard at 10:10, so the same background service has now interfered with this
session twice, in two unrelated ways.

    ⚠️ NOT ACTED ON. Both are the user's own applications and killing them is not mine to do —
    Godot may hold unsaved work. Recorded so the condition is attributable rather than mysterious,
    and so the earlier "thermal" line is not left standing as fact.

    WHAT IT CHANGES ABOUT THE ARM: nothing that the 11:12 note did not already cover. Contention
    and throttling are the same class of variable — machine throughput — and that variable was
    measured inert at 24% by the xserver/xserver2 pair. Knowing the cause does not make the
    extrapolation to 3x any safer; it only makes the confound nameable.

    WHAT IT CHANGES PRACTICALLY: the slowdown is recoverable rather than physical. Closing Godot
    and colima would restore most of the throughput. That is the user's call, and it must NOT be
    done mid-arm anyway — the control is already drawn on 42826 and the arm must share it.

12:42 · INTERIM (5 of 19 files) — outcome (b) is live, and the collateral is a BUG FIX.
The ledger prose arm's target file internal/money/money.go is byte-identical to the control's.
internal/models/models.go, which the edit does not mention, differs in CODE:

    control   ErrInsufficient      = errors.New("insufficient funds")
    arm       ErrInsufficientFunds = errors.New("insufficient funds")

The control reproduced ledger's long-standing defect — the spec asks for ErrInsufficientFunds, the
draw writes the shortened ErrInsufficient, response.go uses the full name, and the tree does not
compile. The campaign recorded that defect as surviving SIX fix rounds unrepaired, with the model
taking models.go three times without correcting the name.

The arm wrote the spec-correct name. The only input difference is two words transposed in a
DIFFERENT file's import sentence: "import exactly strconv, strings" -> "strings, strconv".

    ⚠️ INTERIM — 5 of 19 files. 14 remain, the arm may yet be NOT-GREEN for other reasons, and the
    final grade runs from .pre-fix.json. Recorded now because it is the pre-registered outcome (b)
    at 25%, which was the branch I called unlikely, and because it would be easy to lose.

    WHAT IT SHOWS IF IT HOLDS: the edit changed nothing where it was aimed and repaired a defect
    three files away that six deliberate fix rounds could not. That is the strongest single
    demonstration in this whole record that the draw is not a function of the spec in any local
    sense — and it cuts against the campaign's own framing, where that defect was written up as a
    capability limit ("the longest name got shortened and stayed shortened").

    ⚠️ AND IT IS NOT A FIX ANYONE CAN USE. Nothing about transposing an import list makes sentinel
    names correct; this is a reroll that happened to land better, exactly like the 6-up/2-down
    redraw drift measured on 29 July. The useful reading is about FRAGILITY, not about a technique.
