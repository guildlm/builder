PREDICTION — lrucache fix (extract Put-recency into a named+shown test)
Written 2026-07-17 23:59 (mtime-checked; I stamped 22:2x from memory — WRONG), BEFORE the run. Same pattern as ratelimit.

Fix validated model-free already: TestUpdateMarksRecentlyUsed passes on correct
code, fails on the Put-no-MoveToFront mutant. Now put it in the test-spec, shown
as code, and run.

I PREDICT (one run, before/after audit MANDATORY):
  1. Model WRITES TestUpdateMarksRecentlyUsed (shown code = thesis). 5 -> 6 funcs.
  2. GREEN, coverage ~flat (the MoveToFront line already ran; now it is asserted).
  3. The new test BITES: re-mutate Put's MoveToFront on the NEW artifact -> red.
  4. BLAST RADIUS: before-state preserved (generated/_lrucache-BEFORE, 5 funcs:
     TestConcurrentAccessIsRaceFree, TestEvictsLeastRecentlyUsed,
     TestGetMarksRecentlyUsed, TestPutGet, TestUpdateExistingKey). After = those 5
     + TestUpdateMarksRecentlyUsed = 6, nothing lost/renamed. NOT certain — the
     after-audit decides (ratelimit renamed 2 tests cosmetically; taskapipro lost
     one). Read the bodies, do not count.
REFUTE/REVERT: any of the 5 lost or renamed-and-weakened; or the new test written
  toothless (re-mutant survives); or not green. Keep only on a clean after-audit.

--- RESULT (before the run: prediction above; after: here) — ALL FOUR HELD, CLEANEST YET ---
  1. Model WROTE TestUpdateMarksRecentlyUsed. HELD.
  2. GREEN, coverage 95.8%. HELD.
  3. BITES: re-mutated Put's MoveToFront on the SHIPPED artifact ->
       --- FAIL: TestUpdateMarksRecentlyUsed
     (first attempt broke the build — my sloppy regex left `c.ll.` dangling; redone
     by removing the whole statement line, clean build, then RED. A broken build is
     not a passing mutation test.)
  4. BLAST RADIUS: ZERO. All 5 original tests kept their exact names, +1 new. No
     renames, nothing lost. Cleaner than ratelimit (which cosmetically renamed 2).

VERDICT: KEEP (committed). lrucache now defends the Put-on-existing recency the
impl-spec required and no test checked. Second end-to-end suite-teeth fix, and the
after-audit was clean this time — but I still ran it, because "probably clean" is
what taskapipro looked like the morning it deleted a test.
