PREDICTION — usersapi's Content-Type gap, and a refutation confirmed from the other side
=========================================================================================
2026-07-25. Sixth closure attempt; the LAST closeable gap on the tally (tasks-api and
taskflow are both RED, so their gaps cannot be graded).

CONTROL, RUN FIRST
      handlers.go:74  drop Content-Type   SURVIVED
      spec mentions Content-Type: 0 times
      site is LIVE — Set precedes w.WriteHeader, so it reaches the response (checked
      because this file's own 429 survivor turned out to be a dead double-write today)

  usersapi names only TWO tests across EIGHT entries (TestListSorted, TestRecoverPanic).
  Everything else is described as a flow. That is the widest gap between promised
  behaviour and named tests in any spec I have run.

I AM NOT PREDICTING GREEN OR RED, AND THAT IS THE POINT
  Two predictors died today on shortener-vs-ratelimit: spec entries and corrective-clause
  density. I have nothing left to guess with, so I am not going to dress up a guess. The
  run is the measurement.

  But it is worth running for a reason beyond the closure. usersapi is EIGHT entries — the
  size at which tasks-api went RED, and the size that this morning's boundary called too
  big. The refutation so far rests on ratelimit: a spec that should have been HARD and was
  easy. usersapi tests the same claim from the other side. If it greens, "8 entries is too
  big" is dead twice over and from both directions; if it goes RED, the size story survives
  on one side while still being refuted on the other, which is not a rescue but is worth
  stating precisely rather than leaving implied.

THE EDIT — state the promise, name the test
      TestContentTypeJSON: on a fresh router, GET /users and assert the RESPONSE header
      Content-Type is exactly "application/json" off the recorder. Every JSON response
      sets it.

PREDICTION
  1. TestContentTypeJSON written and passes.
  2. drop Content-Type flips SURVIVED -> CAUGHT at handlers.go.
  3. usersapi's registered mutation (list sort order) stays CAUGHT.
  4. Green or red: NO PREDICTION. Recorded as unknown, deliberately.

GRADED — 2026-07-25
===================
      rc=0   482s   4 fix rounds   2 escalations   GREEN with the fleet

  1. TestContentTypeJSON written and passes       HIT  (users_test.go)
  2. drop Content-Type SURVIVED -> CAUGHT         HIT  (handlers.go)
  3. registered mutations stay CAUGHT             CANNOT GRADE — see below
  4. green or red                                 no prediction was made; GREEN

(3) IS VACUOUS AND I AM NOT COUNTING IT AS A PASS
      store.go   duplicate User ID -> ErrExists (409)      NOAPPLY
      store.go   List returns users sorted by ID           NOAPPLY

  Both registered mutations are exact strings lifted from the ARCHIVED artifact, and the
  fresh coder wrote that code differently, so neither patch applied. NOAPPLY means the
  check DID NOT RUN. Reading it as "no regression" is precisely the failure I documented
  in four instruments an hour ago — no work done rendering as no problem found — and it
  showed up in my own result the first time I looked away. The honest statement is that
  usersapi's non-regression is UNMEASURED, not clean.

THE SECOND MATCHED PAIR, WHICH IS WHY THIS RUN WAS WORTH ITS COST
      tasks-api   8 entries   RED    (3100s, 9 escalations)
      usersapi    8 entries   GREEN  (482s, 4 rounds, 2 escalations)

  Same size, opposite outcome — pre-registered before the run as the test of this
  morning's boundary "from the other side". The refutation no longer rests on ratelimit
  alone:

      at 7 entries: shortener HARD, ratelimit EASY
      at 8 entries: tasks-api RED,  usersapi GREEN

  Both sizes now contain both outcomes. Entry count does not order these runs at either
  end, and "8 is too big" was as much a description of tasks-api as "7 greens" was of
  shortener.
