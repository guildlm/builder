RESULT — the Delete-404 closure survived its own redraw, character for character
=================================================================================

Read 11:30 on 29 July from workapi-v5/internal/api/router_test.go, the moment it landed and
BEFORE the fix loop touched it. Snapshotted first, because the fix loop rewrites this file
and taskapipro's lesson is that the as-drawn state does not survive it.

THE PREDICTION
PREDICTION-the-two-specs-the-reboot-owed-me.txt registered this at 85%, and called it "the
single most consequential row in the finish run: a miss means the week's closure grades
measure the draw they were made on rather than the spec, which retracts the central claim
and not a detail."

WHAT LANDED

    func TestDeleteMissingReturns404(t *testing.T) {
        h := newRouter()
        req := httptest.NewRequest("DELETE", "/tasks/nope", nil)
        req.Header.Set("Authorization", "Bearer secret")
        rec := httptest.NewRecorder()
        h.ServeHTTP(rec, req)
        if rec.Code != http.StatusNotFound {
            t.Fatalf("DeleteMissingReturns404: want 404, got %d", rec.Code)
        }
    }

Every criterion written down in advance, met:
    present, in internal/api/router_test.go                        YES
    fresh router, NO seeding                                       YES  (newRouter(), no POSTs)
    Authorization: Bearer secret                                   YES  (correct token —
                                                                        wrong or absent
                                                                        gives 401, not 404)
    asserts StatusNotFound and nothing else                        YES

AND IT IS CHARACTER-FOR-CHARACTER IDENTICAL TO workapi-chain2 — `diff` reports no
difference. An independent draw, days apart, with no reason to reproduce it except that the
spec asks, produced the same bytes.

    That is the strong form of the durability claim, and it is the form the campaign has
    been asserting on weaker evidence all week.

ALL ELEVEN DECLARED TESTS ARE PRESENT: TestCreateDuplicate, TestCreateInvalid, TestCreateOK,
TestCreateUnauthorized, TestDeleteMissingReturns404, TestDeleteTask, TestGetTask,
TestHealthz, TestListTasks, TestMalformedJSON, TestReadyz. No mode-1 absence in this file.

WHAT IS CONFIRMED AND WHAT IS NOT
  CONFIRMED — presence, shape, token, assertion, and byte-identity with the graded draw.
              All of it read from source, which is the right evidence for "did the spec
              reproduce the closure".
  NOT YET     — the MUTATION grade. CAUGHT means the test notices the Delete handler's
              ErrNotFound branch being broken, and that needs the tree to build and the
              mutant to run. taskapipro shipped NOT-GREEN and its twins were never
              evaluated at all; workapi's fix loop has not started. Until then this is
              "the closure was rewritten identically", not "the closure still catches".

TWO INDEPENDENT ROUTES PREDICTED THIS, AND BOTH HELD
  * the presence/magnitude law: an assertion is a distinguishable element the spec can name
    and a mirroring draw carries intact — the durable class.
  * the entry contains NO QUANTITY to lose. The "as this test's ONLY assertion" phrase that
    freed taskapipro's seed count is harmless here because the correct seed count is zero:
    "an id nobody created" is the whole precondition and a fresh router satisfies it.

Both routes are now one-for-one on an out-of-sample case. That is worth more than either
was yesterday, and it is still two mechanisms agreeing on one instance.

⚠️ THE HONEST BOUND. This is one closure, in one artifact, on one redraw. taskapipro's four
paging sites went the other way in the same sweep. The campaign's answer to "are closure
grades durable" is now split by CLASS rather than uniform — presence-shaped closures held
(shortener race, usersapi and taskapi empty-list, workapi Delete-404), magnitude-shaped ones
did not (taskapipro paging x4). That split is the result, not a caveat on it.


THE SITE IT DEFENDS ALSO REPRODUCED (11:32)
============================================
A closure needs two things to be gradeable: the test, and a live line for the test to
defend. Read tasks.go as drawn, before the fix loop:

    86  func (h *TaskHandler) Delete(w http.ResponseWriter, r *http.Request) {
    90      if errors.Is(err, service.ErrNotFound) {
    91          writeJSON(w, http.StatusNotFound, nil)
    97      writeJSON(w, http.StatusNoContent, nil)

Line 91 is the exact line the spec cites from a coverage run: "the `writeJSON(w,
http.StatusNotFound, nil)` inside Delete is executed ZERO times by the whole suite" — the
measured hole the closure was written to fill.

So the fresh draw reproduced BOTH halves: the branch, and the test that reaches it. The
mutation is well defined (StatusNotFound -> StatusNoContent, or delete the branch) and has
a live target, which means the pending CAUGHT/SURVIVED grade is a real question rather than
a formality on an absent site.

That is not automatic. taskapipro's closures survived as SITES too — all four field
assignments intact — while their WITNESSES died, and the campaign's other failure mode is
the reverse: a witness written against a line that a later draw refactored away, where the
grade is INERT and says nothing. Here both are present and matched.

⚠️ STILL PRE-FIX-LOOP. tasks.go is as-drawn and the loop has not run; it widened into
implementation files three times on taskapipro. Re-read line 91 after the RESULT line before
grading, because a widened round can move it and the grade would then be about a line that
no longer exists in the form it was measured on.


FIX ROUND 1 (11:33) — GENUINE DEFECTS, PRECISE REPAIRS, CLOSURE INTACT
=======================================================================
workapi's round 1 chased two defects, BOTH from the draw and neither self-inflicted:

    internal/worker/worker.go:40   w.processed.Inc undefined
                                   (sync/atomic.Int64 has Add, not Inc)
    internal/api/router.go:17,20   cannot use http.Handler as func(...) in mux.HandleFunc

Both repaired deterministically, and repaired PRECISELY rather than bluntly:

    mux.Handle("POST /tasks",          auth.TokenAuth(...)(http.HandlerFunc(h.Create)))
    mux.HandleFunc("GET /tasks",       h.List)          <- left alone, correct already
    mux.Handle("DELETE /tasks/{id}",   auth.TokenAuth(...)(http.HandlerFunc(h.Delete)))
    w.processed.Add(1)                                  <- was .Inc()

Only the two auth-wrapped routes changed to Handle; the plain HandleFunc routes were left
as they were. A blanket rewrite would have been the easy wrong fix.

THE WHOLE 404 PATH IS INTACT. Line 20 still wraps h.Delete in TokenAuth, so the test's
`Bearer secret` passes auth, reaches h.Delete, and hits the ErrNotFound branch at tasks.go:91.
Test unchanged, route unchanged, branch unchanged — diff against the as-drawn snapshot
reports the closure byte-identical after round 1.

CONTRAST WITH taskapipro, WHICH IS THE POINT OF WRITING THIS DOWN
taskapipro spent five of seven rounds on defects the LOOP introduced — a mirrored status
argument, its half-repair, and a gate repair the model reverted twice — and died on the one
it created at round 3. workapi's round 1 is two draw defects, two correct gates, nothing
new introduced.

    One round is not a trend, and taskapipro's spiral began at round 3, not round 1.
    So this is evidence that the spiral is not INEVITABLE, not evidence it was rare.

The stall audit says the same thing from the corpus: 30 of 279 logs stalled, so most runs
do converge. taskapipro was the bad case, and the interesting question — which this run may
answer — is whether widening into implementation files is what turns a normal run into that
one. workapi has not widened yet.


THE SITE MOVED, AND THE TEXT IS AMBIGUOUS (11:41) — how the grade must be taken
================================================================================
The caveat written an hour ago said to re-read the branch after the fix loop rather than
trusting the recorded line number. It was right, twice over.

MOVED: the ErrNotFound branch inside Delete was at tasks.go:91 as drawn; after round 1's
deterministic fixes it is at line 93. The sentinel also changed, service.ErrNotFound ->
store.ErrNotFound, the same repair taskapipro received.

AMBIGUOUS, WHICH IS WORSE: tasks.go now contains TWO identical lines.

    line 78   inside Get(...)      writeJSON(w, http.StatusNotFound, nil)
    line 93   inside Delete(...)   writeJSON(w, http.StatusNotFound, nil)

Character-for-character the same. A text-matching mutation — which is how
_grade_clamp_sites.sh works, and how every hand-grade in this campaign has worked — would
hit BOTH, or hit Get's and report a verdict about Delete's.

    A mutation that lands on the wrong branch still produces a clean CAUGHT or SURVIVED.
    Get's 404 is exercised by TestGetTask, so mutating it and running
    TestDeleteMissingReturns404 would report SURVIVED — a false negative that looks
    exactly like a failed closure.

HOW THE GRADE MUST BE TAKEN, and this is the instruction, not a note:
  * locate the branch by CONTAINING FUNCTION, not by line number and not by text
  * mutate only within `func (h *TaskHandler) Delete`
  * verify the unmutated baseline PASSES first, as _grade_clamp_sites.sh does — Delete-404
    is expected green here, unlike taskapipro's Twin A, so a red baseline means something
    else broke and the grade is void
  * a sed on the file is WRONG for this artifact. Scope to the function or extract it first.

This is the third distinct way a grade could have come out clean and meant nothing today:
a static instrument disagreeing with mutation (registered as a prediction), a witness that
cannot pass being read as CAUGHT (guarded in the grader), and now a mutation landing on a
twin line in a different function. All three produce well-formed verdicts.


CORRECTION (11:50) — `undefined: fake` is NOT a dropped symbol
===============================================================
At 11:41 I wrote that round 2's `undefined: fake` was "a second dropped symbol that restore
by name didn't catch", by analogy with failStore. Read the code instead of the analogy:

    func TestCreateStoreError(t *testing.T) {
        svc := NewTaskService(failStore{}, &fakeEnqueuer{})     <- constructed ANONYMOUSLY
        ...
        if len(fake.Events) != 0 {                              <- reaches for a name that
                                                                   was never bound here

`fake` exists, but as a LOCAL at line 21 of a DIFFERENT test function. Nothing was dropped by
any rewrite. The draw wrote a test that constructs its double inline and then inspects it
through a name belonging to another function's scope.

So it is a generation defect of a different kind — an assertion that needs a handle the setup
never created — and it has nothing to do with the candidate-guard behaviour I was describing.
The failStore case genuinely was a dropped declaration, restored by name; this one is not the
same thing and I attached it to that story because it appeared in the adjacent round.

    Two errors in consecutive rounds of one run, and I assumed the second was a sequel to
    the first. why_dirty's own docstring warns about exactly this: "a rejection landed in
    the same round as an `undefined: failStore`, the two looked consistent, and I was one
    inference from crediting the rule I had just written. Consistent is not proof."

I quoted that warning at 11:37 and made the same move three minutes later.

WHAT IS UNAFFECTED: everything about Delete-404. This is internal/service; the closure is in
internal/api, which reports `ok` and has run its tests. The stall finding is also unaffected —
rounds 2 and 3 do carry the identical actionable error, whatever its cause.
