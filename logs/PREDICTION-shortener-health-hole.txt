PREDICTION — closing shortener's /health hole with a NAMED test
===============================================================
2026-07-25, written BEFORE the spec edit and the regeneration. Second attempt at this
method; the first (taskflow) failed and this prediction exists because of how.

THE HOLE (logs/FINDING-status-code-holes.txt, exhaustive pass)
  shortener's spec states the endpoint outright — `"GET /health" -> 200 "ok"` — and NO
  test in the artifact touches /health at all. Change the status to 202 and the suite
  stays green. A named behaviour with zero coverage; the cleanest hole the sweep found.

WHY THIS SPEC AND NOT TASKFLOW
  The taskflow attempt came back RED on an unrelated test (TestListLimit) after 47 minutes,
  which voided every verdict — the campaign's own baseline-green rule. shortener greens
  reliably for this base (the routing A/B has it at 719s base-only), so a red result here
  would be informative rather than ambiguous.

THE EDIT — matching the plan this time, which is the whole lesson from the last one
  A separately NAMED test in shortener_test.go's purpose, not a clause appended to an
  existing test's description:
      TestHealthOK: GET /health -> 200 and body "ok".
  The taskflow attempt appended instead of naming, and the model dropped it. Naming is
  what _named_test_audit can see and what this project's law asks for.

PREDICTION
  1. The model writes TestHealthOK. Higher confidence than last time, because the failure
     mode last time was the edit's FORM, not the model's willingness — and every other
     named test in this spec exists in the artifact.
  2. The mutation (StatusOK -> StatusAccepted on /health) flips SURVIVED -> CAUGHT.
  3. The build still greens. shortener is 7 files and this base greens it; adding one test
     that hits an endpoint the router already serves should not disturb the rest.
  4. BLAST RADIUS: I expect the other files to come back equivalent, and I expect to check
     rather than assume — _file_list puts every purpose in every prompt. What would make
     me revert: shortener's registered mutation (redirect 301) going SURVIVED, or the
     build going red on something unrelated.

  If (1) holds and taskflow's did not, that is one clean data point for "name the test,
  do not append to one" — still N=1 each way, and worth exactly that much.

===========================================================================
GRADED — the mechanism worked, the spec sentence did not, and my reason for
choosing this spec was a misreading of my own data.

  RESULT (7B base, 5 fix rounds, 515s): score 2/3, build ok, vet ok, TEST FAIL.

  (1) "The model writes TestHealthOK." CORRECT — `func TestHealthOK(t *testing.T)` is in
      the artifact. Against taskflow's appended clause, which the model dropped entirely,
      this is the contrast the last grading asked for: NAMING produces the test, appending
      a requirement to another test's description does not. Still one run each way.

  (2) "The mutation flips SURVIVED -> CAUGHT." NOT REACHED — no green baseline to grade
      against, and this time the reason is specific rather than mysterious: the test the
      model wrote FAILS. shortener_test.go:107 — "invalid character 'o' looking for
      beginning of value". It JSON-decodes a body that is the plain text `ok`.

      My spec sentence was "GET /health -> 200 and the body is exactly \"ok\"". Every
      other response in this spec is JSON, every other test decodes JSON, and nothing in
      that sentence says this one is not. The model followed the house style. The sentence
      reads unambiguously to someone who already knows the answer, which is the failure
      mode this project's spec-writing law exists to prevent — implicit means broken, and
      "the body is exactly ok" implicitly means plain text only if you already know it.

  (3) "The build still greens." WRONG, and the premise was wrong too. I moved here from
      taskflow because "shortener greens reliably for this base", citing the routing A/B's
      719s base-only run. That run is recorded in RESULT-fleet-ab.txt as **2/3 RED** — it
      took the three-member FLEET to reach 3/3. I read my own result table as support for
      the opposite of what it says. Both specs are red under a plain 7B base, so the spec
      swap bought nothing.

      (A second failure, TestDecodeBadCode, is unrelated and pre-existing in this base's
      output — Decode does not reject bad codes.)

  WHAT TO DO NEXT, in order:
    a. Fix the spec sentence: say the body is PLAIN TEXT, not JSON. That is a one-line
       change and it is the actual defect.
    b. Grade the hole against a GREEN baseline, which on this spec means the fleet, not
       the bare base — the same three members the routing A/B needed. A hole-closing
       experiment run on a spec the coder cannot green is ungradeable by construction, and
       I have now spent two runs learning that twice.
