RESULT — the repair is real, and my one-line summary of it was wrong. ANY rewrite of the
sentinel line fixes the name, including one that adds no information at all.
========================================================================================
10 August. pid 46375 — the one process of four that passed the placebo screen. Eight draws, no
restart, every arm AS-DRAWN (0 repair lines each). Preregs committed before each arm.

    arm             what it changes in models.go's purpose      Δ folded   sha256    size   decls
    ---------------------------------------------------------------------------------------------
    base            nothing                                        —       3a78b0d8  1276    4/5
    base-close      nothing                                        —       3a78b0d8  1276    4/5
    base-final      nothing                                        —       3a78b0d8  1276    4/5
    screen placebo  OTHER sentences, no sentinel named            +54       3a78b0d8  1276    4/5
    treated (ship)  heading + `var ErrX` forms + (each errors.New) +51      ef8c15d6  1361    5/5
    B only          `var ErrX` forms                              +30       ef8c15d6  1361    5/5
    A+C             heading + (each errors.New), names left BARE  +21       ef8c15d6  1361    5/5
    LINE PLACEBO    the line PARAPHRASED, nothing added           +20       649bdef7  1353    5/5

    The line placebo says "Sentinel error values, every one of them matchable with errors.Is:"
    where the baseline says "Sentinel errors, all matchable with errors.Is:". No "exported", no
    declaration form, no errors.New, and the five names byte-identical — asserted one by one in
    the builder's self-test, because this arm's entire value is that it carries NONE of the three
    treatments. Its file differs from the treated file by ONE ERROR STRING ("exists" vs "already
    exists") and declares all five names.

⚠️⚠️ WHAT THIS DOES TO THE RULE I SHIPPED ON 6 AUGUST
    "Show the declaration form in specs — `var ErrX` beats a bare name list" is a WRONG
    DESCRIPTION OF A REAL EFFECT. Three registered arms dismantled it in one session:

        · A+C flips (registered 0.20, MISS) -> B is NOT NECESSARY. 6 August had only shown B
          sufficient and said so; the untested alternative it named has now come true.
        · The line placebo flips (registered 0.60, HIT) -> no INFORMATION is needed at all. A
          paraphrase that changes nothing about what the model is told repairs the declaration.
        · The screen placebo, +54 characters — LARGER than every treatment arm — does nothing,
          three times over counting the baselines.

    So the effect is real, it is local to that LINE, and it is not carried by any content the
    campaign has been able to name. "Show the declaration form" works, but not for the reason it
    claims, and anyone applying it as a general spec rule would be following a story rather than
    the mechanism.

    📌 6 August's RESULT log and the save point's "shippable zero-code rule" must be marked
    SUPERSEDED — the finding is not withdrawn, its DESCRIPTION is replaced:
        OLD: show the declaration form; `var ErrX` beats a bare list.
        NEW (measured, on one screened process): rewriting the sentinel line in any way tested so
             far — four different ways, one of them information-free — makes the declarer write
             the name it otherwise omits. Editing other sentences in the same purpose does not.

⭐ WHAT SURVIVES, AND IT IS NOT NOTHING
    · The repair is REAL and now CONTROLLED. On a process where a bigger content-free edit
      elsewhere in the same purpose does nothing, four edits to the sentinel line all repair it.
      That is the first placebo-controlled evidence in this campaign that any of this works.
    · The effect is LOCALISED TO ONE LINE. +54 characters one sentence away: nothing. +20
      characters on the line: the missing declaration appears.
    · The three baselines bracket the session — first, middle and last, byte-identical — so no
      arm here is drift.

⚠️ AND A THIRD FILE APPEARED, WHICH MATTERS FOR EVERY EARLIER "BYTE-IDENTICAL" CLAIM
    649bdef7 (1353 B) is not one of the two sequences this spec family has produced all week. The
    campaign has been reading byte-identity as the sign of a stable attractor; here a fourth
    manipulation reaches the same OUTCOME (all five declared) by a slightly different file. The
    endpoint that matters is the DECLARATION, and byte-identity is a stronger fact that is not
    always available — a result must not be stated in terms of it by default.

⚠️ WHAT IS STILL NOT CLAIMED
    · ONE process. Rare (1 of 4 informative processes passed the screen) and selected twice over.
      No rate may be computed from any of it; eight processes were probed today, five discarded.
    · The mechanism remains UNNAMED, and today killed four candidate accounts of my own: jobless
      -ness, the parenthetical enumeration, the abstract-word paraphrase, and the declaration form.
    · Nothing about green builds. Every arm dies at file 4 of 19.
📌 CORRECTION TO THIS LOG, SAME DAY — THE RADIUS IS TIGHTER THAN I WROTE, AND IT WAS ALREADY
   MEASURED
    This log first said a same-paragraph, one-sentence-away placebo was "the obvious next control"
    and NOT tested. That was wrong: it is what the screen placebo already is. Its three edits are
    the `Account`, `Posting` and `Transaction` sentences — and the `Transaction` sentence is the
    one IMMEDIATELY BEFORE the sentinel line, inside the same folded paragraph (spec lines 120-123
    against the sentinel line at 124-125).

    So the measured radius is one sentence:

        the sentence DIRECTLY ABOVE the sentinel line, +54 chars across three sentences  -> NULL
        the sentinel line itself, +20 chars, adding no information                        -> FLIPS

    I underclaimed by not checking where my own placebo's edits landed before writing the caveat.

⭐ AND THE SENTENCE BELOW WAS THEN DRAWN — NULL, WHICH MAKES THE RADIUS SYMMETRIC AND THE
   DECLARATION LINE SPECIFICALLY SPECIAL  (registered 0.45 to flip; the 0.55 branch)
    The Account `Validate` clause, directly below the list, paraphrased with content preserved
    (+21 chars — exactly A+C's size, one more than the paraphrase that flipped). It NAMES a
    sentinel (ErrInvalid) and declares none. Result: 3a78b0d8, unchanged.

        one sentence ABOVE, names no sentinel, +54 chars   -> NULL
        THE DECLARATION LINE, adds no information, +20     -> FLIPS
        one sentence BELOW, NAMES a sentinel,      +21     -> NULL

    That separates two rules which every previous draw had confounded, because until now every
    null had touched sentences naming no sentinel and every flip had touched the list:
        (i)  editing the line that DECLARES the sentinels flips it        -> SURVIVES
        (ii) editing any line that MENTIONS a sentinel flips it           -> DEAD
    The below-arm is the stronger of the two nulls: it names a sentinel AND is size-matched to
    the flipping arms, so neither "it mentioned nothing relevant" nor "it was too small" applies.

    ⚠️ A self-test caught a real defect in this arm's builder before it drew: the phrase "returns
    an error wrapping ErrInvalid" occurs TWICE in that purpose (Account and Transaction), so the
    check that the clause had changed failed against a correct edit. The assertion is now scoped
    to the clause the arm actually edits, and a third asserts the other one is untouched.
