RESULT — the empty-list closure caught the bug in the wild, not in a mutation
=============================================================================
2026-07-29. taskapi-v5, during the v5 redraw. Caught live in its fix loop.

    projects_test.go:117: ProjectListEmptyIsEmptyArray: got "null\n", want []
    router_test.go:129:   ListEmptyIsEmptyArray:        got "null\n", want []

This draw built its list with a nil accumulator, so GET /tasks and GET /projects on an empty
store answer `null` instead of `[]`. Both closure tests failed. The fix loop is now repairing
the implementation.

WHY THIS IS STRONGER EVIDENCE THAN EVERY GRADE RECORDED THIS WEEK

Every closure verdict in this campaign — 40 of them — comes from a MUTATION: take a correct
artifact, break one line, ask whether the suite notices. That is a synthetic question. It
establishes that the test COULD catch the bug, given the bug.

This is the other thing:

    The model produced the defect on its own, unprompted, in a fresh draw from the repaired
    spec — and the test caught it.

Before this closure existed, that draw ships GREEN. `null` decodes into a []Task of length
zero exactly as `[]` does, so every existing length check passes; the only assertion that can
tell them apart is one on the RAW body, and until 2026-07-28 no test in this corpus made one.
The endpoint would have gone out returning null to every client that iterates it.

THE TWO HALVES OF THE THESIS, NOW BOTH MEASURED

    "a green suite is not a defended contract"   — measured all week by mutation
    "and the defence catches real bugs"          — measured here, by a draw that made one

The second is the one a mutation programme cannot produce for itself, because it needs a
defect nobody planted. It arrived on the ninth spec of a 22-spec redraw, roughly seven hours
after the closure was written.

BOTH TWINS FIRED, which is the other thing worth noting. This is the mirror specified BEFORE
either half was found open — tasks and projects, written together on the reasoning that
projects.go has its own store call and a request to /tasks never executes it. The draw broke
BOTH accumulators and BOTH tests caught it. Had only the tasks test existed, the projects
endpoint would have shipped returning null with the suite green.

OUTCOME: NOT-GREEN, and neither of the two things I pre-registered

I wrote that the loop would probably repair the accumulator (system working) or "repair" the
TEST (much worse finding). It did neither.

    RESULT taskapi: NOT-GREEN (build=0 vet=0 test=1)

Five fix rounds, seven files touched, every one of them in internal/api.
internal/store/memory.go was never opened and still reads `var tasks []models.Task`. Both
closure assertions survived intact — the loop edited both test FILES and left the assertions
alone. See FINDING-the-fix-loop-widens-within-the-wrong-package.txt: widening resolves the
package from the failing TEST, and in a layered spec the defect is a package down.

So the honest summary of this draw: the closure caught a real bug, the fix loop could not
reach it, and the tree shipped RED — which is the correct outcome. A red tree with a true
test is worth more than a green one with the bug in it, and before 2026-07-28 this draw would
have been the second thing.

AND THE GRADER TOLD ME THE OPPOSITE OF THE TRUTH
_grade_scoped, asked about the closure on this tree, returned PROBE-RED with the message
"the test is wrong, not the code". Exactly backwards: the code has the defect, the test is
right. That wording is a fine default for a probe written against a tree believed good, and
it is an unjustified attribution the moment the artifact is the broken thing. It cannot tell
the two apart — that needs knowing whether the artifact is correct, which is the question —
so it now reports the fact and names BOTH readings instead of choosing one.


AND THE DEFECT IS THE MUTATION, VERBATIM

What the draw actually wrote:

    func (s *MemStore) ListTasks() []models.Task {
        s.mu.RLock()
        defer s.mu.RUnlock()
        var tasks []models.Task          <- nil accumulator
        for _, t := range s.tasks {
            tasks = append(tasks, t)
        }

That is character-for-character the mutation this closure is graded with:
`out := make([]T, 0, n)` -> `var out []T`. I did not choose a contrived break to make the
test look useful. I chose the thing the model does when nothing stops it.

    The nil accumulator is the IDIOMATIC Go choice. `make([]T, 0, n)` is the DELIBERATE one.
    A mutation shape that turns the deliberate form into the idiomatic one is not vandalism —
    it is a rehearsal of the most likely regression.

That retroactively justifies the shape itself, which the whole grading programme rests on and
which nothing had tested. Every "CAUGHT" recorded against nil-slice this week was evidence
about a test. This is evidence about the MUTATION: it models what actually goes wrong.

THE FIX LOOP IS EDITING THE IMPLEMENTATION, NOT THE TEST
Checked while it ran, because the opposite would have been the more important finding:
router_test.go:128 still reads `strings.TrimSpace(rec.Body.String()) != "[]"`, unchanged, and
the loop has widened into internal/api/projects.go and response.go. The test forced the
repair rather than being repaired.
