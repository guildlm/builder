FINDING — the twelve-line projects-entry edit also caused the import-path defect
=================================================================================

15:58 on 29 July. This explains a defect recorded as UNEXPLAINED at 10:02 and confirmed as
"deterministic and unexplained" at 13:52, and it makes the cross-entry effect far larger
than the seed counts alone.

THE EVIDENCE IS THE ROUND-1 LOGS, NOT THE TREES
Round 1's error output describes the AS-DRAWN state, before any repair. Trees on disk are
post-fix and comparing them is the population error I have now made three times today, so
this is read from logs only:

    draw       spec              server     import-path errors in round 1
    chain4     PRE-edit          28 Jul     0
    chain5     PRE-edit          29 Jul     0
    v5 (1st)   post-edit         29 Jul     4
    v5 (2nd)   post-edit         29 Jul     4

Four draws, two spec states, and the import defect tracks the SPEC, not the day and not the
server. Both post-edit draws produce it; neither pre-edit draw does — including chain5, drawn
today on the same process as both v5 draws.

    Adding twelve lines to the PROJECTS entry caused internal/api/tasks.go,
    internal/api/projects.go and internal/api/router.go to drop the module prefix from
    their imports.

WHAT THAT EDIT WAS FOR, AND WHAT IT COST
15e25f3 added twelve lines of measured reasoning to the projects paging entry, explaining why
POSTing one project cannot discriminate a page size. Its intent was to fix two tests. Its
measured effects:

    it did NOT fix the projects twins        1,1 -> 1,2, still 0 of 2 discriminating
    it BROKE the tasks twins                 3,3 -> 1,2, 2 of 2 discriminating -> 0 of 2
    it BROKE the imports in three files      0 errors -> 4 errors
    the tree therefore did not compile       both post-edit draws, seven rounds each
    the capstone lost taskapipro entirely    62 of 314 baseline rows BASELINE-RED, 20%

    A twelve-line addition to one spec entry, intended to close one hole, closed none,
    opened three, and cost the campaign's capstone a fifth of its comparable rows.

THIS IS THE UNEXPLAINED DEFECT, EXPLAINED
FINDING-the-control-arm-regressed-in-its-own-redraw.txt (10:02, extended 13:52) records the
import split as reproducing exactly across two draws with no mechanism: the spec names the
full module path in only go.mod's and store.go's entries, in neither of the files that get it
right, so the transfer-unit law did not cover it. The hypothesis on the table at 13:56 was the
SENTENCE FORM — a standalone import sentence versus a parenthetical aside.

    That hypothesis is now unnecessary. The api entries did not change between pre- and
    post-edit; the PROJECTS entry did. Sentence form is constant across all four draws and
    cannot explain a difference that tracks the edit.

⚠️ WHAT THIS DOES NOT ESTABLISH. Not a mechanism — only that the edit is upstream of the
defect. Twelve lines added to one entry change the whole prompt context for every later file,
and which property of those twelve lines matters (length, position, the words themselves, the
fact that they discuss imports-adjacent concepts at all) is untested. The separating draws are
cheap and obvious: add twelve lines of UNRELATED text to the projects entry and see whether
the imports break anyway.

⚠️ AND IT SHARPENS THE TRANSFER-UNIT FALSIFICATION. That law said text elsewhere does not
reach a test. It does not merely reach a test's SETUP, as recorded at 15:42 — it reaches the
IMPORT STATEMENTS OF THREE IMPLEMENTATION FILES. The blast radius of one entry is the whole
artifact, and nothing in the campaign's model of specs predicted that.


TWO DEFECTS, TWO DIFFERENT CAUSES (16:14) — separated by chain5's round 3
=========================================================================
chain5 runs the PRE-EDIT spec, and its round 3 reports:

    internal/api/projects.go:77:55: too many arguments in call to h.svc.List
        have (context.Context, int, int, string)
        want (context.Context, int, int)

The same mirrored `status` argument the v5 draws hit at 71:55. Different line — the files
differ slightly — same defect. So it appears WITHOUT the projects-entry edit.

    THE IMPORT-PATH DEFECT   caused by the spec edit. 0 in both pre-edit draws, 4 in both
                             post-edit draws, server held fixed. Tracks the spec.
    THE MIRRORED ARGUMENT    caused by the WIDENED REPAIR ROUND. Present in chain5 with the
                             edit reverted. Tracks the loop, not the spec.

I had been treating these as one story since 13:52. They are two, with different causes, and
only the first belongs to the twelve lines.

WHY chain5 WIDENED WHEN chain4 DID NOT — the day difference is the trigger
chain4, same pre-edit spec, converged GREEN after one deterministic fix round. It never
widened, so it never acquired the mirrored argument.

The difference is the one I found at 15:49 and corrected the over-claim for:

    chain4  projects twins  seeds 1 asserts 1   CANNOT-FAIL  -> PASSES, nothing to chase
    chain5  projects twins  seeds 1 asserts 2   CANNOT-PASS  -> FAILS, round 2 chases it

A witness that cannot fail passes silently; a witness that cannot pass fails loudly. chain4
got the harmless one and sailed through. chain5 got the loud one, entered the fix loop
properly, widened into implementation files to chase a TEST-side defect, and the widening
manufactured the mirrored argument.

    So the day/server difference did not break anything by itself. It flipped a broken
    witness from the silent kind to the loud kind, and the loud kind triggers a repair
    regime that introduces a genuine defect.

That is a chain worth stating plainly: an unmeasured day-to-day variation changed which of
two broken shapes a test took, and that choice determined whether the artifact compiled.

⚠️ AND IT VINDICATES THE 15:49 CORRECTION. I nearly wrote that the server was exonerated
outright. It is exonerated for the tasks seed counts and it is NOT irrelevant — the projects
assertion moved 1 -> 2 across days, and that move is what sent chain5 down a path chain4
never took.


chain5 COMPLETE (16:22) — and it grades a prediction that went VOID twice
=========================================================================
    exhausted 7 fix rounds (6 budgeted), still failing
    twins unchanged: projects seeds 1 asserts 2 (CANNOT-PASS x2), tasks seeds 3 (DISCRIMINATES x2)

THE VOID PREDICTION, FROM 09:59, IS NOW ANSWERED — AND BOTH ITS BRANCHES ARE WRONG
It offered two repairs for a witness that cannot pass:
    (a) add two POSTs           -> correct, keeps the closure discriminating
    (b) change the assert to 1  -> green, and destroys the closure
    PREDICT (b), 65%.

The loop did NEITHER, across SEVEN ROUNDS, with the failure naming the file and line
outright:

    projects_test.go:162: ProjectListDefaultsToTheConfiguredPageSize:
                          response = [{1 p1}], want 2 items

It spent every round repairing internal/api/*.go — response, tasks, projects, router — for a
defect in projects_test.go. No handler change can make a store of one item return two.

    THE THIRD OUTCOME: the loop misattributes a TEST-side failure to the IMPLEMENTATION and
    repairs the wrong side until the budget dies.

That prediction has now failed three different ways: void on v5 because nothing compiled and
the twins never ran; void again as "no repair at all" on the sentinel; and now answered, with
neither branch taken because the loop never edited the test.

AND THE MISATTRIBUTION MANUFACTURED A SECOND DEFECT
Rounds 3, 4 and 5 are the mirrored-argument cycle — too many arguments, declared and not
used, too many arguments — introduced by the widening the misattribution caused. So:

    a test-side defect the loop cannot fix by editing implementation
        -> widening into implementation
            -> a mirrored argument that did not exist in the draw
                -> oscillation between that and its half-repair
                    -> budget exhausted, tree red

chain4 escaped this entire chain because its broken witness was the SILENT kind (asserts 1,
cannot fail, passes) rather than the LOUD kind (asserts 2, cannot pass, fails). It converged
green in one round.

    A test that cannot fail is invisible and ships. A test that cannot pass is loud, and the
    loop's response to loud test-side failures is to rewrite implementation until it runs out
    of rounds. Neither broken shape gets fixed; only one of them takes the artifact down.

⚠️ THIS IS THE SAME SHAPE AS THE DRAINED-BODY GATE, ONE LEVEL UP. That gate repaired a test
and silently removed what it tested. This is the loop reading a test-side failure and
repairing the implementation. Both are the machinery being confidently wrong about WHICH SIDE
of a failure to touch, and both are invisible in the RESULT line.


CORRECTION (16:45) — the loop DID edit the test, and the chain4/chain5 comparison is confounded
================================================================================================
Went to design the second separating draw, read chain5's per-round fix targets for the first
time, and three sentences above are wrong. Both errors come from the same shortcut: I read the
ROUND HEADERS and the widened-in lines, and never listed what each round actually targeted.

WHAT THE LOG SAYS, per round:

    ROUND 1   fixing internal/api/projects_test.go
    ROUND 2   fixing internal/api/projects_test.go
              then response.go, middleware.go, tasks.go, projects.go, router.go (widened in)
    ROUND 3   fixing internal/api/projects.go
    ROUND 4   fixing internal/api/projects.go
    ROUND 5   fixing internal/api/projects.go
    ROUND 6   fixing internal/api/projects.go

WITHDRAWN, all three sentences:
  * "the loop spent seven rounds editing internal/api handlers"        — rounds 1 and 2 did not.
  * "it spent EVERY round repairing internal/api/*.go ... for a defect
     in projects_test.go"                                              — false for rounds 1-2.
  * "neither branch taken BECAUSE THE LOOP NEVER EDITED THE TEST"      — it edited it twice.

    THE THIRD OUTCOME, RESTATED. The loop did NOT misattribute the failure. It identified
    projects_test.go correctly and repaired it in the first two rounds. The repair did not
    move either number — seeds stayed 1 and the assertion stayed 2, from first observation
    through to the final tree. Then round 2 widened into implementation IN THE SAME ROUND,
    the widening manufactured the mirrored `status` argument, and rounds 3-6 were consumed
    entirely by that manufactured defect. The test-side failure was never revisited.

That is worse than misattribution, not better. A loop that repairs the wrong file can be
pointed at the right one. This one was already at the right file, could not fix it, widened
anyway within the same round, and the widening's own defect then monopolised every remaining
round — so the original failure got two attempts out of seven and four rounds were spent on
damage the loop had caused itself.

The prediction from 09:59 is still ungraded on its own terms and now for a stateable reason:
its two branches (add two POSTs / change the assert to 1) describe edits to the test, the loop
made edits to the test, and NEITHER branch's edit appeared. What it did write instead is not
recoverable — see below.

⚠️ AND THE chain4-vs-chain5 WITNESS COMPARISON IS CONFOUNDED. The claim above — "an unmeasured
day-to-day variation changed which of two broken shapes a test took" — rests on chain4 having
(seeds 1, asserts 1) and chain5 (seeds 1, asserts 2) from the same pre-edit spec. Those are
not both as-drawn values:

    chain4   projects_test.go: ONE deterministic fix, no model fix. Converged green after
             round 1 (deterministic). Its final tree IS essentially its drawn tree.
    chain5   projects_test.go: a deterministic fix, a DRAINED-BODY REBUILD, and a MODEL fix,
             all in round 1 — and round 1's failure was a store.go compile error, so the
             tests never ran and the twins were never observed until ROUND 2, after all three.

So chain5's (1,2) is a post-repair value and chain4's (1,1) is not, and the difference between
them cannot be attributed to the draw. A round-1 model rewrite of that file explains it just as
well, needs no day/server story, and is a WITHIN-DRAW mechanism.

    The as-drawn twin values for chain5 are NOT RECOVERABLE. The log prints only the
    `!`-prefixed failure subset and round 1 failed to compile, so no observation of those
    tests exists before the file had been rewritten three ways.

WHAT THIS COSTS, stated plainly. "The day/server difference flipped a broken witness from the
silent kind to the loud kind" was the most mechanistic-sounding claim in this file. It is now
one of at least two explanations and not the cheaper one. The IMPORT-PATH result above is
untouched — that is measured from round-1 error output, which is genuinely as-drawn, and it is
why the control arm running right now measures round 1 and nothing else.

⚠️ CONSEQUENCE FOR THE SECOND SEPARATING DRAW, which is why this surfaced. A same-server
pre-edit redraw was about to be queued to test whether the 1 -> 2 assertion shift is
server/day or sampling. It would have inherited exactly this confound: whatever it reports,
the value is only observable after the loop has had a round at the file. Any such draw must
report (a) the round at which the twins were FIRST observed, and (b) whether projects_test.go
was model-fixed before that round — and if it was, the draw does not answer the question.

CHECKED AND CLEARED IN PASSING: _twin_witness counts `httptest.NewRequest("POST")` SITES, not
successful insertions, and 6 of 16 audited paging witnesses in the corpus serve one POST
request a second time. That looked like an instrument defect and is not. A re-serve with no
reassignment sends a drained body, seeds nothing, and adds no NewRequest site — not counted,
correctly. A drained-body REBUILD inserts a fresh NewRequest — counted, correctly. And zero
audited witnesses create a POST they never serve, which is the only direction that would
inflate. The counting basis tracks bodies-available, which is the right quantity.


SECOND CORRECTION (16:48) — the TWIN QUANTITIES in this file are post-repair, not as-drawn
===========================================================================================
Same check, applied to the v5 logs instead of chain5's, and it reaches the headline table at
the top of this file. Per-round targets:

    v5 (1st)   ROUND 1  deterministic fixes + DRAINED-BODY REBUILD + `fixing` on BOTH
                        internal/api/projects_test.go AND internal/api/router_test.go
               ROUND 2  `fixing` both test files again
               ROUND 5  `fixing` both test files a third time
               and the tree NEVER COMPILED, so no test ever RAN — the twins were never
               observed by execution at any point in the draw.

So the v5 twin values are read by _twin_witness off the FINAL tree, after three model
rewrites of both test files. chain4's are read off a tree that took ONE deterministic fix and
converged. Those are not the same kind of measurement, and the difference between them is not
attributable to the spec edit.

WITHDRAWN from the table at the top of this file:
    "did NOT fix the projects twins   1,1 -> 1,2"
    "BROKE the tasks twins            3,3 -> 1,2, 2 of 2 discriminating -> 0 of 2"

Both compare a barely-touched tree to a thrice-rewritten one. The edit may well have done
these things; nothing here shows it did.

    THE GENERAL RULE, and it is the root cause of both of today's corrections:
    ANY QUANTITY READ FROM A GENERATED TREE IS A POST-REPAIR QUANTITY unless the draw
    converged on deterministic fixes alone. Deterministic fixes are goimports-class and do
    not rewrite assertions or seeding; a model `fixing` call rewrites whatever it likes.
    The only trees in this whole comparison with clean provenance are chain4's.

WHAT SURVIVES, and it is not nothing:
  * THE IMPORT-PATH RESULT IS UNTOUCHED. 0 errors in both pre-edit draws, 4 in both
    post-edit draws, read from ROUND-1 error output — emitted before any fix of any kind.
    That is the one measurement in this file taken at a point where nothing had been
    repaired, and it is precisely why the control arm running now measures round 1 and
    nothing else.
  * THE PIPELINE IS DETERMINISTIC END TO END. The two post-edit draws are independent —
    separate generations, separate seven-round repair sequences — and land on IDENTICAL twin
    values: projects (1,2) and (2,2), tasks (1,2) and (2,2), 0 of 4 discriminating in both.
    Two independent repair sequences converging to the same four numbers is a stronger
    reproducibility result than the twin comparison it replaces.
  * The tree never compiling, seven rounds, in both post-edit draws — an outcome, not a
    tree-read quantity. Stands.

⚠️ AND THE INSTRUMENT SHOULD HAVE SAID SO. _twin_witness reads a tree and prints a confident
verdict with no idea whether the file it read was rewritten three times after it was drawn.
That is the same shape as every instrument bug found today: the tool is right about its query
and silent about the world. It now takes --log= and refuses to print an unqualified verdict
when the log shows a model `fixing` of the file it is reading.


THIRD CORRECTION (17:26) — one of the two PRE-edit arms was never measured, and the server
control goes with it
============================================================================================
Pre-flighting the control arm's grader against logs with known answers, which is the only way
to know an instrument works. It reproduced the table — and then showed the table is wrong.

GO REPORTS ERRORS PER PACKAGE AND SKIPS PACKAGES WHOSE DEPENDENCY FAILED. So "0 import-path
errors in round 1" has two completely different meanings, and a count cannot tell them apart:

    chain4   round 1: `# guildlm.dev/taskapipro/internal/api`, then
             `internal/api/projects.go:34:36: undefined: service.ErrExists`.
             A TYPE error INSIDE internal/api. You cannot reach an undefined-symbol error in
             package `service` unless the import of `service` RESOLVED — so internal/api
             compiled far enough to prove its imports were fine.
             0 errors, MEASURED. ✓

    chain5   round 1: `# guildlm.dev/taskapipro/internal/store`, `undefined: Task`.
             The string "internal/api" does not appear ANYWHERE in that round — checked, 0
             matches. internal/api was never built, its imports were never evaluated.
             0 errors, NOT MEASURED. ✗

THE TABLE, CORRECTED
    draw       projects entry   server        round-1 verdict
    chain4     PRE-edit         28 Jul        FINE, measured
    chain5     PRE-edit         29 Jul/4439   ⚠ UNMEASURED — cannot be scored
    v5 (1st)   POST-edit        29 Jul/4439   DEFECT, 4 error lines
    v5 (2nd)   POST-edit        29 Jul/4439   DEFECT, 4 error lines

⚠️ AND THAT COSTS THE SERVER CONTROL. The claim was "the import defect tracks the SPEC, not the
day and not the server — both post-edit draws produce it; neither pre-edit draw does, INCLUDING
chain5, drawn today on the same process as both v5 draws." chain5 was the entire basis for
holding the server fixed. Without it, the surviving comparison is pre-edit/28-Jul/other-server
against post-edit/29-Jul/server-4439, and spec and server differ together again.

    The import result is not refuted. Its CONTROL is gone, which is a different and smaller
    claim than the one written at 15:58 and repeated in three files since.

✅ AND THE RUNNING CONTROL ARM RESTORES IT, WHICH IT WAS NOT DESIGNED TO DO. The inert draw is
PRE-edit prose, drawn TODAY on server 4439 — the same process as both v5 draws. If it grades
FINE-and-measured it supplies the pre-edit/same-server arm chain5 was supposed to be, and the
spec attribution recovers on a properly controlled pair. If it grades UNMEASURED, it says
nothing on this axis and that is a real outcome, not a failure.

A SMALLER CLARIFICATION, same check: "BROKE the imports in THREE FILES".
Three files DID carry the unprefixed path — projects.go, router.go, tasks.go — read from the
source at 10:02, before the fix loop. The COMPILER names only internal/api/projects.go, twice,
because it stops after the first file in a package fails. Both statements are true and they
are different measurements; the table's row "three files -> 4 errors" welds a source-read file
count onto a compiler error count and reads as though the compiler named three. It did not.

INSTRUMENT: _grade_import_defect.py, with a self-test whose decisive case is that the two
zero-count arms must receive DIFFERENT verdicts. Run on all four logs above; exit 1 whenever
any arm is unmeasured, so a masked arm cannot be scored by accident.
