INDEX — what this session established, and what it took back
=============================================================
2026-07-26. One line each, with status, because the findings below are chronological and
several of them correct earlier ones. A file that was true when written and is half-false
now is the dangerous kind: nothing about it looks stale.

STANDS
  capability-walls-were-one-edit-deep     Two specs recorded as "the coder cannot do this"
                                          were one edit from green — taskflow one confused
                                          test function, tasks-api one constructor line.
                                          Both proven by hand before the runs confirmed it.
  count-repeat-is-free                    -count=4 adds no failures across 25 artifacts and
                                          fits the timeout; the live A/B showed the loop
                                          fixing the flake it had never been shown.
  closure-scoreboard-by-mutation          7 of 7 single-site closures CAUGHT on the corpus
                                          as it stands. The SOUND table.
  test-presence-is-not-test-content       ratelimit kept all five test functions and
                                          dropped every Retry-After and 200 assertion.
  the-corpus-is-not-tracked               generated/ is gitignored: "the archive" is local
                                          state, and logs/hole-hunt-rows.tsv is the only
                                          durable record of what it said.
  the-broken-archives-are-the-only-control  The two artifacts that look like rot are the
                                          only inputs exercising the BASELINE-RED rule.
  status-survivors-audited                7 candidates -> 1 new genuine hole; the rest were
                                          already closed, dead sites, or unpromised.
  unnamed-behaviour-is-unconstrained      "0 intermittent" covered only what the spec NAMES.

SUPERSEDED IN PART — each carries a banner or an appended correction
  closure-durability-is-unmeasured        Its "0 intermittent, closure HOLDS" table measures
                                          PRESENCE. Banner at the top; the mutation table
                                          replaces the inference.
  closures-are-per-site-and-per-run       The per-site rule stands; "a shape's sites are a
                                          promise's sites" does not — corrected in-file.
  status-code-holes                       Headline denominator corrected: 64 of 150 probes
                                          could answer, not 150.
  registry-drift-measured                 First number was 3x too high (invented renames);
                                          corrected in-file to 5 brittle, then repaired to
                                          27/28 surviving.

REFUTED, and left visible because the refutations cost real runs
  spec-entry count predicts difficulty    7 entries: shortener HARD, ratelimit EASY.
  corrective-clause density predicts it   identical 22 markers for that same pair.
  per-spec test budget                    taskapipro writes 48 of 49.
  per-file test count                     workapi carries 11 in one entry and drops none.
  per-file prose length                   ratelimit's entry is the longest and drops none.
  total named tests                       tasks-api names 17 across 3 trees, drops none.

OPEN
  Why taskflow drops tests and flickers assertions. Six hypotheses refuted. What IS known:
  it is the only spec unstable in either sense, it pays for a harder spec in DROPPED TESTS
  where shortener pays in TIME, and its one appended (not named) assertion is the one that
  oscillates.
  Whether the seven live closures survive the NEXT regeneration. Two specs re-run so far,
  both held.

THE METHOD LESSON, four times in one day
  Every wrong number today was wrong because of what went INTO it, not how it was computed:
  invented renames, a red tree among green ones, an archive I had touched, a window reading
  past the end of a function. When a score surprises you, audit the input list first.


=============================================================================
2026-07-26, SECOND SESSION — added below rather than merged, so the earlier
index stays readable as what was true when it was written.

STANDS
  (closure) tasks-api Content-Type      All four predictions hit; 288s, ONE deterministic
                                        fix round, 18/18 named tests. The same spec had
                                        spent 3381s and eleven rounds red one gate earlier.
                                        The "same fleet" half of that comparison was
                                        retracted the same hour — that run's invocation was
                                        never recorded.
  (closure) taskflow Content-Type       Named instead of appended: written, CAUGHT at
                                        httpjson.go, TestCreateOK unharmed, green. All four
                                        registered taskflow mutations still applied and
                                        were still CAUGHT on the fresh tree.
  the-dropped-tests-are-sort-twins      Refutes two more hypotheses (implementation gap,
                                        prose length) and restates to what holds: every
                                        dropped test that leaves a promise UNDEFENDED is
                                        half of a sort pair. Its own headline ("the only
                                        dropped tests") was refuted by the next draw and
                                        corrected in-file.
  a two-site mutation reports the       taskflow's registered sort row broke BOTH
  stronger site                         comparators at once and read CAUGHT while
                                        ListProjects alone SURVIVED. Split per site;
                                        taskapi is the control and reads CAUGHT + CAUGHT.
  the shadow gate's ambiguity guard     It read the domain type off the COMPILER MESSAGE,
  was vacuous on two of three shapes    which only one of the three shapes carries. With a
                                        `Name()` method in the project the gate turned a
                                        compile error into a GREEN, permanently vacuous
                                        test. Now read off the source; corpus blast radius
                                        measured at 0 of 6 typed shadows.

OPERATIONAL, and the expensive one
  i-deleted-the-corpus                  `rm -f generated/*/[a-z]*` emptied 136 of 243
                                        artifacts and took every go.mod. 105 module files
                                        were reconstructed from surviving self-imports
                                        (_restore_gomod.py); the 20 flat -v4 trees are
                                        regenerating through _rebuild_corpus.sh. The flat
                                        _fail-* archives and both real BASELINE-RED
                                        controls are gone for good; generated/plantedred-v4
                                        now carries that property and is tracked in git.
  the suite was writing into the corpus tests/test_corpus_keeps_a_red_baseline built every
                                        artifact IN PLACE, leaving nine 8MB binaries in the
                                        corpus. Fixed to build into a temp dir. The same
                                        test also counted an EMPTY directory as a red
                                        baseline, so it passed all the way through the
                                        deletion.

QUEUED BY THE ACCIDENT — the measurement it forces
  Rebuilding all twenty -v4 trees is a full corpus REDRAW: same specs, same model, a week
  apart. _redraw_diff.py compares the tracked hole-hunt rows against the post-rebuild sweep
  and separates verdict flips from sites that merely moved. That answers "do the closures
  survive the next regeneration" for every spec at once, where the programme had answered
  it for two of five.

LATER THE SAME AFTERNOON — three instrument defects, all found by real data
  a tree with no tests is not         `go test` exits 0 with no _test.go, so a mutant passed
  "undefended"                        and verdict_for printed its strongest claim about a
                                      half-generated usersapi. New NOTESTS verdict, threaded
                                      through every consumer that classifies verdicts.
  the redraw comparator dropped 45%   Keyed by (artifact, file, shape), the eight same-shape
  of the corpus                       sites in one file collapsed to one row: 149 -> 82.
                                      Ordinal added. It had passed its own self-test; only
                                      production data exposed it.
  instruments must ask whether the    _corpus_state.py: REFUSE when the tree being graded is
  corpus is moving                    in flight, WARN when anything else is generating.
                                      _hole_hunt refuses a whole-corpus sweep outright,
                                      because that sweep overwrites the tracked row file.

  The common shape: each tool was correct on the inputs I had thought to plant, and wrong on
  an input the day produced. A fixture suite proves what you imagined; pointing the tool at
  live state is a different test, and it found something all three times.


=============================================================================
2026-07-27 — the boundary survivors, adjudicated

STANDS
  the-witness-was-off-the-boundary    bitset satisfies every rule established the day
                                      before — invariant NAMED, own test WRITTEN, right
                                      thing ASSERTED, assertion SURVIVED the redraw — and
                                      the mutation still lives, because the witness is 200
                                      and the flip moves behaviour at 128. Third failure
                                      mode: not "does the assertion exist" but "can it
                                      discriminate". Rule gains a clause: at the value
                                      where the invariant could fail.
  nine-of-fifteen                     All 15 boundary survivors probed one at a time. 9 are
                                      real holes; 6 are green for reasons that have nothing
                                      to do with tests (3 paginate inert, 1 config masked
                                      downstream, 1 dead site, 1 message-only). Acting on
                                      the raw SURVIVED count would have meant asking five
                                      specs to defend behaviour that cannot change.
  inertness-is-semantic               All four inert cases got past _hole_hunt's syntactic
                                      filter and none COULD have been caught by it: the
                                      reason lives three lines away, in a later guard, in
                                      the callers, or inside strconv. The residue of a
                                      syntactic filter needs an empirical one.
  expreval-branch-is-unreachable      Measured, not read: a planted panic survives every
                                      string of length 0..3 over the operator alphabet
                                      (5,220) plus 200,000 random ones, while the same
                                      panic three lines away fires on the empty string.
  two-guards-hide-an-untested-        taskflow's `[]` wire shape is held by store make()
  contract                            AND paginate's []T{} return. Neither mutation alone
                                      is observable; BOTH together give `null`. Single-site
                                      mutation testing structurally cannot see a property
                                      with redundant guards — and three artifacts with only
                                      ONE guard ship `null` on a one-line change with green
                                      suites.
  the-corpus-never-lists-empty        Every list test in every HTTP artifact populates the
                                      store first. The empty case is not weakly asserted;
                                      it is never reached. Raw-JSON-body assertions in the
                                      whole corpus: zero.

SUPERSEDED IN PART — corrected in place
  paginate "nothing was broken"       True of the mutation, false about the property. The
                                      inert verdict stands; the conclusion drawn from it
                                      did not. Banner added in-file.
  "DeepEqual passes on both"          Wrong, and measured wrong the same hour: DeepEqual
                                      against a non-nil empty slice DISCRIMINATES `[]` from
                                      `null`. len() is the blind one.

MEASURED AND SKIPPED
  numeric-constant shape              A seventh shape flipping documented constants by one:
                                      ~10 sites corpus-wide and the config defaults among
                                      them already pinned by TestLoadDefaults. A gate that
                                      would never fail.
  nil-slice as a hole-hunt SHAPE      12 sites, but 3 of 6 HTTP artifacts would report
                                      SURVIVED for the redundancy reason above. A
                                      majority-inert shape is how this day started. Kept as
                                      a _bound_probe family instead, where observability is
                                      proven per site.

TWO TOOL DEFECTS, same shape as yesterday's three
  a self-test that was not mine       _hole_hunt dispatched --self-test at MODULE level, so
                                      importing it for its mutators made a NEW tool's
                                      --self-test run _hole_hunt's fixtures and print its
                                      green. Found by reading output that was too familiar.
  the writer list was already stale   _corpus_state matched driver scripts by name, and four
                                      drivers written this morning were invisible to it —
                                      including in the minutes each spends running go test
                                      between builds, when no guildlm-build process exists
                                      at all. Matched by convention now, with the negative
                                      case pinned.

OPEN
  The nine closures. Five Chain (one edit, five specs — an N=5 replication rather than
  five anecdotes), two config zero-guards, two bitset witnesses. Generating now; graded by
  _grade_closures.sh with the shape that found each hole.
  Three empty-list closures, deliberately held back so the N=5 comparison stays clean.
